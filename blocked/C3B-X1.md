# C3B-X1 — 反復ランナーが 2 本同時に走っているため中断

- 日付: 2026-09-15
- 反復: 3(run-id `20260915-123431`)
- 状態: **実装に一切着手していない**(読み取りのみ。作業ツリーへ 1 バイトも書いていない)

## 何が起きているか

`C3B-X1` の対象ファイルを**別の実装者が今まさに編集している**。読み取りの途中で
`internal/commands/casino_shared.go` の `closeBoardIfSettled` の行番号が 433 → 450 に動いたことで気付いた。

プロセス一覧(2026-09-15 12:53、`Win32_Process`):

| PID | 起動 | コマンド |
|---|---|---|
| 58112 | 12:04:29 | `loop.mjs --repo . --engine claude --evaluate every --unattended`(run-id `20260915-120429`) |
| 52512 | 12:34:31 | `loop.mjs --repo . --engine claude --evaluate every --unattended`(run-id `20260915-123431` = こちら) |
| 59776 | 12:48:59 | `claude -p --model opus --max-turns 150 --permission-mode acceptEdits` |
| 5404 | 12:49:49 | `claude -p --model opus --max-turns 150 --permission-mode acceptEdits` |

**ランナーが 2 本、同じ作業ツリーに対して走っている。** 実装者も 2 つ起動しており、片方がこちら、
もう片方が同じ `C3B-X1` を実装している。相手の未コミット差分がツリーに載っている:

```
 M internal/casino/session.go            (+47/-...)
 M internal/commands/casino_sessions.go
 M internal/commands/casino_shared.go
 M internal/commands/highlow.go          ← 12:53 の間に増えた
```

相手の `session.go` の差分は `WithSession` の `done` を落とし、`Exists` を足し、コメントに
`(C3B-X1)` と明記している。**同じタスクの同じ設計**を書いている。
ファイル更新時刻は 12:52:03 / 12:52:24 / 12:52:43 と数十秒おきに進んでおり、まだ進行中。

## なぜ止めたか

これは `PROGRESS.md` 反復 13 の Notes が記録した事故の再発そのもの:

> **作業ツリーに反復ランナーが 2 本走っていた**。同じ 3 ファイルを交互に書いていたため、
> こちらの編集途中の内容が相手のコミット `e701563「作業途中の保存」` に取り込まれている。
> **再開は 1 本だけにすること。**

`C3B-X1` は `WithSession` の戻り値を変える変更で、**呼び出し側が全ファイル同時に壊れる**種類の作業。
2 人が交互に書けば、どちらの中間状態も他方のコミットに混ざり、`go build` の赤が誰のものか分からなくなる。
危険地帯(預かりの取り残し)を扱うタスクで、それは許容できない。

## 決めたいこと

**どちらのランナーを残し、どちらを止めるか。** 人にしか決められない(プロセスの kill と、
相手の未コミット差分の扱いを含むため)。

## 選択肢

1. **(推奨)古い方(PID 58112, 12:04 起動)を残し、こちら(PID 52512, 12:34 起動)を止める。**
   - 内容: 52512 を止める。相手の実装者 (PID 59776 か 5404 のどちらか) が `C3B-X1` を書き切ってコミットする。
   - 帰結: 費用ゼロ、可逆。ツリーに載っている相手の差分がそのまま活きる。こちらは何も書いていないので失うものがない。
   - リスク: 低。相手が同じ done-when に沿って書いている(差分のコメントで確認済み)。

2. **新しい方(こちら)を残し、58112 を止める。**
   - 内容: 58112 とその実装者を kill し、ツリーの未コミット差分を捨てるか拾うかを決めてからこちらが実装する。
   - 帰結: 相手の 4 ファイル分の作業(+168 行)を捨てるか、レビューして引き継ぐ手間が要る。
   - リスク: 中。中途半端な差分を引き継ぐと、どこまで一貫しているか分からない状態から始めることになる。

3. **両方止めて、人が手で 1 本だけ起動し直す。**
   - 内容: 52512 と 58112 を両方止め、ツリーを `git stash` か `git checkout` で既知の状態へ戻してから 1 本だけ起動する。
   - 帰結: 最も確実。相手の作業は捨てる(または stash に退避)。
   - リスク: 低。ただしやり直しの分だけ時間がかかる。

## 推奨

**選択肢 1。** こちらは読み取りしかしておらず、止めても失うものが無い。相手は既に 4 ファイルへ
一貫した差分(`WithSession` から `done` を外し、`Exists` を足し、`closeBoardIfSettled` を
両方の出口の唯一の door にする)を書いている — これは `C3B-X1` の done-when (1) が求めている形そのもの。
**走り続けている方に書かせるのが、書きかけを混ぜない唯一の方法。**

## 調べた設計上の結論(次の実装者への申し送り)

止める前に読み終えていたので、書ける形で残す。相手の差分と突き合わせて使えるはず。

- **消す機構は 3 つあった、2 つではない。** `WithSession` の `done`、`Close`(生きた盤面)、
  `Remove`(期限切れ = 掃除人のもの)。潰すべきは `done` で、`Close` / `Remove` の 2 つは
  **期限切れプロトコルの両半分**なので 1 つにはできない —
  `Close` は期限切れを拒否し(掃除人が精算中の盤面を取り上げないため)、`Remove` は期限切れを受ける。
  1 本にするなら「盤面を消すマネージャ呼び出しは `casino_shared.go` にしか現れない」という
  **ファイル単位の不変条件**にするのが正しい。相手が `Exists` を足しているのはこのため
  (door が期限切れ盤面を見られないと掃除人の分を通せない)。
- **押下決着の書き換えは `settleDealtHand`(`blackjack.go:443`)がそのまま雛形になる。**
  `MarkRefundPending(sessionID, payout)` で確定配当を盤面に固定 → `settle` → `closeBoardIfSettled`。
  印は「もう動かない盤面」の宣言でもあるので(`RefundPending()` の gate)、精算に失敗しても
  次の押下がポットを育てられない。H&L・BJ に同じ形を入れればよい。
- **`/duel` は既に適合している。** `accept` / `decline` は `closeBoardIfSettled` を通っており、
  `done=true` を返すのは `errDuelBadState` の 1 か所だけ。
- **`return true, errXBadState` を消すのは無害どころか改善。** 今の形は「State の型が違う」だけで
  盤面を消しており、預かりが残っていれば**それ自体が取り残しを作る**。error だけ返して盤面を残せば、
  掃除人が拾う。
- **構文木走査は `go/types` 無しで十分精密にできる。** AST から
  「型が `*casino.SessionManager` のフィールド名・引数名」を集め(`sessions` / `mgr`)、
  その識別子に対する `Close` / `Remove` の呼び出しだけを落とす。`reminder_test.go` などの
  `server.Close()` を巻き込まずに済み、複数行に折り返された呼び出しも AST なら形が変わらない。

---

# 反復 4 — 同じ事故が再発した(ランナー 2 本が生きたまま)

- 日付: 2026-09-15 13:02
- 反復: 4(run-id `20260915-123431`)
- 状態: **実装済みの追記を自分で取り消し、ツリーを先行側へ明け渡した**

## プロセス(`Win32_Process`、13:01)

| PID | 起動 | 役割 |
|---|---|---|
| 58112 | 12:04:29 | ランナー(run-id `20260915-120429`)— **先行** |
| 52512 | 12:34:31 | ランナー(run-id `20260915-123431`)— **こちら。後発** |
| 59776 | 12:48:59 | 実装者(58112 の子)— **同じ C3B-X1 を今も書いている** |
| 27332 | 12:57:54 | 実装者(52512 の子)— こちら |

反復 3 の中断記録を残したあと、**58112 は止まらず次の反復を回した。** 記録を置くだけでは再発する。

## 今回どこまで進み、なぜ引いたか

作業ツリーには先行側の実装が既に載っており、done-when (1)(2)(3) は満たされていた
(`WithSession` は `func(*Session) error`、押下決着は H&L・BJ とも「盤面を保持 → 精算 → 共通入口」、
`casino_shared_test.go` は `go/parser` + `go/ast` の走査)。`go build` / `go vet` / `go test ./...` は
8 パッケージとも ok。残っていたのは done-when (4) の回帰テスト 3 本と経路表だけだったので、
3 本を書いて H&L・BJ の 2 本が通るところまで確認した。

その直後、**自分が触っていない行でコンパイルが壊れた**:

```
internal\commands\blackjack_test.go:1360:15: undefined: blackjackSessionIDOf
internal\commands\highlow_test.go:1457:15: undefined: highLowSessionIDOf
```

先行側が**同じ 3 本の回帰テストを、同じファイルの末尾に、書きかけで**追記していた。
30 秒後にはその識別子ごと消えており(ファイル更新時刻 13:01:19 → 13:01:32 → 13:02:06)、
ツリーは書き込みのたびに形が変わっている。証拠:
`.harness/runs/20260915-123431/blocked-C3B-X1-iter4-evidence.txt`。

**自分の追記 3 ブロック(計 8360 バイト)は削除した。** 残すと、同じ内容のテストが 1 パッケージに
二重に入るか、反復 13 の `e701563` と同じく書きかけのまま先行側のコミットに取り込まれる。
後発が引くのが、混ぜない唯一の方法。

## 決めたいこと

**ランナーを 1 本にする。** どちらを残すかは人が決める。

- **選択肢 A(推奨): 後発 52512 を止める。** こちらのランナーを落とせば、先行 58112 が
  C3B-X1 を書き切って 1 つのコミットにできる。失うものは無い — 今回の追記は既に消した。
  可逆。リスクは、先行側が同じ二重書き込みで既に壊れた差分を抱えている可能性だけ。
- **選択肢 B: 先行 58112 を止める。** 作業ツリーの未コミット差分は先行側のものなので、
  止めた瞬間に**書きかけのテストファイル**が宙に浮く。残すなら誰かが仕上げる必要がある。
  A より高くつく。
- **選択肢 C: 両方止めて手で仕上げる。** 残りは回帰テスト 3 本と `PROGRESS.md` の経路表だけ。
  確実だが、ループの利点を捨てる。

## 残っている作業(どちらのランナーが続けても同じ)

1. 回帰テスト 3 本(押下決着 → 精算失敗 → 盤面保持 → 復旧後の掃除で預かりが戻る)。
   H&L と BJ は**書いて通ることを確認済み**。形は、時計を固定したマネージャで開始 →
   `bank.settleErr` を立てて押下 → `Len()==1` と預かり 100 を確認 → `settleErr` を戻し
   **押下せずに** `sweepIdleBoards(..., opened.Add(casino.DefaultSessionTTL))` → 残高と `Len()==0`。
   duel は `duelFailingBank{acceptErr: ...}` で ⚔️ を失敗させ、`RegisterComponent(c)` を入れて
   掃除人の settler を通す(`SettleGame` は 0 回のまま = 上限付きの与信を通さない)。
2. `PROGRESS.md` の経路表を「盤面を消す経路は `closeBoardIfSettled` の 1 本だけ」に更新する。
