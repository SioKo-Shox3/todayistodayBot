# 実装計画: フェーズC-1(カジノ経済基盤+スロット)

**規模トリアージ: 重量(heavy)**(`CLAUDE.md`「規模トリアージ」)。理由は §リスクレベル に詳述するが、
本フェーズは危険地帯(新規の永続化層+単一ライター規律)・公開データ形式(`data/casino.json`)・
並行処理(掲示 goroutine)・全コマンドに波及する `Register` 契約に同時に触れるため。
全ゲート+基幹フェーズの heavy-artillery(削除テスト/サボタージュテスト)を適用する。

**改訂履歴**: 本文書は計画レビュー(一次 Claude `plan-reviewer` / 二次 Codex CLI)の指摘を反映した
**改訂版**である。ユーザー決定(起動時キャッチアップ・経済上限方式・プラットフォーム非依存・
7分割コミット)とオーケストレーター裁定(doc コメントは英語 / `os.Rename` の atomicity 表現の訂正)を
取り込み、blocker 7件・should-fix 13件・nit 10件を反映済み。**スロット配当表の数値は改訂前後で不変**
(3系統で独立検証済みのため — §Task 4)。

**参照した設計書**: `Docs/superpowers/specs/2026-07-10-casino-c1-design.md`(レビュー指摘反映済みの最終版、全206行)

**参照した既存コード**(実際に読んで確認済み):
- `internal/store/events.go`(単一ライターmutex + temp file&rename 永続化の原型。「atomic write」という表現は §3.7-1 のとおり訂正した)
- `internal/store/events_test.go`(並行書き込みテストの原型)
- `internal/commands/registry.go` / `registry_test.go`
- `internal/commands/weather.go`, `dice.go`, `dice_test.go`(純粋関数切り出しパターン)
- `internal/commands/schedule.go`, `schedule_result.go`, `reminder.go`, `schedule_test.go`(store注入・エラー変換・サブコマンド・`floatPtr`等の既存ヘルパー)
- `internal/commands/help.go`, `help_test.go`(自動探索— 変更不要の確認)
- `cmd/bot/main.go`, `main_test.go`
- `Docs/agent-guide/architecture.md`, `build-and-verify.md`, `coding-style.md`, `orchestration.md`
- `.gitignore`(`/data/` が既にカジノのJSONもカバーすることを確認)
- discordgo v0.29.0 のソース(`go env GOMODCACHE` 配下)を直接読み、`Contexts *[]InteractionContextType`、`DefaultMemberPermissions *int64`、`PermissionAdministrator = 1<<3`、`Member.Permissions int64`、`Session.handle`(イベントは`go`で個別goroutine起動される=Handle内での待機は安全)、`ApplicationCommandInteractionDataOption.ChannelValue(nil)`/`UserValue(nil)`、`WebhookEdit.Content *string`、`InteractionResponseEdit`、`ChannelMessageSend`/`ChannelMessageSendEmbed` を確認済み。

**スロット配当表の数値的検証**: Pythonの厳密分数演算(`fractions.Fraction`)で解析的にRTP/ニアミス頻度を算出し、さらに実際のGo `math/rand`(v1 API、本プロジェクトが`dice.go`で使っているのと同じAPI)で5つの乱数シード×100万回スピンを実行して数値を実証した(詳細はTask 4)。検証に使った一時ファイルはリポジトリ直下に誤生成したが、全て削除しgit statusがクリーンであることを作成者・オーケストレーター双方で確認済み。

---

## 1. Goal & 期待される挙動変化

- サーバーごとに独立した2層通貨(コイン=管理者発行/チップ=Bot自動配布)経済が動く。
- 毎日、隠れトレンド方式で「読める」相場(70〜140、四捨五入)が生成され、毎朝9:00 JSTに指定チャンネルへ自動掲示される。
- ユーザーは `/balance` `/daily` `/rate` `/exchange` `/slot` `/rank` を使え、サーバー管理者は `/casino-admin` でコイン発行・掲示チャンネル設定ができる。
- `/slot` はRTP93%±1%・ニアミス演出(リールを1個ずつ約0.8秒間隔で止める)・💎💎💎/7️⃣7️⃣7️⃣の公開祝いを持つ。
- Bot起動後、ダウンタイムがあっても「レートが存在しない日」は発生しない(フォールバック生成)。
- `internal/commands.Register` が同名コマンドの二重登録を起動時にpanicで検出するようになる(既存8+新規7=15コマンドの名前衝突を機械的に防止)。

## 2. Scope

### 新規ファイル

```
internal/casino/types.go            型定義(Data/GuildEconomy/DailyRate/UserAccount/TrendState/EventKind/randSource)+経済上限定数
internal/casino/errors.go           センチネル/カスタムエラー型(日本語文言を持たない)
internal/casino/store.go            永続化・Update/Snapshot トランザクション・シングルトン・経済メソッド群
internal/casino/store_test.go
internal/casino/economy.go          レート遷移・両替・デイリー計算(純粋関数、discordgo非依存)
internal/casino/economy_test.go
internal/casino/slot.go             リール抽選(純粋関数、discordgo非依存)
internal/casino/slot_test.go
internal/casino/announce.go         9時スケジューラのタイミングロジック(discordgo非依存)
internal/casino/announce_test.go
internal/casino/concurrency_test.go (または store_test.go に追記。実装者の裁量)

internal/commands/casino_shared.go  requireGuildContext/requireAdministrator/permPtr/guildOnlyContexts/sparkline/rateRankCommentary
internal/commands/casino_shared_test.go
internal/commands/balance.go / balance_test.go
internal/commands/daily.go / daily_test.go
internal/commands/rate.go / rate_test.go
internal/commands/exchange.go / exchange_test.go
internal/commands/slot.go / slot_test.go        ← internal/casino/slot.go とは別ファイル(別パッケージ)。実装時に混同しないこと。
internal/commands/rank.go / rank_test.go
internal/commands/casino_admin.go / casino_admin_test.go
internal/commands/casino_announce.go / casino_announce_test.go   ← discordgo依存のスケジューラ配線(main.goから呼ばれる、Commandではない)
```

### 変更ファイル

```
internal/commands/registry.go       Register に重複名検出を追加
internal/commands/registry_test.go  重複名panicのテストを追加
cmd/bot/main.go                     掲示スケジューラのgoroutine起動を1箇所追加
Docs/agent-guide/architecture.md    internal/casino を構造表に追加、危険地帯に4点目を追加
CLAUDE.md / AGENTS.md               絶対規則3に casino.Default() を追記(両ファイル同一内容で)
```

### 書き込み禁止(明示)

**「禁止」の定義を2種類に分ける**(検証手順と衝突しないようにするための明確化):
**(a) 編集・コミット禁止** = ファイルを手で書き換えない・git に載せない。
**(b) 生成も禁止** = そもそも作らせない。

- `config.json`(秘密情報)— **(b) 生成も禁止**。読むだけ。diff・ログに混入させない。
- `bin/`(ビルド生成物)— **(a) 編集・コミット禁止。検証コマンド(`make build`)による生成は許可**。
  `.gitignore` 済みなのでコミットには載らない。生成物を手で編集しないこと。
- `data/`(実行時生成物)— **(a) 編集・コミット禁止。テスト・手動確認による生成は許可**。
  `data/casino.json` は `.gitignore` の `/data/` で既にカバー済み(追加のgitignore変更は不要)。
  **自動テストは `data/` を一切触らない**(必ず `t.TempDir()` を使う)。手動確認(`go run ./cmd/bot`)は
  `data/casino.json` を生成してよいが、**コミット前に `git status` がクリーンであることを確認する**。
- `internal/store/*`(フェーズBの既存ストア。今回のフェーズでは触らない)— **(b) 変更禁止**。
- `internal/choseisama/*`, `internal/weather/*`, `internal/today/*`, `internal/config/*`(無関係)
- `cmd/bot/main_test.go`(変更不要 — 純粋関数`findCommandByName`のみをテストしており、今回の変更の影響を受けない。念のため実行して確認はする)
- `internal/commands/help.go` / `help_test.go`(`All()`から自動収集するため変更不要。`help_test.go`は合成`defs`を使っており登録数に依存しないことを確認済み)

## 3. Approach(全体アーキテクチャ)

### 3.1 パッケージ依存方向(新規に追加される矢印)

```
cmd/bot                → internal/commands, internal/config   (既存のまま)
internal/commands/*    → internal/casino                      (新規: 7コマンド + casino_announce.go)
internal/casino/*      → 標準ライブラリのみ(discordgo非依存)   (新規: store/economy/slot/announce)
```

**重要な決定(設計書の記述を補強する解釈)**: 設計書は `internal/casino/announce.go` を「毎朝の定時掲示(常駐goroutine)」と書いているが、コードベース全体を確認したところ `internal/weather`・`internal/today`・`internal/choseisama`・`internal/store` はいずれも discordgo を一切importしていない(既存の一貫した規約)。この規約に従い、**`internal/casino` パッケージ全体(announce.go含む)を discordgo非依存に保つ**。9時掲示の「何を送るか」を決める純粋なタイミング/データロジックは `internal/casino/announce.go` に置き、実際に `*discordgo.Session` でメッセージを送る処理は新規 `internal/commands/casino_announce.go`(discordgoコールバックの配線のみ)に置く。`cmd/bot/main.go` はこの `internal/commands` の関数を1回呼ぶだけ。これは設計書の意図(経済ロジックとdiscordgoの分離)をコードベースの既存規約に矛盾なく適用した結果であり、設計からの逸脱ではない。

### 3.2 データ構造(`internal/casino/types.go`)

設計書 §アーキテクチャ のGo構造体スケッチに厳密に従う。`DailyRate` 構造体は設計書に明示されていないが `Rates []DailyRate` という記述から要求される最小フィールド(Date/Rate/Trend)に加えて、**その日の暴騰/暴落イベント種別 `Event` を永続化する**:

```go
package casino

// TrendState is a guild's hidden day-to-day rate momentum ("隠れトレンド").
type TrendState string

const (
    TrendBull TrendState = "bull"
    TrendBear TrendState = "bear"
    TrendFlat TrendState = "flat"
)

// EventKind records whether a day's rate move came from the 5% 暴騰/暴落
// draw (設計書 §日次レート) rather than the normal drift+noise path. It is
// PERSISTED rather than re-derived from the day-over-day percentage,
// because the 70/140 clamp can swallow the evidence: a +15% surge from 130
// clamps to 140, i.e. only +7.7% day-over-day, so a threshold-based guess
// would silently drop 🚀 on a genuine surge day (計算: 130*1.15 = 149.5 →
// clamp 140 → (140-130)/130 = 7.69% < 10%). Display code must read this
// field, never guess.
type EventKind string

const (
    EventNone  EventKind = "none"
    EventSurge EventKind = "surge" // 🚀 +15..30%
    EventCrash EventKind = "crash" // 💥 -15..30%
)

// Data is the root persisted structure: guild ID -> that guild's economy
// state. Top-level map, exactly as specified by the design doc.
type Data map[string]*GuildEconomy

type GuildEconomy struct {
    AnnounceChannelID string                  `json:"announce_channel_id"`
    Rates             []DailyRate             `json:"rates"` // oldest..newest, capped at 30 entries
    LastAnnounced     string                  `json:"last_announced"` // JST calendar date "2026-07-10"
    Users             map[string]*UserAccount `json:"users"`
}

type DailyRate struct {
    Date  string     `json:"date"`  // JST calendar date "2026-07-10"
    Rate  int        `json:"rate"`
    Trend TrendState `json:"trend"`
    Event EventKind  `json:"event"` // "" (legacy/hand-edited records) is treated as EventNone by all readers
}

type UserAccount struct {
    Coins         int64  `json:"coins"`
    Chips         int64  `json:"chips"`
    LastDailyDate string `json:"last_daily_date"`
    StreakDays    int    `json:"streak_days"`
}

// randSource is the minimal randomness surface economy.go and slot.go need
// injected for deterministic tests. *rand.Rand (via rand.New) satisfies it
// structurally; production code always constructs a real *rand.Rand (never
// uses the global math/rand top-level functions, for reproducibility).
// Defined HERE (types.go), not in economy.go, because Store embeds it — see
// §3.5 on task/commit ordering.
type randSource interface {
    Float64() float64
    Intn(n int) int
}
```

**`EventKind` を追加したことによる波及**(実装時に漏らさないこと): `nextRate` の戻り値(§Task 3)、
`ensureTodayRateLocked` の `DailyRate` 組み立て(§Task 5)、掲示 embed の 🚀/💥 と相場コメント
(§Task 8 / §Task 16)、`/rate` の表示(§Task 11)、および `DailyRate` を組み立てる全テスト。

### 3.3 Store の骨格(単一ライター規律 — `internal/store/events.go` を踏襲)

```go
const DefaultPath = "data/casino.json"

type Store struct {
    mu   sync.Mutex
    path string
    rng  randSource // production: rand.New(rand.NewSource(time.Now().UnixNano())); read ONLY while mu is held (i.e. from inside an Update closure) — *rand.Rand is not safe for concurrent use
}

func New(path string) *Store { return &Store{path: path, rng: rand.New(rand.NewSource(time.Now().UnixNano()))} }

var (
    defaultOnce  sync.Once
    defaultStore *Store
)

func Default() *Store {
    defaultOnce.Do(func() { defaultStore = New(DefaultPath) })
    return defaultStore
}

// Update runs fn against the current on-disk Data under the store's single
// writer lock, and persists fn's mutations if fn returns nil (a non-nil
// error aborts the write — the file is left untouched). This is the ONLY
// way production code may mutate casino data; every balance change, rate
// generation, or admin mint must go through Update so concurrent commands
// (e.g. two simultaneous /slot bets in the same guild) serialize correctly
// — mirrors internal/store.Store's mutex discipline.
//
// RE-ENTRANCY CONTRACT (危険地帯 — violating this deadlocks the bot):
// s.mu is a plain, NON-reentrant sync.Mutex. fn therefore must NOT call any
// method that takes the lock (Update, Snapshot, EnsureTodayRate,
// EnsureCasinoAccess, ClaimDaily, Exchange*, Spin, ViewAccount, TopAssets,
// RecentRates, Mint, SetAnnounceChannel, MarkAnnounced,
// collectDailyAnnouncements). Use the unexported *Locked helpers instead.
// fn must also not perform network I/O, Discord API calls, or sleeps — the
// whole guild economy is blocked for its duration.
func (s *Store) Update(fn func(*Data) error) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    data, err := s.readLocked()
    if err != nil { return err }
    if err := fn(&data); err != nil { return err }
    return s.writeLocked(data)
}

// Snapshot returns the current on-disk Data, read under the store's lock so
// it cannot observe a concurrent Update's half-applied read-modify-write.
// The returned value is freshly unmarshalled on every call and aliased by
// nobody (the Store keeps no in-memory copy), so the caller may read it
// freely after Snapshot returns — but mutating it has NO effect on the file
// (use Update for that).
//
// This deliberately replaces an earlier `Read(fn func(Data) error)` design:
// running an arbitrary callback while holding the mutex reproduced Update's
// re-entrancy hazard (any locking method called from the callback
// deadlocks) and its I/O-under-lock hazard, without Update's justification
// for taking that risk. Returning a value has no such contract to violate.
func (s *Store) Snapshot() (Data, error) {
    s.mu.Lock()
    defer s.mu.Unlock()
    return s.readLocked()
}

// readLocked parses the JSON file. Caller must hold mu. It ALWAYS returns a
// non-nil Data on success — see the nil-map hazard below. Temp-file glob for
// writeLocked: ".casino-*.json.tmp" (events.go uses
// ".chosei-events-*.json.tmp" — same convention, different prefix so the two
// stores' temp files are visually distinguishable in a shared data/
// directory during debugging).
func (s *Store) readLocked() (Data, error) {
    raw, err := os.ReadFile(s.path)
    if err != nil {
        if os.IsNotExist(err) {
            return Data{}, nil // pre-first-command state, not an error
        }
        return nil, fmt.Errorf("casino: reading %q: %w", s.path, err)
    }
    if len(raw) == 0 {
        return Data{}, nil // truncated/zero-byte file — see hazard note below
    }
    var d Data
    if err := json.Unmarshal(raw, &d); err != nil {
        return nil, fmt.Errorf("casino: parsing %q: %w", s.path, err)
    }
    if d == nil {
        d = Data{} // JSON literal `null` unmarshals a map to nil
    }
    return d, nil
}
```

**⚠️ nilマップ経路(実装者が絶対に踏んではいけない罠 — 恒久クラッシュループの原因)**:
既存 `internal/store/events.go` の `readLocked` は **3経路**で nil を返す
(`events.go:143-144` の `os.IsNotExist`、`events.go:148-150` の `len(data)==0`、
JSON 内容が `null` のときの `json.Unmarshal`)。既存は `[]EventRecord`(nil スライスにも
`append` できる)なので無害だが、**`Data` は map なので nil のまま `ensureGuildLocked` に渡すと
`(*d)[guildID] = ...` が `panic: assignment to entry in nil map` になる**。
`len(data)==0` は現実に到達する — 電源断で0バイトの `data/casino.json` が残り得るため
(§3.7 の `tmp.Sync()` を入れても完全には消えない)。
discordgo はイベントハンドラを **recover 無しの個別 goroutine** で起動する
(`discordgo@v0.29.0/event.go:171` `go eh.eventHandler.Handle(s, i)`)ので、この panic は
**Bot プロセス全体を落とす**。壊れたファイルは残るため、再起動後もカジノコマンドを叩くたびに
落ち続ける**恒久クラッシュループ**になる。
→ **`readLocked` は欠損・0バイト・JSON `null` のいずれの場合も非 nil の `Data{}` を返すこと。
`json.Unmarshal` 成功後も必ず `if d == nil { d = Data{} }` を通すこと。**
同種の nil マップ/nil ポインタ経路は他に2箇所ある(いずれも本計画で対処済み):
`GuildEconomy.Users` が nil のケース(→ `ensureGuildLocked` が補う、§Task 5)と、
`{"guild1": null}` のように**マップの値が nil ポインタ**のケース
(→ `collectDailyAnnouncements` は `range *d` で得た値を直接使わず必ず `ensureGuildLocked(d, guildID)`
を通す、§Task 6)。

**危険地帯(CLAUDE.md 絶対規則3と同格、設計書の同規律)**: 7コマンド + 掲示スケジューラは必ず `casino.Default()` を経由して同一の `*Store`(=同一の `sync.Mutex`)を共有すること。`casino.New(path)` は**テストコードでの `t.TempDir()` 使用専用**(`internal/store` と全く同じ規約)。この規律はTask 18(architecture.md更新)で文書化する。

### 3.4 エラー方針(`internal/casino/errors.go`)

`internal/store/events.go` を確認した結果、**日本語ユーザー向け文言は常に `internal/commands` 層だけが持つ**という一貫した規約を発見した(`internal/store` は英語のエラーのみを返し、日本語❌文言は `schedule.go`/`schedule_result.go`/`reminder.go` の中で組み立てられている。`reminder.go` の `errors.As(err, &apiErr)` によるAPI固有エラーの判定パターンが好例)。

この規約に従い、**`internal/casino` は構造化されたセンチネル/カスタムエラー型のみを返し、日本語❌文言は一切持たない**。`internal/commands/*.go` が `errors.Is`/`errors.As` で判定し、日本語文言に翻訳する。設計書の「エラーハンドリング」節の4つの❌文言は、**この翻訳後の最終文字列としてコマンド層で使う**(既存の慣習と整合)。

```go
var (
    ErrAlreadyClaimedToday  = errors.New("casino: daily bonus already claimed today")
    ErrBetOutOfRange        = errors.New("casino: bet out of range")
    ErrInvalidAmount        = errors.New("casino: amount must be a positive integer")
    ErrBelowMinimumExchange = errors.New("casino: exchange result would be below the 1-coin minimum")
    ErrAmountOverflow       = errors.New("casino: amount would overflow int64")
    // ErrChipCapExceeded / ErrCoinCapExceeded are the §3.5 economic-cap
    // violations: the credit would push the account past MaxChips/MaxCoins.
    // Returning them from an Update closure aborts the whole transaction, so
    // nothing (not even a partial debit) is persisted.
    ErrChipCapExceeded = errors.New("casino: chip balance would exceed MaxChips")
    ErrCoinCapExceeded = errors.New("casino: coin balance would exceed MaxCoins")
)

// ErrInsufficientChips carries the current balance so the command layer can
// render "❌ チップが足りません(現在: N枚)" without a second store round-trip.
type ErrInsufficientChips struct{ Balance int64 }
func (e *ErrInsufficientChips) Error() string { return fmt.Sprintf("casino: insufficient chips (balance=%d)", e.Balance) }

// ErrInsufficientCoins mirrors ErrInsufficientChips for coin→chip exchange.
type ErrInsufficientCoins struct{ Balance int64 }
func (e *ErrInsufficientCoins) Error() string { return fmt.Sprintf("casino: insufficient coins (balance=%d)", e.Balance) }
```

いずれも**日本語文言を持たない**。`ExchangeCoinToChip`/`Mint` の「入力量は1以上」チェックには `ErrInvalidAmount` を、`ExchangeChipToCoin` の「**計算結果**が1コイン未満」チェックには `ErrBelowMinimumExchange` を使い分ける(設計書: コイン→チップは入力側の最低量、チップ→コインは結果側の最低量 — チェック対象が異なるので別センチネルにする)。

### 3.5 経済上限(int64 オーバーフローを「検査」ではなく「構造」で封じる)

`int64` の全域を扱おうとすると、総資産計算 `Chips + Coins × rate` のように**掛け算と足し算が
連鎖する経路すべて**にガードが必要になり、1箇所でも漏れると静かに負値へラップする
(検知しにくい破損)。そこで **経済そのものに上限を定義し、上限を守っている限りオーバーフローが
数学的に起こり得ない状態にする**(ユーザー決定)。

```go
// MaxChips / MaxCoins bound every account balance. They are NOT arbitrary:
// they are chosen so that the widest intermediate this package ever
// computes stays far inside int64. See the margin calculation below.
const (
    MaxChips int64 = 1_000_000_000_000 // 1e12
    MaxCoins int64 = 1_000_000_000_000 // 1e12
)
```

**安全余裕の計算**(int64 上限 = 9,223,372,036,854,775,807 ≈ 9.2234e18):

| 経路 | 最大値 | 計算 | int64 上限との比 |
|---|---|---|---|
| 総資産 `Chips + Coins×rate` | 141,000,000,000,000(1.41e14) | 1e12 + 1e12×140 | **約 65,413 倍の余裕** |
| 両替の中間積 `coins×rate×97` | 13,580,000,000,000,000(1.358e16) | 1e12×140×97 | **約 679 倍の余裕** |
| 両替の中間積 `chips×97` | 97,000,000,000,000(9.7e13) | 1e12×97 | 約 95,086 倍の余裕 |
| スロット払い戻し | 196,000 | bet上限1,000 × 最高倍率196 | 無視できる |

いずれも桁で余裕があり、`rate` の上限は 140(クランプ上限)で確定しているので、
**上限を守る限り本パッケージのどの中間値も int64 を超えない**。1e12(1兆)は
デイリーボーナス(200〜1,000)・ベット上限(1,000)に対して十分に大きく、遊びの制約にならない。

**上限の強制は2層**(既存の前例: `internal/commands/schedule.go:73-83` の `validateScheduleDays` が
Discord の `MinValue`/`MaxValue` と `internal/commands` 側の再検査を二重化している):

1. **Discord 側**: `/casino-admin mint` の `amount` に `MinValue` **と `MaxValue` の両方**を付ける
   (現行計画は `MinValue` のみ)。
2. **ストア側(本命)**: **すべての credit 経路**で上限を検査し、超過ならエラーを返す。対象は
   `Chips +=`(デイリーボーナス / 両替の出力側 / スロット払い戻し / ウェルカムボーナス)と
   `Coins +=`(Mint / 両替の出力側)。**Discord のオプション検証は防御にならない**
   (`discordgo@v0.29.0/interactions.go:460-465` の `IntValue()` は `int64(o.Value.(float64))` で、
   直接 API を叩けば ~2^53 まで通る)。

上限超過は `Update` のクロージャから `ErrChipCapExceeded`/`ErrCoinCapExceeded` を返す形で表現する。
`Update` は fn が非nilエラーを返すと `writeLocked` を呼ばないので、**トランザクションは一切
永続化されない**(控除だけ通って加算が弾かれる、という半端な状態にならない)。
この「エラー時に残高が1枚も変わらないこと」は §Task 5 のテストで明示的に検証する。

```go
// creditChipsLocked adds delta to account.Chips, refusing to exceed
// MaxChips. Caller must already hold the Store's lock. Every chip credit in
// this package goes through here — do NOT write `account.Chips += x`
// anywhere else.
func creditChipsLocked(account *UserAccount, delta int64) error {
    if delta < 0 || account.Chips > MaxChips-delta {
        return ErrChipCapExceeded
    }
    account.Chips += delta
    return nil
}

// creditCoinsLocked mirrors creditChipsLocked for MaxCoins.
func creditCoinsLocked(account *UserAccount, delta int64) error {
    if delta < 0 || account.Coins > MaxCoins-delta {
        return ErrCoinCapExceeded
    }
    account.Coins += delta
    return nil
}
```

`account.Chips > MaxChips-delta` は `MaxChips-delta ≥ 0`(∵ delta ≤ MaxChips は上位の入力検証で保証、
かつ delta が MaxChips を超える場合も `MaxChips-delta` が負になるだけで比較は正しく偽/真を返す)なので
**それ自体がオーバーフローしない**書き方である(`account.Chips+delta > MaxChips` と書くと加算側で
ラップし得る)。

**両替の乗算前ガードの過剰拒否について(意図的な設計)**: `exchangeCoinToChip` の
`coins > math.MaxInt64/(int64(rate)*97)` というガードは、**最終商が int64 に収まる入力まで拒否する**
(除算前の積で判定しているため)。ただし本方式ではこのガードの閾値は
`MaxInt64/(140×97) = 679,187,926,130,690`(約 6.79e14)であり、口座の `Coins` は `MaxCoins = 1e12` を
超えられないので、**このガードは本番経路では到達不能**である。到達不能なまま残すのは意図的で、
純粋関数 `exchangeCoinToChip` を上限方式の外から呼んだ場合(将来の再利用・テスト)の最後の砦にするため。

### 3.6 TDD の Red の取り方(全タスク共通の規律)

**「コンパイルが通らない」を Red と呼ばない。** ファイルやシンボルが存在しないことによる
compile failure は、テストが**意味のある失敗をしたことの証拠にならない**(テストが1行も実行されて
いない)。各タスクは次の順序で進める:

1. **コンパイル可能な最小 stub を先に置く** — 型・関数シグネチャだけを定義し、本体はゼロ値/
   `errors.New("not implemented")` を返す。**`panic` は使わない**(`CLAUDE.md`: パニックで
   制御フローを作らない。唯一の例外は Task 1 の `Register` — あれは仕様としての panic)。
2. **テストを書いて実行し、`FAIL` の実出力を得る**(= 本物の Red。アサーションが評価された証拠)。
3. **実装して Green にする。**

各タスクの「Red」の記述はこの規律を前提とする。

**テストの結果が「いつ実行したか」に依存してはならない(全テスト共通の必須規律)**:
`go test ./...` は1日のどの時刻に走っても同じ結果でなければならない。したがって

1. **時計は必ず注入する。** 本番の `time.Now` / `casino.RealSleepUntil` を渡してよいのは
   `internal/commands.StartCasinoAnnounceScheduler`(本番ラッパー)**だけ**。
   テストから叩く関数は `now func() time.Time` と `sleep casino.SleepFunc` を引数で受け取る形にする
   (`RunAnnounceScheduler` / `startAnnounceScheduler` / `Store` の各メソッド / `buildResponse` など)。
   テストは `time.Date(...)` で作った固定時刻を渡す。
2. **「起きなかったこと」を検証するテストは、起きなかった理由が1つに絞れる状態で書く。**
   掲示系は `announceWindowOpen`(09:00 下限)という第2の抑止条件があるため、
   時計を固定しないと「窓が閉じていたから呼ばれなかった」だけで通ってしまい、
   **検証したい条件を一度も確かめていない空振りテスト**になる。
   `build-and-verify.md`「失敗し得ない出力は証拠にならない」に該当する。
3. **実クロックで待機しない。** `sleep` をスタブせずに走らせると、最悪 `go test` の既定タイムアウト
   (パッケージあたり10分)まで固まって FAIL する。Done ゲートが時刻依存で落ちるのは事故である。

### 3.7 プラットフォーム非依存(Linux でも Windows でも同一動作)

開発機は Windows、想定運用先は Linux。**GOOS 依存コード・build tag を一切導入しない**。

1. **`os.Rename` の atomicity を過大に主張しない(表現の訂正)。** `go doc os.Rename` の実出力:
   > Even within the same directory, on non-Unix platforms Rename is not an atomic operation.

   したがって本計画は「atomic write」という表現を撤回する。正しい表現は:
   **「Linux(同一ディレクトリ)では rename は atomic。Windows では atomic 保証がないので
   best-effort。どちらの OS でも、壊れた/空のファイルを読んでも panic しない読み込みガード
   (§3.3 の nilマップ経路)で実害を受け止める。」**
   **永続化方式は作り直さない** — `temp file + os.Rename` はそもそも移植可能なイディオムであり、
   フェーズBの `internal/store` で実績がある。
2. **`tmp.Sync()` を `os.Rename` の前に追加する**(両 OS で durability 向上、移植可能):
   `tmp.Write(data)` → **`tmp.Sync()`** → `tmp.Close()` → `os.Rename(...)`。
   既存 `internal/store/events.go:179-191` にはこの `Sync()` が無いため、**新規 `internal/casino` と
   既存 `internal/store` の間に意図的な差異が生まれる**(既存 store は本フェーズの禁止パスなので
   触らない)。既存側の是正は**残課題 → code-gardening**(→ §残課題)。
3. **`time.Local` に依存しない。** 日付境界はすべて JST 固定。特に「昨日」の計算は
   **`now` のロケーションのまま `AddDate` してはならない** — TZ が `America/*`/`Europe/*` の Linux
   サーバでは DST 切替日に1日が23h/25hになり、JST 換算で「一昨日」や「今日」になり得る。
   **必ず `now.In(jst)` を取ってから `AddDate(0,0,-1)` する**(§Task 5 の `ClaimDaily`)。
4. **`time.FixedZone("JST", 9*3600)` を使う理由**: JST は夏時間を持たないので固定オフセットで正確、
   かつ **tzdata ファイルを必要としない**(`time.LoadLocation("Asia/Tokyo")` は最小構成の Linux
   コンテナで失敗し得る)。設計書 §スケジューラと時刻 の指定どおり。
5. **パス結合は必ず `filepath.Join`。** 区切り文字(`/` や `\`)をハードコードしない。
   例外は `DefaultPath = "data/casino.json"` の定数リテラルのみで、これは既存
   `internal/store.DefaultPath = "data/chosei-events.json"` と同じ慣習であり、Go の `os` パッケージは
   Windows でも `/` を受理する(フェーズBで実績あり)。
6. **`-race` は Linux/CI で実行する。** 開発機(Windows)は C コンパイラが無く `go test -race` が
   動かない既知事情がある。**コードは Linux/CI で `-race` が通る前提でレース安全に書く。**
   Windows 開発機では非race のテスト成功を代替証拠とし、**`-race` を省略した旨を必ず報告する**
   (黙って省略しない)。

### 3.8 全コマンド共通の入口 `EnsureCasinoAccess`(口座自動開設と当日レートの保証)

設計書 L33-34 は「カジノ系コマンドを**初めて使ったユーザー**には口座を自動作成し、ウェルカムボーナス
1,000チップを付与する」と要求している。しかし**本処理と同じトランザクションで口座を作ると、
本処理が失敗したときに口座作成ごとロールバックされる**(`Update` は fn がエラーを返すと何も書かない)。
例: 初回の `/exchange` が残高不足で失敗すると、直前に作られた口座とウェルカムボーナスが消える。
また `/rate`・`/rank` は userID をストアへ渡さないので、そもそも口座を作れない。
結果として「どのコマンドを最初に叩いたか」で挙動が変わる。

→ **口座開設(+ウェルカムボーナス)と当日レート保証を、失敗し得る本処理より前の
独立したトランザクションとして確定させる**:

```go
// EnsureCasinoAccess commits, as its OWN transaction, the two invariants
// every casino command depends on: (1) guildID has a DailyRate for today's
// JST calendar date (設計書「レートは絶対に欠損しない」), and (2)
// guildID/userID has an account, credited with the 1,000-chip welcome bonus
// if this is its first ever casino interaction (設計書 L33-34).
//
// It runs BEFORE the caller's own Update so that a failing main operation
// (insufficient balance, cap exceeded, ...) cannot roll the new account and
// its welcome bonus back. Idempotent: calling it repeatedly neither
// re-grants the bonus nor regenerates the rate.
//
// ALL SEVEN casino commands call this first — including /rate and /rank,
// which otherwise would never open an account.
func (s *Store) EnsureCasinoAccess(guildID, userID string, now time.Time) error
```

呼び出し規約(§Task 9〜16 で各コマンドに明記):
- `/balance` `/daily` `/rate` `/exchange` `/slot` `/rank` — 実行者の userID で1回呼ぶ。
- `/casino-admin` — 実行者(管理者)の userID で1回呼び、`mint` サブコマンドは**発行先ユーザーの
  userID でもう1回**呼んでから `Mint` する(発行先の口座とウェルカムボーナスが、`Mint` の上限超過
  エラーでロールバックされないようにするため)。

---

## Task 1 — `internal/commands/registry.go`: 重複コマンド名の起動時panic検出

**目的**: 今回7コマンド追加するにあたり、同名の後勝ち/先勝ちバグを機械的に防ぐ。

**変更ファイル**: `internal/commands/registry.go`, `internal/commands/registry_test.go`

**現状**(読了済み、`internal/commands/registry.go:20-22`):
```go
func Register(cmd Command) {
	registered = append(registered, cmd)
}
```

**Red(失敗するテストを先に書く)**: `registry_test.go` に以下を追加。
```go
func TestRegister_DuplicateName_Panics(t *testing.T) {
	resetForTest()
	defer func() {
		if recover() == nil {
			t.Fatal("expected Register to panic on a duplicate command name")
		}
	}()
	Register(&fakeCommand{name: "dup"})
	Register(&fakeCommand{name: "dup"})
}
```
この時点で `go test ./internal/commands/...` を実行し、失敗する(panicしない=recover()がnilでt.Fatalに到達する)ことを確認してから次へ進む。

**Green(最小実装)**:
```go
var (
	registered      []Command
	registeredNames = map[string]bool{}
)

func Register(cmd Command) {
	name := cmd.Definition().Name
	if registeredNames[name] {
		panic(fmt.Sprintf("commands: duplicate command name %q registered via Register — every command must have a unique Definition().Name", name))
	}
	registeredNames[name] = true
	registered = append(registered, cmd)
}

func resetForTest() {
	registered = nil
	registeredNames = map[string]bool{}
}
```
`import "fmt"` を追加。

**なぜ安全か**: `Register` は各コマンドファイルの `init()` から呼ばれるだけなので、`Definition()` を1回追加で呼ぶコストは無視できる。全ての既存コマンド名(ping/help/weather/today/dice/schedule/schedule-result/reminder)は相互に重複していないため、この変更により既存テスト・既存起動シーケンスが壊れることはない。

**テスト**: `go test ./internal/commands/... -run TestRegister` — `TestRegisterAndAll`(既存、無変更で通ること)と `TestRegister_DuplicateName_Panics`(新規)の両方が `ok`。

**Red の取り方**: このタスクだけは §3.6 の stub 手順が不要 — `Register` は既に存在するので、
新規テストは**コンパイルが通ったうえで実行時に失敗する**(panic しないので `recover()` が nil を
返し `t.Fatal` に到達する)。これが本物の Red。

**完了条件**: `go build ./...` エラー0、`go vet ./...` 警告0、`go test ./internal/commands/...` で新旧テストすべて `ok`。

**コミット境界**: **Commit 1**(単独でコミット可能、他の変更に依存しない)。

---

## Task 2 — `internal/casino`: 型定義 + センチネルエラー + 永続化基盤(`types.go` / `errors.go` / `store.go`)

**目的**: 設計書 §アーキテクチャ の永続化層と、パッケージ全体が参照する型・エラー・定数を実装する。
この時点では経済計算メソッド(`EnsureTodayRate` 等)は含めない(Task 5で追加) — まず「箱」を作り、
読み書きの原子性・並行性をテストで固める。

**変更ファイル**: `internal/casino/types.go`(新規), `internal/casino/errors.go`(新規),
`internal/casino/store.go`(新規), `internal/casino/store_test.go`(新規)

**⚠️ このタスクが `types.go`/`errors.go` を含む理由(前方参照の解消)**: 当初の計画は
`randSource` を Task 3(`economy.go`)で、`ErrAmountOverflow` を Task 5 で定義していたが、
`Store` 構造体は `rng randSource` フィールドを持ち、Task 3 の両替関数は `ErrAmountOverflow` を返すため、
**そのタスク順ではどちらもコンパイルできない**(同一パッケージであることは「順序不問」を意味するだけで、
まだ*どこにも*書かれていない識別子は参照できない)。したがって
**パッケージ全体で共有される型・インターフェース・センチネルエラー・上限定数はすべてこのタスクで定義する**。

**このタスクで定義するもの(§3.2 / §3.4 / §3.5 の内容)**:
- `types.go`: `TrendState`(+3定数)、`EventKind`(+3定数)、`Data`、`GuildEconomy`、`DailyRate`、
  `UserAccount`、`randSource`、`MaxChips`/`MaxCoins`
- `errors.go`: `ErrAlreadyClaimedToday` / `ErrBetOutOfRange` / `ErrInvalidAmount` /
  `ErrBelowMinimumExchange` / `ErrAmountOverflow` / `ErrChipCapExceeded` / `ErrCoinCapExceeded` /
  `ErrInsufficientChips` / `ErrInsufficientCoins`
- `store.go`: `DefaultPath`、`Store`、`New`、`Default`、`Update`、`Snapshot`、`readLocked`、`writeLocked`、
  `jst`、`jstDate`、`ensureGuildLocked`、`ensureAccountLocked`、`creditChipsLocked`、`creditCoinsLocked`、
  `Mint`、`SetAnnounceChannel`
  (`Mint`/`SetAnnounceChannel` は当日レートを必要としないのでここで完結できる。§Task 5 参照)

**テストファイルの package(必須)**: `internal/casino` のテストは**すべて `package casino`(内部テスト)**
とする。`package casino_test` にすると `st.rng` へのスタブ代入や `ensureGuildLocked` などの非公開
ヘルパーに触れず、書き直しになる。

**Red→Green の進め方**(§3.6 の規律): まず `types.go`/`errors.go` と、本体がゼロ値/
`errors.New("not implemented")` を返すだけの `store.go` の stub を置いて**コンパイルが通る状態**にし、
下のテストを書いて `go test` を実行し **`FAIL` の実出力**を得る。そこから実装して Green にする。

**テスト(`store_test.go`)**:
1. `TestStore_Update_PersistsAndIsReadableByFreshInstance` — `t.TempDir()` に `New(path)` で作り、`Update(func(d *Data) error { (*d)["guild1"] = &GuildEconomy{Users: map[string]*UserAccount{}}; return nil })`、別の `New(path)` インスタンスで `Snapshot()` して読み戻せることを確認(`events_test.go` と同型)。
2. `TestStore_Update_MissingFile_TreatsAsEmptyNotError` — 存在しないパスへの `Snapshot`/`Update` がエラーにならず、**非nilの空 `Data{}`** を渡すこと(`d == nil` でないことを明示的にアサートする)。
3. **`TestStore_Read_EmptyFileTreatedAsEmptyData`** — **0バイトのファイルを実際に作ってから** `Update` し、panic せず空 `Data{}` として扱われること。(現行の「存在しないパス」だけのテストは**失敗し得ないテスト**だった — §3.3 の nilマップ経路を参照)
4. **`TestStore_Read_NullJSONTreatedAsEmptyData`** — ファイルに `null` の4バイトを書いてから `Update` し、同じく panic せず空 `Data{}` として扱われること。
5. **`TestStore_Mint_AfterZeroByteFile_DoesNotPanic`** — 0バイトファイルを置いた状態で実際のカジノ操作(`Mint`)を通し、`panic: assignment to entry in nil map` が起きないことを検証する統合的な回帰テスト(恒久クラッシュループの再発防止)。
6. `TestStore_Update_FnReturnsError_DoesNotPersist` — `fn` が非nilエラーを返した場合、ファイルが書き込まれない(または既存内容が変化しない)こと。**新規テスト、既存storeパッケージに対応物なし** — Update特有の「読み→変更→書き」トランザクション性を検証する重要なテスト。
7. `TestStore_Update_ConcurrentWritesDoNotCorruptFile` — `events_test.go` の並行テストを移植。20並行goroutineがそれぞれ異なるguildIDを`Update`し、最終的なJSONが破損せず20エントリすべて存在すること。
8. `TestDefault_ReturnsSameInstanceAcrossCalls` — `events_test.go` と同型。
9. `TestStore_Mint_{CreditsCoinsAutoCreatesAccount,BelowMinimum,ExceedsMaxCoins_NoMutation}` — 上限ちょうど(`MaxCoins` に到達する mint は成功)/上限+1(`ErrCoinCapExceeded` で**残高が1枚も変化しない**)の境界を含む。
10. `TestStore_SetAnnounceChannel_Persists`
11. `TestCreditChipsLocked_{AtCap,OverCap,NegativeDelta}` / `TestCreditCoinsLocked_{AtCap,OverCap,NegativeDelta}` — §3.5 のヘルパーの境界を直接検証する。

**実装のポイント**:
- `readLocked` は **欠損・0バイト・JSON `null` のいずれの場合も非 nil の `Data{}` を返す**
  (§3.3 のコードと nilマップ経路の注記のとおり。`json.Unmarshal` 成功後の `if d == nil { d = Data{} }` を忘れない)。
- `writeLocked` は `internal/store/events.go:162-193` とほぼ同型だが、**`tmp.Write` の後・`tmp.Close` の前に
  `tmp.Sync()` を入れる**(§3.7-2。既存 store との意図的な差異)。
- 一時ファイル名は `.casino-*.json.tmp`。パス操作は `filepath.Dir`/`filepath.Join` のみを使う(§3.7-5)。
- `New()` は `rng` フィールドも初期化する(Task 5 で使用開始するが、フィールド自体はここで用意しておく。
  Go は未使用の構造体フィールドを警告しないので `go vet` は通る)。
- `Mint` は `creditCoinsLocked` を通すこと(`account.Coins += amount` を直書きしない)。

**完了条件**: `go build ./internal/casino/...` エラー0、`go test ./internal/casino/... -run 'TestStore|TestDefault|TestCredit'` すべて `ok`。`go vet ./internal/casino/...` 警告0。

**コミット境界**: **Commit 2**(Task 2 のみ。この時点で `internal/commands` からは一切参照されないので、
`go build ./... && go vet ./... && go test ./...` が独立して緑になる)。

---

## Task 3 — `internal/casino/economy.go`: 純粋関数(レート遷移・両替・デイリー計算)

**目的**: 設計書 §日次レート・§デイリーボーナス・§両替 の数式を、discordgo/store非依存の純粋関数として実装する。

**変更ファイル**: `internal/casino/economy.go`(新規), `internal/casino/economy_test.go`(新規)

### 3-A. `nextRate`(`randSource` は Task 2 の `types.go` で定義済み)

```go
const (
	baseRate            = 100
	minRate             = 70
	maxRate             = 140
	trendTransitionProb = 0.15
	eventProb           = 0.05
)

// nextRate computes tomorrow's rate, trend and event kind from today's
// rate/trend, using rng for all randomness. Pure — no I/O, no locking, no
// wall-clock reads. hasPrev=false means "this guild's very first day":
// returns exactly (baseRate, TrendFlat, EventNone) with NO randomness
// consumed (day 1 is a fixed starting point per 設計書 "基準100", not a
// random draw — a disclosed interpretation since the design doc doesn't
// state day-1 behavior explicitly).
//
// The returned EventKind is what makes the 🚀/💥 announcement reliable: it
// reports the DRAW, not the visible outcome, so a surge whose move was
// eaten by the 70/140 clamp is still reported as EventSurge (§3.2).
//
// Precedence when hasPrev is true (disclosed interpretation of 設計書,
// which doesn't spell out interaction with the 15% roll): a boundary-forced
// transition REPLACES the 15% roll for that day (not "both apply") — if
// prevRate is at the 70/140 clamp, the trend is forced toward the center
// and the independent 15% transition check is skipped entirely for this
// call.
func nextRate(prevRate int, prevTrend TrendState, hasPrev bool, rng randSource) (rate int, trend TrendState, event EventKind) {
	if !hasPrev {
		return baseRate, TrendFlat, EventNone
	}

	trend = prevTrend
	switch {
	case prevRate <= minRate:
		trend = TrendBull
	case prevRate >= maxRate:
		trend = TrendBear
	case rng.Float64() < trendTransitionProb:
		trend = otherTrend(prevTrend, rng)
	}

	var pct float64
	event = EventNone
	if rng.Float64() < eventProb {
		magnitude := 15 + rng.Float64()*15 // 15..30
		if rng.Float64() < 0.5 {
			pct, event = magnitude, EventSurge
		} else {
			pct, event = -magnitude, EventCrash
		}
	} else {
		var drift float64
		switch trend {
		case TrendBull:
			drift = 1 + rng.Float64()*3 // 1..4
		case TrendBear:
			drift = -(1 + rng.Float64()*3) // -1..-4
		case TrendFlat:
			drift = 0
		}
		noise := -3 + rng.Float64()*6 // -3..3
		pct = drift + noise
	}

	raw := float64(prevRate) * (1 + pct/100)
	clamped := math.Max(minRate, math.Min(maxRate, raw))
	return int(math.Round(clamped)), trend, event // math.Round = 四捨五入(半分は0から遠い方へ), never floor
}

func otherTrend(current TrendState, rng randSource) TrendState {
	candidates := make([]TrendState, 0, 2)
	for _, t := range []TrendState{TrendBull, TrendBear, TrendFlat} {
		if t != current {
			candidates = append(candidates, t)
		}
	}
	return candidates[rng.Intn(len(candidates))]
}
```

### 3-B. デイリーボーナス

```go
// dailyBonusAmount returns the chip amount for a claim landing on the
// given streak (already incremented for today's claim; streak>=1).
// 200 + 50*(min(streak,7)-1), plus +500 "大入り袋" when streak%7==0.
func dailyBonusAmount(streak int) int64 {
	capped := streak
	if capped > 7 {
		capped = 7
	}
	amount := int64(200 + 50*(capped-1))
	if streak%7 == 0 {
		amount += 500
	}
	return amount
}
```

### 3-C. 両替(除算1回のみ)

```go
// exchangeCoinToChip converts coins to chips at rate, 3% fee applied on the
// output side, truncated via exactly ONE final integer division (設計書
// — do not compute an intermediate percentage step, which would
// double-truncate). Returns ErrAmountOverflow if coins*rate*97 would
// overflow int64.
func exchangeCoinToChip(coins int64, rate int) (int64, error) {
	if coins > math.MaxInt64/(int64(rate)*97) {
		return 0, ErrAmountOverflow
	}
	return coins * int64(rate) * 97 / 100, nil
}

// exchangeChipToCoin converts chips to coins at rate, single final division.
func exchangeChipToCoin(chips int64, rate int) (int64, error) {
	if chips > math.MaxInt64/97 {
		return 0, ErrAmountOverflow
	}
	return chips * 97 / (100 * int64(rate)), nil
}
```

**注**: `ErrAmountOverflow` は Task 2 の `errors.go` で定義済み。overflowガードは設計書に明記されていない**防御的追加**であり、明示的にそう記載する。§3.5 の経済上限方式により本番経路では到達不能になるが(口座の `Coins` は `MaxCoins = 1e12`、ガード閾値は約 6.79e14)、純粋関数を上限方式の外から呼んだ場合の最後の砦として残す(§3.5 末尾の「過剰拒否について」を参照)。

### 3-D. 相場コメントは `internal/commands` 側に置く(配置の明示)

設計書 L66 が要求する「相場コメント1行」は**日本語のユーザー向け文言**なので、§3.4 の規約
(日本語文言は `internal/commands` 層だけが持つ)に従い **`internal/commands/casino_shared.go` の
`marketCommentary`** として実装する(→ §Task 8)。`internal/casino` は `EventKind` と `TrendState` という
**構造化された事実**だけを提供し、文章は持たない。同じ理由で既存の `rateRankCommentary` も
`casino_shared.go` にある。

**テスト(`economy_test.go`)— Red→Green で1関数ずつ(§3.6 の stub 手順で本物の Red を取る)**:
1. `nextRate` の境界固定: `TestNextRate_NeverEscapesClampRange` — シード付き `rand.New(rand.NewSource(N))` で1万〜10万回連続遷移させ、常に `70<=rate<=140` であることを確認(統計テスト)。
2. `TestNextRate_ForcedCentralTransition_AtFloor` — `prevRate=70` を渡し、常に `trend==TrendBull` が返ることを、rngに「何が来ても関係ない」ことを示す固定値スタブで複数回確認。
3. `TestNextRate_ForcedCentralTransition_AtCeiling` — 同様に `prevRate=140` → `TrendBear`。
4. **`TestNextRate_ForcedCentralTransition_PersistsWhileAtBoundary`** — 端に**複数日**留まるケース。
   外向きイベント(`prevRate=140` で `EventSurge` 相当の値を返すスタブ)を連続で与え、
   **端にいる限り毎日 `TrendBear` へ強制される**ことを3日以上検証する。
   (現行は 70→Bull / 140→Bear の**単発**しか見ておらず、「端にいる限り継続」という設計の核心を
   検証していなかった。コード案自体は正しい構造なので、これは**テストの穴を塞ぐ追加**である)
5. `TestNextRate_FirstDay_ReturnsBaseRateNoRandomness` — `hasPrev=false` で `(100, TrendFlat, EventNone)` を返すこと、かつrngが一度も呼ばれないこと(呼ばれたら即失敗するスタブ `randSource` を渡して確認)。
6. `TestNextRate_RoundsHalfAwayFromZero_NotFloor` — 個別分岐(通常ドリフト、`eventProb`判定を確実に外す/内すスタブ値)を使い、四捨五入であって切り捨てでないことを検証する具体的な数値例(例: 期待値100.5相当の入力で101になること)。
   - **注**: 各分岐(強制転換/確率転換/暴騰暴落/通常ドリフト)は個別の決定的 `randSource` スタブ(呼び出しごとに固定値または定数を返す)でテストする。呼び出し回数・順序をこの計画では固定しない — 実装内の分岐構造に依存するため、各テストはその分岐が単独で起きる値だけを注意深く選んで検証する。
7. **`TestNextRate_EventKind_{Surge,Crash,None}`** — 暴騰/暴落分岐に入るスタブ値で `EventSurge`/`EventCrash` が、通常ドリフト分岐で `EventNone` が返ること。
8. **`TestNextRate_SurgeClampedToCeiling_StillReportsEventSurge`** — **クランプでイベント情報が失われないことの回帰テスト**。`prevRate=130` で +15% の暴騰を引かせると `130×1.15 = 149.5` → クランプ 140 → 前日比は `(140-130)/130 = 7.69%` で10%しきい値を下回るが、**戻り値の `EventKind` は `EventSurge` であること**を検証する(前日比からの推測方式なら🚀が消えていたケース)。
9. `TestDailyBonusAmount_Table` — streak=1→200, 2→250, 7→1000(500+500), 8→500, 14→1000 のテーブル駆動テスト。
10. `TestExchangeCoinToChip_SingleTruncation` — `coins=3, rate=101` → `3*101*97/100 = 29391/100 = 293`。
    二重切り捨て実装なら 291 になる(`floor(3*101/100)*97 = 3*97 = 291`、`3*floor(101*97/100) = 3*97 = 291`)ので、**この値は二重切り捨てを識別できる**。
11. `TestExchangeChipToCoin_SingleTruncation` — **`chips=1100, rate=70` → `15`**(具体値を確定。導出は下記)。
12. `TestExchange_RoundTrip_NeverIncreasesTotalValue` — coins→chips→coins の往復で、結果のcoinsが元のcoins以下であることをプロパティテスト(複数の`(coins, rate)`組み合わせで)。設計書の受け入れ条件。
13. `TestExchangeCoinToChip_OverflowGuard` / `TestExchangeChipToCoin_OverflowGuard` — `math.MaxInt64` に近い値で `ErrAmountOverflow` が返ること。

#### `TestExchangeChipToCoin_SingleTruncation` の具体値の導出(計算過程)

チップ→コインは **`floor(97c/100)` を先に取ってから `r` で割る形は、単一除算 `floor(97c/(100r))` と
数学的に常に一致する** — つまり「手数料を先に切り捨てる」二重切り捨ては**識別不能**である。証明:

> 整数 `c ≥ 0`, `r ≥ 1` に対し `97c = 100q + s`(`0 ≤ s ≤ 99`)と置くと `floor(97c/100) = q`。
> さらに `q = ar + b`(`0 ≤ b ≤ r-1`)と置くと二重切り捨ては `floor(q/r) = a`。
> 一方 `floor(97c/(100r)) = floor((100q+s)/(100r)) = a + floor((100b+s)/(100r))` であり、
> `100b + s ≤ 100(r-1) + 99 = 100r - 1 < 100r` なので第2項は必ず 0。よって両者は常に `a`。∎

(機械的確認: `r ∈ [70,140] × c ∈ [1,200000]` の 14,200,000 組を全数走査して**差分0件**。
レビュー指摘は「一致するケースが**多い**」だったが、実際は**常に一致**するため、
その形の二重切り捨てを狙うテストは原理的に書けない。)

したがって**識別すべき誤実装は「先にコインへ換算してから手数料を掛ける」形** `floor(floor(c/r)*97/100)` である。
確定値 **`chips = 1100`, `rate = 70`**:

- **正しい実装(単一除算)**: `1100 × 97 = 106700`、`100 × 70 = 7000`、`106700 / 7000 = 15.2428…` → **15**
  (余り `106700 - 7000×15 = 1700 ≠ 0` なので、これは本物の切り捨てを1回踏んでいる)
- **誤実装(換算→手数料の二重切り捨て)**: `floor(1100/70) = 15`、`15 × 97 / 100 = 1455/100 = 14.55` → **14**

→ 期待値 15 に対し誤実装は 14 を返すので、**このテストは失敗し得る**。
`rate = 70` はクランプ下限で有効範囲内(70〜140)。

**完了条件**: `go test ./internal/casino/... -run 'TestNextRate|TestDailyBonus|TestExchange'` すべて `ok`。`go vet ./internal/casino/...` 警告0。

**コミット境界**: **Commit 3**(Task 3 + Task 5。§コミット境界 の対応表を参照)。

---

## Task 4 — `internal/casino/slot.go`: リール抽選(確定済み配当表)

**目的**: 設計書 §スロット の「配当表は計画段階で確定する」という指示に従い、以下の**確定済み・検証済み**の重み/配当表をそのまま実装する。

**変更ファイル**: `internal/casino/slot.go`(新規), `internal/casino/slot_test.go`(新規)

### 4-A. 確定した配当表(このプランで数値的に導出・検証済み)

**導出方法**: `fractions.Fraction`(Python標準ライブラリ)による厳密分数演算で **6シンボル×3リール = 6³ = 216通り**の順序付き結果を全列挙し、期待値(RTP)とニアミス確率を**厳密値**として算出した。さらに実際の `math/rand`(Go, v1 API — 本プロジェクトが `dice.go` で使っているのと同じパッケージ)で5つの異なるシード×100万回スピンを実行し、解析値と実測値が一致することを確認した。
(改訂前の本文は「全27通り」と書いていたが、これは `3³ = 27` との混同による**誤記**であり、
正しくは 216 通り。導出そのものは 216 通りで行われており、**RTP・ニアミス頻度の数値に変更はない**。)

| symbol | 絵文字 | 重み(/100) | 3個揃い倍率 |
|---|---|---|---|
| cherry | 🍒 | 28 | ×7 |
| lemon | 🍋 | 22 | ×16 |
| grape | 🍇 | 21 | ×22 |
| bell | 🔔 | 18 | ×32 |
| diamond | 💎 | 7 | ×98 |
| seven | 7️⃣ | 4 | ×196 |

追加ルール: 🍒がちょうど2個(3個目は別シンボル)→ ベット額返還(倍率×1)。

**重み・倍率とも頻度順に単調**(cherry最頻出/最低倍率 → seven最希少/最高倍率)であり、設計書の「🍒(高頻度・小当たり)/🍋🍇🔔(中)/💎7️⃣(激レア・大当たり)」という3層構造と整合する(探索の過程で「cherryの倍率がlemon/grapeより高くなってしまう」候補を複数リジェクトした上でこの表に確定した)。

**解析的な厳密値**(分数演算、丸め誤差なし。各リールの重みは /100 なので分母は `100³ = 1,000,000`):
- 3個揃いの寄与 = **760556/1000000 = 0.760556**
  (`28³×7 + 22³×16 + 21³×22 + 18³×32 + 7³×98 + 4³×196`
  `= 153664 + 170368 + 203742 + 186624 + 33614 + 12544 = 760556`)
- 🍒ちょうど2個(×1返還)の寄与 = **169344/1000000 = 0.169344**(`3 × 28² × (100-28) = 3 × 784 × 72 = 169344`)
- **RTP(期待倍率) = (760556 + 169344)/1000000 = 929900/1000000 = 9299/10000 = 92.99%**(目標93%±1%の帯の中央付近)
- ニアミス(💎または7️⃣がちょうど2個一致、3個目は別) = `3×7²×93 + 3×4²×96 = 13671 + 4608 = ` **18279/1000000 = 1/54.71**(「数十スピンに1回程度」の目標を満たす)
- 🍒2個返還 確率 = 169344/1000000 = **1/5.91**

**この4つの分子(760556 / 169344 / 929900 / 18279)は SF-4 の literal 固定テストで機械的に検査する**
(下記テスト6)。配当表を「もっともらしい別の数値」に差し替えると、広いRTP許容帯 `[0.92, 0.94]` は
通ってしまってもこのテストが必ず落ちる。

**実測値**(Go `math/rand`, 100万スピン/シード、ベット=100):

| seed | RTP | ニアミス頻度 |
|---|---|---|
| 1 | 0.93079 | 1-in-55.2 |
| 2 | 0.92539 | 1-in-54.3 |
| 42 | 0.92424 | 1-in-54.7 |
| 12345 | 0.92148 | 1-in-54.2 |
| 999 | 0.92293 | 1-in-54.7 |

全シードが `[0.92, 0.94]` の帯に収まり、解析値(92.99%, 1-in-54.71)と高い精度で一致した。**統計テストには `seed=1` を使うことを推奨する**(RTP=0.93079で目標93%に最も近く、帯の中心から余裕がある)。

**統計的な健全性の根拠**(RTP推定量の理論標準偏差): 分散 `Var(X)≈19.31`(1スピンあたりの倍率の分散)から、100万スピンの平均RTP推定量の標準誤差は約0.44パーセントポイント。したがって固定シードでの単発テストが偶然帯域外に出る確率は無視できないゼロではないが(3σで約±1.3pt)、上記5シードすべてが安全に収まっていることを実測で確認済みなので、`seed=1` を採用すれば実装時に再度シード探しをする必要はない。

### 4-B. 実装

```go
package casino

type SlotSymbol string

const (
	SymbolCherry  SlotSymbol = "🍒"
	SymbolLemon   SlotSymbol = "🍋"
	SymbolGrape   SlotSymbol = "🍇"
	SymbolBell    SlotSymbol = "🔔"
	SymbolDiamond SlotSymbol = "💎"
	SymbolSeven   SlotSymbol = "7️⃣"
)

// reelWeights is the fixed draw order for the cumulative-distribution
// lookup in drawSymbol. MUST be a slice, not a map iterated directly — Go
// randomizes map iteration order, which would make Spin's output
// non-deterministic for a given rng sequence even with a seeded source.
// Sum of Weight == 100 (verified: 28+22+21+18+7+4=100).
var reelWeights = []struct {
	Symbol SlotSymbol
	Weight int
}{
	{SymbolCherry, 28}, {SymbolLemon, 22}, {SymbolGrape, 21},
	{SymbolBell, 18}, {SymbolDiamond, 7}, {SymbolSeven, 4},
}

var triplePayout = map[SlotSymbol]int64{
	SymbolCherry: 7, SymbolLemon: 16, SymbolGrape: 22,
	SymbolBell: 32, SymbolDiamond: 98, SymbolSeven: 196,
}

const cherryPairPayout int64 = 1 // "🍒2個でベット額返還"

// SpinResult is Spin's outcome — exported because internal/commands/slot.go
// (a different package) renders it into the Discord response.
type SpinResult struct {
	Reels     [3]SlotSymbol
	Bet       int64
	Payout    int64 // total chips credited back; 0 on a loss
	IsJackpot bool  // true iff Reels is 💎💎💎 or 7️⃣7️⃣7️⃣ — triggers the public celebration message
}

func drawSymbol(rng randSource) SlotSymbol {
	n := rng.Intn(100)
	acc := 0
	for _, e := range reelWeights {
		acc += e.Weight
		if n < acc {
			return e.Symbol
		}
	}
	return reelWeights[len(reelWeights)-1].Symbol // unreachable: weights sum to 100
}

// spin draws 3 independent reels and resolves the payout. Pure — bet is
// NOT deducted or credited here (that's Store.Spin's job, inside a locked
// Update transaction — see Task 5).
func spin(bet int64, rng randSource) SpinResult {
	reels := [3]SlotSymbol{drawSymbol(rng), drawSymbol(rng), drawSymbol(rng)}
	return SpinResult{
		Reels:     reels,
		Bet:       bet,
		Payout:    payoutFor(reels, bet),
		IsJackpot: reels == [3]SlotSymbol{SymbolDiamond, SymbolDiamond, SymbolDiamond} || reels == [3]SlotSymbol{SymbolSeven, SymbolSeven, SymbolSeven},
	}
}

func payoutFor(reels [3]SlotSymbol, bet int64) int64 {
	if reels[0] == reels[1] && reels[1] == reels[2] {
		return bet * triplePayout[reels[0]]
	}
	cherryCount := 0
	for _, r := range reels {
		if r == SymbolCherry {
			cherryCount++
		}
	}
	if cherryCount == 2 {
		return bet * cherryPairPayout
	}
	return 0
}
```

**テスト(`slot_test.go`)**:
1. `TestPayoutFor_TripleSeven` / `_TripleDiamond` / `_TripleCherry` / `_CherryPair` / `_NoMatch` — 決定的な `[3]SlotSymbol` を直接渡した表駆動テスト(rng不要)。
2. `TestSpin_UsesInjectedRNG_Deterministic` — 固定値を返すスタブ `randSource` で `spin` の出力が完全予測可能であることを確認(dice_test.goの `randFunc` 注入パターンを踏襲)。
3. `TestSpin_RTP_WithinTargetBand_OneMillionSpins` — `rand.New(rand.NewSource(1))` で100万回 `spin(100, rng)` を回し、`totalPayout/(bet*trials)` が `[0.92, 0.94]` に収まることを確認。**理由をコメントで明記**(4-A節の解析値を参照する形で)。
4. `TestSpin_NearMissFrequency_WithinTensOfSpinsBand` — 同じ100万回試行で「💎が2個一致(3個目は別)または7️⃣が2個一致(3個目は別)」の頻度を数え、`[1/90, 1/15]` の帯(「数十スピンに1回程度」の許容範囲)に収まることを確認。
5. `TestReelWeights_SumTo100` — `reelWeights` の `Weight` 合計が100であることを回帰テスト。
6. **`TestPayoutTable_ExactLiterals`(SF-4 — 配当表の退行を機械的に防ぐ本命)** — 上の統計テスト
   (3・4)は許容帯が広いので、**重みや倍率を入れ替えても通ってしまう**。そこで次を
   **literal で直接検査する**(キックオフの「配当表を一字一句そのまま転記する」要求の機械的な受け皿):
   - `reelWeights` の**順序と全6重み**が `{🍒,28},{🍋,22},{🍇,21},{🔔,18},{💎,7},{7️⃣,4}` と完全一致すること
     (順序も検査する — `drawSymbol` の累積分布は順序に依存する)
   - `triplePayout` の**全6倍率**が `🍒:7, 🍋:16, 🍇:22, 🔔:32, 💎:98, 7️⃣:196` と完全一致すること、
     およびマップの要素数がちょうど6であること
   - `cherryPairPayout == 1`
   - **216通り全列挙による厳密値**: `reelWeights` と `payoutFor` から
     `Σ w_i·w_j·w_k · payoutFor(...)/bet` を整数で積み上げ、
     **RTP分子 == 929900**、**3個揃い寄与分子 == 760556**、**🍒2個寄与分子 == 169344**、
     **ニアミス分子 == 18279**(いずれも分母 1,000,000)であることを検査する。
     この積み上げは `int64` で行い浮動小数点を使わない(丸め誤差ゼロ)。
     ループ回数は 216 なので実行時間はマイクロ秒オーダー。
     この1本があれば、統計テストが偶然通っても配当表の改変は必ず検出される。

**完了条件**: `go test ./internal/casino/... -run 'TestPayoutFor|TestPayoutTable|TestSpin|TestReelWeights'` すべて `ok`(100万回テストなので数百ms〜数秒かかる見込み — タイムアウトしないことを確認)。`go vet` 警告0。

**コミット境界**: **Commit 4**(Task 4 + Task 5 の `Spin` + Task 7)。

---

## Task 5 — `internal/casino/store.go`: 経済オーケストレーションメソッド群

**目的**: Task 2(永続化の箱・型・エラー)+ Task 3(純粋計算)+ Task 4(スロット純粋計算)を、`Update` トランザクション内で組み合わせる、外部(`internal/commands`)から呼ばれる唯一のAPI surface を作る。

**変更ファイル**: `internal/casino/store.go`(追記), `internal/casino/store_test.go`(追記)

**⚠️ このタスクはコミットを2つにまたがる**(各コミット時点でゲートを緑に保つため):
`Spin`(§5-F)だけが Task 4 の `spin` に依存するので **Commit 4** に載せ、
それ以外(§5-B〜§5-E と §5-F の `Spin` 以外)は Task 3 の `nextRate`/`dailyBonusAmount`/`exchange*` に
依存するので **Commit 3** に載せる。§コミット境界 の対応表を参照。

### 5-A. エラー方針(§3.4 の実装)

センチネル/カスタムエラー型と経済上限は Task 2 の `errors.go`/`types.go` で定義済み(§3.4 / §3.5)。
いずれも日本語文言を持たない。
**本タスクの全 credit 経路は `creditChipsLocked`/`creditCoinsLocked` を通す**
— `account.Chips += x` / `account.Coins += x` を直書きしてはならない(§3.5)。

### 5-B. JST日付ヘルパーとguild/account自動生成ヘルパー(パッケージ非公開 — Task 2 で定義済み)

```go
var jst = time.FixedZone("JST", 9*3600) // 夏時間なし・tzdataファイル不要(設計書、§3.7-4)

func jstDate(t time.Time) string { return t.In(jst).Format("2006-01-02") }

// jstYesterday returns the JST calendar date one day before t's JST date.
// It converts to JST FIRST and only then subtracts a day: doing
// jstDate(t.AddDate(0,0,-1)) instead would subtract one calendar day in t's
// OWN location, which is 23h or 25h long on a DST transition day in
// America/*, Europe/* etc. On a Linux host with such a TZ that silently
// yields 一昨日 or 今日 in JST terms, breaking /daily streaks (§3.7-3).
func jstYesterday(t time.Time) string { return t.In(jst).AddDate(0, 0, -1).Format("2006-01-02") }

// ensureGuildLocked returns d's entry for guildID, creating an empty one if
// absent. Also repairs a nil Users map and a nil *GuildEconomy value (which
// a hand-edited or partially-written file can contain as `{"g": null}`).
// Caller must already hold the Store's lock (call only from inside an
// Update closure). EVERY read of (*d)[guildID] must go through this — see
// the nil-map hazard in §3.3.
func ensureGuildLocked(d *Data, guildID string) *GuildEconomy {
	if (*d)[guildID] == nil {
		(*d)[guildID] = &GuildEconomy{Users: map[string]*UserAccount{}}
	}
	if (*d)[guildID].Users == nil {
		(*d)[guildID].Users = map[string]*UserAccount{}
	}
	return (*d)[guildID]
}

const welcomeBonusChips = 1000 // 設計書: ウェルカムボーナス

// ensureAccountLocked returns economy's account for userID, auto-creating
// it with the 1,000-chip welcome bonus if this is userID's first casino
// interaction in this guild. Caller must already hold the Store's lock.
func ensureAccountLocked(economy *GuildEconomy, userID string) *UserAccount {
	if economy.Users[userID] == nil {
		economy.Users[userID] = &UserAccount{Chips: welcomeBonusChips}
	}
	return economy.Users[userID]
}
```

**注**: `welcomeBonusChips`(1,000)は `MaxChips`(1e12)に対して自明に安全なので
`creditChipsLocked` を経由しない**唯一の**チップ増加である(新規口座の初期値であって加算ではない)。

### 5-C. ⚠️ 重要な正しさの注意点(実装者が絶対に踏んではいけない罠)

`Store.mu` は **再入不可**(Goの`sync.Mutex`の標準的な性質)。したがって「`Update` の中から `s.Update` を呼ぶ」ことは**デッドロックする**。しかし `ExchangeCoinToChip`/`ExchangeChipToCoin`/`TopAssets`/`ViewAccount`/`collectDailyAnnouncements`(Task 6)はいずれも「同じトランザクション内で"今日のレートを保証してから残高を触る"」ことが必須(そうしないと、レート確認とチップ操作の間に別goroutineが割り込んでレートを変えてしまい得る=経済不整合)。

このため **`EnsureTodayRate`(公開・独自にロックを取る)と、ロック取得済み文脈でだけ呼べる `ensureTodayRateLocked`(非公開・ロックを取らない)を分離する**:

```go
// EnsureTodayRate makes sure guildID has a DailyRate for today's JST
// calendar date, generating one via a random-walk step if missing. Public
// entry point — takes the lock itself. Do NOT call this from inside
// another Update closure (deadlock) — internal callers that need "today's
// rate" as part of their own transaction must call ensureTodayRateLocked
// directly instead (see ExchangeCoinToChip/ExchangeChipToCoin/TopAssets/
// ViewAccount below, and collectDailyAnnouncements in Task 6).
func (s *Store) EnsureTodayRate(guildID string, now time.Time) (DailyRate, error) {
	today := jstDate(now)
	var result DailyRate
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		result = ensureTodayRateLocked(economy, today, s.rng)
		return nil
	})
	return result, err
}

// ensureTodayRateLocked returns economy's DailyRate for `today`, appending
// a freshly-generated one (via nextRate, Task 3) if missing, and trimming
// history to the most recent 30 entries (設計書). Caller must already
// hold the Store's lock.
func ensureTodayRateLocked(economy *GuildEconomy, today string, rng randSource) DailyRate {
	if n := len(economy.Rates); n > 0 && economy.Rates[n-1].Date == today {
		return economy.Rates[n-1]
	}
	var prevRate int
	var prevTrend TrendState
	hasPrev := len(economy.Rates) > 0
	if hasPrev {
		last := economy.Rates[len(economy.Rates)-1]
		prevRate, prevTrend = last.Rate, last.Trend
	}
	rate, trend, event := nextRate(prevRate, prevTrend, hasPrev, rng)
	result := DailyRate{Date: today, Rate: rate, Trend: trend, Event: event}
	economy.Rates = append(economy.Rates, result)
	if len(economy.Rates) > 30 {
		economy.Rates = economy.Rates[len(economy.Rates)-30:]
	}
	return result
}
```

### 5-C-2. 全コマンド共通の入口 `EnsureCasinoAccess`(§3.8 の実装)

```go
// EnsureCasinoAccess commits, as its OWN transaction, the two invariants
// every casino command depends on: today's DailyRate exists for guildID,
// and guildID/userID has an account (welcome bonus granted on first ever
// access). Callers run this BEFORE their own Update so a failing main
// operation cannot roll the new account and its welcome bonus back (§3.8).
// Idempotent. All seven commands call it, /rate and /rank included.
func (s *Store) EnsureCasinoAccess(guildID, userID string, now time.Time) error {
	today := jstDate(now)
	return s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		ensureTodayRateLocked(economy, today, s.rng)
		ensureAccountLocked(economy, userID)
		return nil
	})
}
```

同じ理由で `TopAssets` の並べ替えロジックも `topAssetsLocked`(非公開・ロック不要)として切り出し、`TopAssets`(公開)と Task 6 の `collectDailyAnnouncements` の両方から呼べるようにする:

```go
type RankEntry struct {
	UserID      string
	TotalAssets int64 // Chips + Coins*rate
}

// topAssetsLocked ranks economy's users by total assets at `rate`, ties
// broken by ascending UserID for deterministic output. Caller must already
// hold the Store's lock.
//
// Chips + Coins*rate cannot overflow int64: the store invariants are
// Chips <= MaxChips (1e12), Coins <= MaxCoins (1e12) and rate <= maxRate
// (140, the clamp ceiling), so the sum is bounded by
// 1e12 + 1e12*140 = 1.41e14, about 65,413x below math.MaxInt64
// (9.223e18). Every credit path enforces those caps — see §3.5.
func topAssetsLocked(economy *GuildEconomy, rate int, limit int) []RankEntry {
	entries := make([]RankEntry, 0, len(economy.Users))
	for userID, account := range economy.Users {
		entries = append(entries, RankEntry{UserID: userID, TotalAssets: account.Chips + account.Coins*int64(rate)})
	}
	sort.Slice(entries, func(i, j int) bool {
		if entries[i].TotalAssets != entries[j].TotalAssets {
			return entries[i].TotalAssets > entries[j].TotalAssets
		}
		return entries[i].UserID < entries[j].UserID
	})
	if len(entries) > limit {
		entries = entries[:limit]
	}
	return entries
}

// TopAssets returns guildID's top `limit` accounts by total assets, using
// today's rate (generated first if missing, same transaction).
func (s *Store) TopAssets(guildID string, now time.Time, limit int) ([]RankEntry, error) {
	today := jstDate(now)
	var result []RankEntry
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		rate := ensureTodayRateLocked(economy, today, s.rng)
		result = topAssetsLocked(economy, rate.Rate, limit)
		return nil
	})
	return result, err
}
```

### 5-D. デイリーボーナス

```go
type DailyClaimResult struct {
	Amount       int64 // credited chips
	NewStreak    int
	JackpotBonus bool // true iff the +500 "大入り袋" applied (NewStreak%7==0)
}

// ClaimDaily pays out today's daily bonus to guildID/userID (JST calendar
// date), auto-creating the account (welcome bonus) on first interaction.
// Returns ErrAlreadyClaimedToday (no mutation) if already claimed today,
// or ErrChipCapExceeded (also no mutation) if the payout would break
// MaxChips.
func (s *Store) ClaimDaily(guildID, userID string, now time.Time) (DailyClaimResult, error) {
	today := jstDate(now)
	yesterday := jstYesterday(now) // NOT jstDate(now.AddDate(0,0,-1)) — see jstYesterday's doc (§3.7-3)
	var result DailyClaimResult
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		account := ensureAccountLocked(economy, userID)
		if account.LastDailyDate == today {
			return ErrAlreadyClaimedToday
		}
		streak := 1
		if account.LastDailyDate == yesterday {
			streak = account.StreakDays + 1
		}
		amount := dailyBonusAmount(streak) // Task 3
		if err := creditChipsLocked(account, amount); err != nil {
			return err // aborts the transaction: streak/date/balance all stay untouched
		}
		account.StreakDays = streak
		account.LastDailyDate = today
		result = DailyClaimResult{Amount: amount, NewStreak: streak, JackpotBonus: streak%7 == 0}
		return nil
	})
	if err != nil {
		return DailyClaimResult{}, err
	}
	return result, nil
}
```

### 5-E. 両替

```go
type ExchangeResult struct {
	Spent    int64 // debited from source currency
	Received int64 // credited to destination currency
	RateUsed int
}

// ExchangeCoinToChip converts coins to chips for guildID/userID at today's
// rate (ensured in the same transaction). Minimum: coins >= 1.
func (s *Store) ExchangeCoinToChip(guildID, userID string, coins int64, now time.Time) (ExchangeResult, error) {
	if coins < 1 {
		return ExchangeResult{}, ErrInvalidAmount
	}
	today := jstDate(now)
	var result ExchangeResult
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		rate := ensureTodayRateLocked(economy, today, s.rng)
		account := ensureAccountLocked(economy, userID)
		if account.Coins < coins {
			return &ErrInsufficientCoins{Balance: account.Coins}
		}
		chips, err := exchangeCoinToChip(coins, rate.Rate) // Task 3
		if err != nil {
			return err
		}
		if err := creditChipsLocked(account, chips); err != nil {
			return err // ErrChipCapExceeded — aborts before the debit is persisted
		}
		account.Coins -= coins
		result = ExchangeResult{Spent: coins, Received: chips, RateUsed: rate.Rate}
		return nil
	})
	return result, err
}

// ExchangeChipToCoin converts chips to coins for guildID/userID at today's
// rate. Minimum is on the RESULT (設計書: "結果が1コイン以上になる量"),
// checked after computing the exchange.
func (s *Store) ExchangeChipToCoin(guildID, userID string, chips int64, now time.Time) (ExchangeResult, error) {
	if chips < 1 {
		return ExchangeResult{}, ErrInvalidAmount
	}
	today := jstDate(now)
	var result ExchangeResult
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		rate := ensureTodayRateLocked(economy, today, s.rng)
		account := ensureAccountLocked(economy, userID)
		if account.Chips < chips {
			return &ErrInsufficientChips{Balance: account.Chips}
		}
		coins, err := exchangeChipToCoin(chips, rate.Rate) // Task 3
		if err != nil {
			return err
		}
		if coins < 1 {
			return ErrBelowMinimumExchange
		}
		if err := creditCoinsLocked(account, coins); err != nil {
			return err // ErrCoinCapExceeded — aborts before the debit is persisted
		}
		account.Chips -= chips
		result = ExchangeResult{Spent: chips, Received: coins, RateUsed: rate.Rate}
		return nil
	})
	return result, err
}
```

### 5-F. スロット・口座照会・ミント・チャンネル設定

**`Spin` は Commit 4**(Task 4 の `spin` に依存するため)。それ以外は Commit 3。
`Mint`/`SetAnnounceChannel` は当日レートを必要としないので **Task 2(Commit 2)で実装済み**
— ここでは参照のために再掲する。

```go
// Spin atomically deducts bet chips, resolves the spin (via Task 4's pure
// `spin`, using s.rng), credits any payout, and persists — all in one
// Update ("控除→抽選→払い戻しの永続化", 設計書). Does NOT perform the
// reveal animation (internal/commands/slot.go's job — see Task 13). Returns
// ErrBetOutOfRange (defense in depth), *ErrInsufficientChips, or
// ErrChipCapExceeded — in every error case nothing at all is persisted, so
// the bet is never silently eaten.
func (s *Store) Spin(guildID, userID string, bet int64) (SpinResult, error) {
	if bet < 10 || bet > 1000 {
		return SpinResult{}, ErrBetOutOfRange
	}
	var result SpinResult
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		account := ensureAccountLocked(economy, userID)
		if account.Chips < bet {
			return &ErrInsufficientChips{Balance: account.Chips}
		}
		account.Chips -= bet
		result = spin(bet, s.rng) // Task 4
		if err := creditChipsLocked(account, result.Payout); err != nil {
			return err // aborts: the deduction above is discarded with the transaction
		}
		return nil
	})
	if err != nil {
		return SpinResult{}, err
	}
	return result, nil
}

type AccountView struct {
	Account     UserAccount
	RateUsed    int
	TotalAssets int64
}

// ViewAccount ensures guildID/userID's account exists (welcome bonus on
// first use) and today's rate exists, returning both plus derived total
// assets — used by /balance.
func (s *Store) ViewAccount(guildID, userID string, now time.Time) (AccountView, error) {
	today := jstDate(now)
	var result AccountView
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		rate := ensureTodayRateLocked(economy, today, s.rng)
		account := *ensureAccountLocked(economy, userID)
		result = AccountView{Account: account, RateUsed: rate.Rate, TotalAssets: account.Chips + account.Coins*int64(rate.Rate)}
		return nil
	})
	return result, err
}

// Mint credits amount coins to guildID/userID (auto-creating the account
// if needed). Caller (internal/commands/casino_admin.go) owns the
// permission check — Mint has no notion of Discord permissions. Refuses
// amounts outside [1, MaxCoins] and any credit that would push the balance
// past MaxCoins (§3.5) — Discord's MaxValue on the option is a UX nicety,
// not a guarantee: discordgo's IntValue() is int64(o.Value.(float64)), so a
// direct API call can send ~2^53.
// (Defined in Task 2 — repeated here for context.)
func (s *Store) Mint(guildID, userID string, amount int64) error {
	if amount < 1 || amount > MaxCoins {
		return ErrInvalidAmount
	}
	return s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		account := ensureAccountLocked(economy, userID)
		return creditCoinsLocked(account, amount)
	})
}

// SetAnnounceChannel sets guildID's 9am announcement channel.
// (Defined in Task 2 — repeated here for context.)
func (s *Store) SetAnnounceChannel(guildID, channelID string) error {
	return s.Update(func(d *Data) error {
		ensureGuildLocked(d, guildID).AnnounceChannelID = channelID
		return nil
	})
}

// RecentRates ensures today's rate exists (same transaction) then returns
// up to the most recent `limit` DailyRate entries (oldest..newest, today
// included as the last element) for guildID. Used by /rate and by
// internal/commands' exchange result commentary.
func (s *Store) RecentRates(guildID string, now time.Time, limit int) ([]DailyRate, error) {
	today := jstDate(now)
	var result []DailyRate
	err := s.Update(func(d *Data) error {
		economy := ensureGuildLocked(d, guildID)
		ensureTodayRateLocked(economy, today, s.rng)
		recent := economy.Rates
		if len(recent) > limit {
			recent = recent[len(recent)-limit:]
		}
		result = recent
		return nil
	})
	return result, err
}
```

**テスト(`store_test.go` に追記、§3.6 の stub 手順で1メソッドずつ Red→Green)**: 各メソッドにつき「初回成功」「エラー分岐(残高不足/既受取/下限未満/上限超過)でエラー時に**一切変更が永続化されないこと**」「口座自動生成+ウェルカムボーナス」を最低限カバーする。具体的に:

- `TestStore_EnsureTodayRate_{FirstDay,Idempotent,NextDay,TrimsHistoryTo30}`
- `TestStore_EnsureTodayRate_PersistsEventKind` — `st.rng` に暴騰分岐を強制するスタブを代入し、保存された `DailyRate.Event == EventSurge` になること。
- **`TestStore_EnsureCasinoAccess_{FirstAccessGrantsWelcomeBonus,Idempotent_NoDoubleBonus,GeneratesTodayRate}`**
- **`TestStore_EnsureCasinoAccess_ThenFailingOperation_KeepsAccountAndBonus`** — `EnsureCasinoAccess` の直後に必ず失敗する操作(残高不足の `ExchangeCoinToChip`)を実行し、**口座とウェルカムボーナス1,000チップが残っていること**を検証する(BL-4 の本丸)。
- `TestStore_ClaimDaily_{FirstClaim,SameDayTwice,ConsecutiveDay,GapResetsStreak,SeventhDayJackpot}`
- **`TestStore_ClaimDaily_NonJSTLocation_StreakFollowsJSTCalendar`** — `now` を `time.FixedZone("EST", -5*3600)` と **DST を持つ location**(`time.LoadLocation("America/New_York")`。取得に失敗した環境では `t.Skip` ではなく `time.FixedZone` の固定オフセット2本で代替し、その旨をコメントに書く)で与え、ストリークの継続/リセットが**JST暦日基準**どおりになることを検証する。現行のテスト名は UTC/JST しか踏まないため**失敗し得ないテスト**だった(§3.7-3)。
- `TestStore_ExchangeCoinToChip_{Success,InsufficientCoins_NoMutation,BelowMinimumInput}`
- `TestStore_ExchangeChipToCoin_{Success,InsufficientChips_NoMutation,ResultBelowMinimum_NoMutation}`
- **`TestStore_ExchangeCoinToChip_ChipCapExceeded_NoMutation`** — `Chips` を `MaxChips` 直下に、`Coins` を十分に置いて両替させ、`ErrChipCapExceeded` が返り**`Coins` も `Chips` も1枚も動いていない**ことを検証する。
- **`TestStore_ExchangeChipToCoin_CoinCapExceeded_NoMutation`** — 同様に `ErrCoinCapExceeded`。
- `TestStore_Spin_{InsufficientChips_NoDeduction,DeductsThenCreditsPayout,BetOutOfRange}`(`InsufficientChips`/`BetOutOfRange`は`s.rng`を問わない。`DeductsThenCreditsPayout`は`st.rng`に固定値スタブを代入して既知の結果を強制する)【Commit 4】
- **`TestStore_Spin_ChipCapExceeded_NoMutation`** — `Chips` を `MaxChips` 直下にして当たりを強制し、`ErrChipCapExceeded` が返って**ベット額も控除されていない**ことを検証する。【Commit 4】
- `TestStore_Mint_{CreditsCoinsAutoCreatesAccount,BelowMinimum}` および **`_ExactlyAtMaxCoins_Succeeds` / `_OneOverMaxCoins_NoMutation` / `_AmountAboveMaxCoins_ReturnsInvalidAmount`**(上限ちょうど/上限+1 の境界)【Task 2 で実装済み・ここで再確認】
- `TestStore_SetAnnounceChannel_Persists`【Task 2 で実装済み】
- `TestStore_TopAssets_{SortsDescendingTieBrokenByUserID,LimitsToRequestedCount}`
- **`TestStore_TopAssets_MaxBalances_DoesNotOverflow`** — 2ユーザーに `Chips = MaxChips` / `Coins = MaxCoins` を直接 `Update` で仕込み、`rate = 140`(クランプ上限)で `TopAssets` を呼んで、`TotalAssets` が **正の値 141,000,000,000,000 になる**(負値へラップしない)ことを検証する。
- `TestStore_ViewAccount_FirstAccess_GrantsWelcomeBonus`
- `TestStore_RecentRates_{GeneratesTodayIfMissing,LimitsToRequestedCount,ReturnsOldestToNewestOrder}`

**完了条件**: `go test ./internal/casino/...` すべて `ok`。`go vet ./internal/casino/...` 警告0。

**コミット境界**: **Commit 3**(`Spin` と `Spin` のテストのみ **Commit 4**)。§コミット境界 の対応表を参照。

**残課題(→ code-gardening)**: 表示専用の `TopAssets`/`ViewAccount`/`RecentRates` は当日レート保証のために
`Update`(= JSON 全書き戻し)を使っており、`/balance` `/rank` `/rate` のたびにファイル全体を書き換える。
`EnsureCasinoAccess` が先に当日レートを確定させる設計になったので、**これらを `Snapshot` ベースに
切り替えれば書き込みを省ける**が、現規模(1ギルド数十ユーザー)では実害がないため C-1 では変更しない。
→ §残課題 に記録。

---

## Task 6 — `internal/casino/announce.go`: 9時スケジューラ(discordgo非依存)

**目的**: 設計書 §スケジューラと時刻 を、discordgoに一切依存しない形で実装する(§3.1 の決定)。「何を送るか」を決めるロジックのみをここに置き、「実際にDiscordへ送る」処理は Task 16 の `internal/commands/casino_announce.go` に委譲する。

**変更ファイル**: `internal/casino/announce.go`(新規), `internal/casino/announce_test.go`(新規)

```go
package casino

import (
	"context"
	"log/slog"
	"time"
)

// AnnouncementJob is a neutral (discordgo-free) description of one guild's
// pending 9am rate announcement — internal/commands/casino_announce.go
// turns this into an actual discordgo.MessageEmbed and posts it.
type AnnouncementJob struct {
	GuildID     string
	ChannelID   string
	Today       DailyRate
	RecentRates []DailyRate // oldest..newest, up to 7 entries (includes Today as the last element)
	TopAssets   []RankEntry // top 3 (設計書)
}

// collectDailyAnnouncements ensures every known guild's rate exists for
// now's JST calendar date, then returns an AnnouncementJob for every guild
// with an AnnounceChannelID configured that has not yet been announced
// today (LastAnnounced != today; 設計書). Does NOT mark LastAnnounced (only
// MarkAnnounced does, after a confirmed send) and does NOT send anything.
func (s *Store) collectDailyAnnouncements(now time.Time) ([]AnnouncementJob, error) {
	today := jstDate(now)
	var jobs []AnnouncementJob
	err := s.Update(func(d *Data) error {
		for guildID := range *d {
			// Deliberately re-fetch via ensureGuildLocked rather than using
			// the value from `range *d`: a hand-edited or partially-written
			// file can hold `{"guild1": null}`, and a nil *GuildEconomy
			// would panic on the first field access — which, in a discordgo
			// handler goroutine, kills the whole process (§3.3).
			economy := ensureGuildLocked(d, guildID)
			rate := ensureTodayRateLocked(economy, today, s.rng)
			if economy.AnnounceChannelID == "" || economy.LastAnnounced == today {
				continue
			}
			recent := economy.Rates
			if len(recent) > 7 {
				recent = recent[len(recent)-7:]
			}
			jobs = append(jobs, AnnouncementJob{
				GuildID: guildID, ChannelID: economy.AnnounceChannelID,
				Today: rate, RecentRates: recent,
				TopAssets: topAssetsLocked(economy, rate.Rate, 3),
			})
		}
		return nil
	})
	return jobs, err
}

// MarkAnnounced records guildID as announced for `date`. Call ONLY after a
// confirmed successful send — leave LastAnnounced untouched on failure so
// tomorrow's 9am wake naturally retries (設計書: 送信失敗はログして継続).
func (s *Store) MarkAnnounced(guildID, date string) error {
	return s.Update(func(d *Data) error {
		ensureGuildLocked(d, guildID).LastAnnounced = date
		return nil
	})
}

// nextRunAt returns the next 9:00 JST instant strictly after now (an input
// exactly at 9:00:00.000 JST rolls to the FOLLOWING day — a deliberate
// simplification avoiding a double-fire race right at the boundary).
func nextRunAt(now time.Time) time.Time {
	j := now.In(jst)
	next := time.Date(j.Year(), j.Month(), j.Day(), 9, 0, 0, 0, jst)
	if !next.After(j) {
		next = next.AddDate(0, 0, 1)
	}
	return next
}

// AnnounceCallback is invoked once per pending job by RunAnnounceScheduler.
// Returning nil means "sent successfully" (triggers MarkAnnounced); a
// non-nil error is logged here and left for tomorrow's retry.
type AnnounceCallback func(job AnnouncementJob) error

// SleepFunc blocks until `until` or ctx cancellation, returning false if
// ctx was cancelled (caller should stop) or true if it woke at `until`.
// Exported because RunAnnounceScheduler (exported) takes one as a
// parameter — an exported function must not have an unexported type in its
// signature, or callers outside this package cannot supply their own.
type SleepFunc func(ctx context.Context, until time.Time) bool

// RealSleepUntil is the production SleepFunc — a real, cancellable timer
// wait. Exported so internal/commands/casino_announce.go can pass it.
func RealSleepUntil(ctx context.Context, until time.Time) bool {
	timer := time.NewTimer(time.Until(until))
	defer timer.Stop()
	select {
	case <-ctx.Done():
		return false
	case <-timer.C:
		return true
	}
}

// announceWindowOpen reports whether now is at or after 09:00 JST on now's
// OWN JST calendar date — i.e. whether today's scheduled announcement time
// has already arrived.
//
// This is the LOWER BOUND of the catch-up rule, and it is not optional: it
// is what "毎朝9時(JST)に掲示する" means. There is deliberately NO upper
// bound (see runAnnouncePass).
func announceWindowOpen(now time.Time) bool {
	j := now.In(jst)
	return !j.Before(time.Date(j.Year(), j.Month(), j.Day(), 9, 0, 0, 0, jst))
}

// runAnnouncePass performs exactly one sweep:
//
//  1. ALWAYS ensure every known guild has today's rate — regardless of the
//     time of day. This is 設計書 L170-174「レートは絶対に欠損しない」.
//  2. ONLY IF announceWindowOpen(now): post + mark every guild that is
//     configured for announcements and has not been announced today.
//
// Errors are logged and never abort the sweep (設計書: 送信失敗はログして
// 継続し、レート生成には影響させない).
//
// WHY THE 09:00 LOWER BOUND (ユーザー決定の確定解釈): a 03:00 restart must
// NOT post. That day's 09:00 has not happened yet, so posting now would set
// LastAnnounced, the 09:00 wake six hours later would find nothing pending,
// and the day's announcement would have been permanently relocated to the
// middle of the night — directly contradicting 設計書「毎朝9時(JST)に
// 掲示する」.
//
// WHY NO UPPER BOUND: a 23:00 restart DOES post, late. That day's 09:00 has
// already passed and was missed, so a late post is better than losing the
// day entirely. The user chose this behaviour knowing that a late-night
// restart produces a late-night announcement. "時間窓を切らない" means "no
// upper bound", not "no lower bound".
func runAnnouncePass(ctx context.Context, st *Store, callback AnnounceCallback, now time.Time) {
	jobs, err := st.collectDailyAnnouncements(now) // step 1: rates, unconditionally
	if err != nil {
		slog.Error("casino: collectDailyAnnouncements failed", "error", err)
		return
	}
	if !announceWindowOpen(now) {
		// Before 09:00 JST: today's rate is now guaranteed to exist, but the
		// scheduled posting time has not arrived. The loop below sleeps to
		// this very same 09:00 and posts then.
		return
	}
	for _, job := range jobs { // step 2: post, no upper bound
		if ctx.Err() != nil {
			return // shutting down: stop starting new sends
		}
		if err := callback(job); err != nil {
			slog.Error("casino: announcement callback failed", "guild_id", job.GuildID, "error", err)
			continue // leave LastAnnounced untouched so a later pass retries
		}
		if err := st.MarkAnnounced(job.GuildID, job.Today.Date); err != nil {
			slog.Error("casino: MarkAnnounced failed", "guild_id", job.GuildID, "error", err)
		}
	}
}

// RunAnnounceScheduler blocks until ctx is cancelled. It runs ONE pass
// immediately at startup (the catch-up pass), then sleeps to the next
// 09:00 JST and runs another after every wake. now/sleep are injected so
// tests never block on a real clock.
//
// STARTUP CATCH-UP (ユーザー決定): without this first pass, LastAnnounced
// could never become "today" through any code path — the persisted field
// the design doc mandates would be dead code — and a bot that was down at
// 09:00 would skip that day's announcement forever. The pass always
// backfills today's rate; whether it also POSTS is decided by
// runAnnouncePass's 09:00 lower bound (no upper bound).
//
// NOTE the single `t := now()` per iteration: the pass and the sleep target
// are computed from the SAME instant on purpose. Reading the clock twice
// would open a narrow hole — booting at 08:59:59.9, skipping the post
// (window not open), then computing nextRunAt from a clock that has since
// ticked past 09:00 and sleeping until TOMORROW, silently losing today's
// announcement. With one reading, nextRunAt(08:59:59.9) is today's 09:00,
// so the very next wake posts.
//
// NO DOUBLE FIRE, three independent reasons:
//  1. collectDailyAnnouncements skips any guild with LastAnnounced == today,
//     and MarkAnnounced sets exactly that after every successful send.
//  2. The 09:00 lower bound means a pre-09:00 startup cannot consume the
//     day's slot early.
//  3. nextRunAt rolls an input of exactly 09:00:00.000 to the FOLLOWING
//     day, so a boot at precisely 09:00 posts once and then sleeps a full
//     day instead of re-firing immediately.
//
// (If a pass ever ran so long that nextRunAt(t) is already in the past, the
// timer fires at once and the extra pass is a harmless no-op: every guild
// is already marked for today.)
func RunAnnounceScheduler(ctx context.Context, st *Store, callback AnnounceCallback, now func() time.Time, sleep SleepFunc) {
	for {
		if ctx.Err() != nil {
			return
		}
		t := now()
		runAnnouncePass(ctx, st, callback, t)
		if !sleep(ctx, nextRunAt(t)) {
			return
		}
	}
}
```

**起動時パスの条件(確定)**:

| 条件 | レート生成 | 掲示 |
|---|---|---|
| 時刻に関わらず | **常に行う** | — |
| `now`(JST)が当日 09:00 **以降** かつ `LastAnnounced != 今日` かつ掲示チャンネル設定済み | — | **行う** |
| `now`(JST)が当日 09:00 **より前** | 行う | **行わない**(6時間後などの定時パスが担当) |
| 上限 | — | **設けない**(23時の復帰でも掲示する) |

**テスト(`announce_test.go`)**:
- `TestNextRunAt_BeforeNineAM_SameDayNineAM` / `_AfterNineAM_NextDayNineAM` / `_ExactlyNineAM_NextDayNineAM`(境界)
- `TestNextRunAt_UTCInput_ConvertsToJSTCorrectly`(例: `2026-07-10T00:30:00Z` = JST 09:30 → 次は翌日09:00 JST)
- `TestCollectDailyAnnouncements_{GeneratesRateIfMissing,SkipsNoChannelConfigured,SkipsAlreadyAnnouncedToday,IncludesUpTo7RatesAndTop3}`
- **`TestCollectDailyAnnouncements_NilGuildValue_DoesNotPanic`** — `{"guild1": null}` を含む JSON を置いてから呼び、panic せずに処理されること(§3.3 の nil ポインタ経路)。
- `TestMarkAnnounced_PersistsLastAnnouncedDate`
- **`TestAnnounceWindowOpen_{BeforeNineAM_False,ExactlyNineAM_True,AfterNineAM_True,LateNight_True,NonJSTInput_ConvertedFirst}`** —
  下限判定の単体テスト。`NonJSTInput` は `2026-07-10T00:30:00Z`(= JST 09:30)で `true`、
  `2026-07-09T23:30:00Z`(= JST 08:30)で `false` になること(§3.7-3 と同じ「先に JST へ変換する」規律)。
- `TestRunAnnounceScheduler_StopsWhenContextCancelled`(**`now` を当日 10:00 JST に固定**し、**先に startup pass が1回走る**ことを前提に、`sleep`スタブが即`false`を返す → 関数がすぐ戻ることを確認。実時間待機は一切発生しない)
- `TestRunAnnounceScheduler_MarksOnlySuccessfullyCallbackedGuilds`(`now` を当日 10:00 JST に固定し、`sleep`スタブが即`false`を返すようにして startup pass だけを走らせ、2ギルド分のjob中1つのcallbackがエラーを返すケースで、成功した方だけ`LastAnnounced`が更新されることを確認)
- **起動時キャッチアップ(D1、必須)**:
  - `TestRunAnnounceScheduler_StartupCatchUp_AfterNineAM_Announces` — `now` を当日 10:00 JST に固定し、
    掲示チャンネル設定済み・`LastAnnounced` が前日のギルドに対して **callback が1回呼ばれ、
    `LastAnnounced` が今日になる**こと。
  - **`TestRunAnnounceScheduler_StartupCatchUp_BeforeNineAM_GeneratesRateOnly`** — `now` を当日 03:00 JST に
    固定した場合、**当日レートは生成されるが callback は一度も呼ばれず、`LastAnnounced` も更新されない**こと。
    **これは掲示の下限が 09:00 であることの回帰テストであり、省略不可**。
    もしここで掲示してしまうと `LastAnnounced` が立ち、6時間後の定時パスがスキップされて、
    **その日の掲示が恒久的に深夜へ移動する**(設計書「毎朝9時(JST)に掲示する」への正面からの違反)。
  - **`TestRunAnnounceScheduler_StartupCatchUp_LateNight_StillAnnounces`** — `now` を当日 23:30 JST に
    固定した場合、**掲示される**こと。**上限を設けない**というユーザー決定の回帰テスト
    (その日の 09:00 は既に過ぎて取り逃しているので、遅れてでも掲示するほうが良い、という選択。
    深夜に掲示が飛ぶ副作用はユーザーが承知のうえで選んだもの)。
  - `TestRunAnnounceScheduler_StartupCatchUp_ExactlyNineAM_Announces` — 境界。`now` をちょうど
    09:00:00.000 JST に固定して掲示されること(下限は「以降」であって「超過」ではない)。
  - `TestRunAnnounceScheduler_StartupCatchUp_AlreadyAnnouncedToday_Skips` — `now` を当日 10:00 JST、
    `LastAnnounced == today` のギルドでは callback が**一度も呼ばれない**こと(定時パスとの二重発火防止の根拠1)。
  - `TestRunAnnounceScheduler_StartupCatchUp_NoAnnounceChannel_GeneratesRateWithoutAnnouncing` —
    **`now` を当日 10:00 JST に固定**し、`AnnounceChannelID == ""` のギルドでは callback は呼ばれないが、
    **当日レートは生成されている**こと。
    **`now` の固定は必須**: 実クロックのままだと 00:00〜08:59 JST に走らせたとき
    「窓が閉じているから callback が呼ばれない」だけで通ってしまい、
    **検証したい「掲示チャンネル未設定だから呼ばれない」を確かめたことにならない**(空振りテスト)。
  - **`TestRunAnnounceScheduler_BeforeNineAM_SleepsUntilSameDayNineAM`** — `now` が当日 03:00 JST のとき、
    `sleep` スタブに渡された `until` が**当日の 09:00 JST**(翌日ではない)であることを検証する。
    起動時に掲示を見送った日が、同じ日のうちに必ず拾われることの保証
    (`RunAnnounceScheduler` が1周あたり `now()` を**1回だけ**読む理由の回帰テストでもある)。
- **`TestRunAnnounceScheduler_WakesAtNineAM_AnnouncesOnSecondPass`(必須 — 定時掲示の生存確認)** —
  上の各テストはいずれも `sleep` スタブが即 `false` を返す=**起動時パス1周だけ**を見ており、
  「**起床後の2周目で掲示する**」という定時掲示の本体経路を**誰も検証していない**。
  実装者が起動時パスの後に誤って `return` しても他のテストは全部通るため、
  **毎朝の掲示が全滅しても検出されない**。これを塞ぐ通しテストを必須とする:
  - `now` スタブ: **`[08:59:59.9 JST, 09:00:00.0 JST]` の順**に返す(呼ばれるたびに次の値へ進む)
  - `sleep` スタブ: 戻り値を **`[true, false]` の順**に返す(1回目は起床、2回目でループ終了)。
    渡された `until` を記録する
  - 掲示チャンネル設定済み・`LastAnnounced` が前日のギルドを1つ仕込む
  - **検証4点**: ① 1周目(08:59:59.9)では callback が **0回** /
    ② `sleep` に渡された1回目の `until` が **当日 09:00 JST** /
    ③ 2周目(09:00:00.0)で callback が **ちょうど1回** /
    ④ `LastAnnounced` が今日になる
  - 実クロック待機は発生しない(`sleep` は完全にスタブ)
- **`TestRunAnnounceScheduler_CancelDuringCallback_ReturnsAfterCallbackCompletes`(SF-3)** —
  **`now` を当日 10:00 JST に固定し**(窓が開いていないと callback がそもそも呼ばれず、
  キャンセル検証が成立しない — 実クロックのままだと 00:00〜08:59 JST で不成立になる)、
  callback をチャネルでブロックさせた状態で ctx を cancel し、callback を解放するまで
  `RunAnnounceScheduler` が戻らない(=呼び出し側が join できる)ことを検証する。

**完了条件**: `go test ./internal/casino/... -run 'TestNextRunAt|TestAnnounceWindowOpen|TestCollectDailyAnnouncements|TestMarkAnnounced|TestRunAnnounceScheduler'` すべて `ok`。実行時間はすべてミリ秒オーダー(実クロック待機なし)であることを確認(数秒以上かかっていたら`sleep`注入が機能していない兆候)。

**コミット境界**: **Commit 5**(Task 6 のみ)。

---

## Task 7 — `internal/casino`: 経済系の並行性回帰テスト

**目的**: 設計書「同時ベットによる残高競合を構造的に防ぐ」の**明示的な検証**。Task 2の汎用「ファイルが壊れない」テストとは別に、**残高の保存則(lost updateが起きない)**を数値で検証する。

**変更ファイル**: `internal/casino/store_test.go`(追記、または可読性のため `internal/casino/concurrency_test.go` に分けてもよい — どちらでもGoの挙動に影響しないため実装者の裁量)

```go
func TestStore_Spin_ConcurrentSpinsBySameUser_ConservesExactBalance(t *testing.T) {
	dir := t.TempDir()
	st := New(filepath.Join(dir, "casino.json"))
	const guildID, userID = "guild1", "user1"
	const startingChips = int64(100000)
	const bet = int64(100)
	const n = 50

	if err := st.Update(func(d *Data) error {
		ensureAccountLocked(ensureGuildLocked(d, guildID), userID).Chips = startingChips
		return nil
	}); err != nil {
		t.Fatalf("seeding balance: %v", err)
	}

	var wg sync.WaitGroup
	var mu sync.Mutex
	var totalPayout int64
	for i := 0; i < n; i++ {
		wg.Add(1)
		go func() {
			defer wg.Done()
			result, err := st.Spin(guildID, userID, bet)
			if err != nil {
				t.Errorf("Spin: %v", err)
				return
			}
			mu.Lock()
			totalPayout += result.Payout
			mu.Unlock()
		}()
	}
	wg.Wait()

	snapshot, err := st.Snapshot()
	if err != nil {
		t.Fatalf("Snapshot: %v", err)
	}
	finalChips := snapshot[guildID].Users[userID].Chips

	want := startingChips - n*bet + totalPayout
	if finalChips != want {
		t.Fatalf("lost update detected: got Chips=%d, want %d (starting=%d - n*bet=%d + totalPayout=%d) — Store.Update's mutex is not serializing concurrent Spin calls correctly",
			finalChips, want, startingChips, n*bet, totalPayout)
	}
}

// TestStore_ConcurrentMixedOperations_AcrossUsers_NoCorruption mirrors
// internal/store/events_test.go's concurrent-Save regression test (proven
// pattern in this codebase), but exercises the higher-level casino API
// surface (ClaimDaily/Spin/ExchangeCoinToChip mixed across N different
// users in the same guild) instead of a single raw write method.
func TestStore_ConcurrentMixedOperations_AcrossUsers_NoCorruption(t *testing.T) {
	dir := t.TempDir()
	st := New(filepath.Join(dir, "casino.json"))
	const guildID = "guild1"
	const n = 30
	now := time.Date(2026, 7, 10, 10, 0, 0, 0, time.UTC)

	var wg sync.WaitGroup
	for i := 0; i < n; i++ {
		wg.Add(1)
		go func(i int) {
			defer wg.Done()
			userID := fmt.Sprintf("user-%d", i)
			if _, err := st.ClaimDaily(guildID, userID, now); err != nil {
				t.Errorf("ClaimDaily(%s): %v", userID, err)
			}
			if _, err := st.Spin(guildID, userID, 50); err != nil {
				t.Errorf("Spin(%s): %v", userID, err)
			}
		}(i)
	}
	wg.Wait()

	snapshot, err := st.Snapshot()
	if err != nil {
		t.Fatalf("Snapshot: %v", err)
	}
	userCount := len(snapshot[guildID].Users)
	if userCount != n {
		t.Fatalf("expected %d distinct user accounts after %d concurrent goroutines, got %d — a lost update or file corruption dropped an account",
			n, n, userCount)
	}
}
```

**完了条件**: `go test ./internal/casino/... -run 'TestStore_.*Concurrent' -race` が `ok`。**`-race` フラグを明示的に使うこと** — `build-and-verify.md` の現行ゲートは `-race` を必須としていないが、このタスクはミューテックス規律そのものを検証する回帰テストなので、この特定のテスト実行に限り `-race` を追加で要求する(既存ゲートに矛盾しない追加)。
**`-race` は Linux/CI で実行する**(§3.7-6)。開発機(Windows)は C コンパイラが無く `-race` が cgo 不足で動かないことが既知(フェーズA/Bで確認済み)なので、その場合は**非raceでのテスト成功を代替証拠とし、`-race` を省略した旨を必ず報告する**(黙って省略しない)。コードは Linux/CI で `-race` が通る前提でレース安全に書くこと。

**コミット境界**: **Commit 4**(Task 4 + Task 5 の `Spin` + Task 7)。この時点で `go build ./... && go vet ./... && go test ./...`(まだ `internal/commands` からは一切参照されていないので既存挙動に影響なし)が通ることを確認してからコミットする。

---

## Task 8 — `internal/commands/casino_shared.go`: 7コマンド共通ヘルパー

**目的**: 設計書の二重ガード(ギルド専用・管理者権限)と、`/rate`/`/exchange`/掲示で共有する表示ヘルパーを1箇所にまとめる。`schedule.go` の `floatPtr`/`resolveUserID`/`resolveDisplayName` と同じ「複数コマンドファイルが参照する共通ヘルパーは既存ファイルまたは専用共有ファイルに置く」慣習に従う。

**変更ファイル**: `internal/commands/casino_shared.go`(新規), `internal/commands/casino_shared_test.go`(新規)

**再利用(新規定義しない)**: `resolveUserID(i *discordgo.InteractionCreate) string` と `resolveDisplayName(i *discordgo.InteractionCreate) string` は `schedule.go` に既に存在する。7コマンドはすべてこれをそのまま使う — 再定義すると同一パッケージ内でシンボル重複コンパイルエラーになるので注意。

```go
package commands

import (
	"fmt"
	"strings"

	"github.com/SioKo-Shox3/todayistodayBot/internal/casino" // required by rateRankCommentary/marketCommentary below
	"github.com/bwmarrin/discordgo"
)

// guildOnlyContexts restricts a slash command to guild contexts (hidden in
// DMs client-side). Shared Definition() field for all 7 casino commands.
var guildOnlyContexts = []discordgo.InteractionContextType{discordgo.InteractionContextGuild}

// permPtr returns a pointer to p — ApplicationCommand.DefaultMemberPermissions is *int64.
func permPtr(p int64) *int64 { return &p }

// requireGuildContext returns a non-empty ❌ Japanese message if i was
// invoked outside a guild, or "" if the check passes. All 7 casino
// commands call this first, IN ADDITION to Contexts: &guildOnlyContexts in
// Definition() — Discord's client-side Contexts enforcement is not a
// substitute for this server-side check (設計書の二重ガード方針).
func requireGuildContext(i *discordgo.InteractionCreate) string {
	if i.GuildID == "" {
		return "❌ このコマンドはサーバー内で使用してください。"
	}
	return ""
}

// requireAdministrator returns a non-empty ❌ Japanese message (設計書の
// 文言と完全一致させる) if the invoking member lacks Administrator,
// or "" if the check passes. /casino-admin only, in addition to
// DefaultMemberPermissions in Definition().
func requireAdministrator(i *discordgo.InteractionCreate) string {
	if i.Member == nil || i.Member.Permissions&discordgo.PermissionAdministrator == 0 {
		return "❌ このコマンドはサーバー管理者のみ使用できます"
	}
	return ""
}

var sparkChars = []rune{'▁', '▂', '▃', '▄', '▅', '▆', '▇', '█'}

// sparkline renders values (oldest..newest rate integers) as a block-chart
// string (設計書). Each value is bucketed into 8 levels by its
// position within [min(values), max(values)]; a flat series renders the
// lowest bar for every point.
func sparkline(values []int) string {
	if len(values) == 0 {
		return ""
	}
	lo, hi := values[0], values[0]
	for _, v := range values {
		if v < lo {
			lo = v
		}
		if v > hi {
			hi = v
		}
	}
	span := hi - lo
	var b strings.Builder
	for _, v := range values {
		idx := 0
		if span > 0 {
			idx = (v - lo) * (len(sparkChars) - 1) / span
		}
		b.WriteRune(sparkChars[idx])
	}
	return b.String()
}

// rateRankCommentary returns a one-line "参謀コメント" (設計書の例:
// 「今日のレートは直近7日で2番目の高値です」) ranking today's rate (the last
// entry of history) among up to the last 7 entries. history must be
// oldest..newest. Disclosed interpretation (設計書 gives an example
// sentence, not an exact algorithm): rank = 1-indexed position when the
// window is sorted descending by Rate.
//
// The empty guard is not decorative: every caller runs inside a discordgo
// handler goroutine, which discordgo starts WITHOUT recover
// (discordgo@v0.29.0/event.go:171), so an index-out-of-range here would
// kill the whole bot process. A doc-comment contract is not a guard.
func rateRankCommentary(history []casino.DailyRate) string {
	if len(history) == 0 {
		return ""
	}
	window := history
	if len(window) > 7 {
		window = window[len(window)-7:]
	}
	today := window[len(window)-1].Rate
	rank := 1
	for _, r := range window {
		if r.Rate > today {
			rank++
		}
	}
	return fmt.Sprintf("今日のレートは直近%d日で%d番目の高値です", len(window), rank)
}

// trendStreak counts how many consecutive trailing entries of history share
// the newest entry's TrendState (>=1 for non-empty history, 0 for empty).
func trendStreak(history []casino.DailyRate) int {
	if len(history) == 0 {
		return 0
	}
	last := history[len(history)-1].Trend
	streak := 0
	for i := len(history) - 1; i >= 0; i-- {
		if history[i].Trend != last {
			break
		}
		streak++
	}
	return streak
}

// marketCommentary returns the 相場コメント line required by 設計書 L66
// (掲示embedの3要素目; example: 「コイン強気3日目。天井はどこだ?」).
// history must be oldest..newest; the last entry is today.
//
// It reads the PERSISTED casino.EventKind rather than guessing a surge or
// crash from the day-over-day percentage: the 70/140 clamp can hide a real
// event behind a small visible move (§3.2). An event outranks the trend
// because a surge/crash is the headline of that day. Both "" and
// casino.EventNone mean "no event" ("" can only come from a hand-edited or
// pre-C-1 record).
func marketCommentary(history []casino.DailyRate) string {
	if len(history) == 0 {
		return ""
	}
	today := history[len(history)-1]
	switch today.Event {
	case casino.EventSurge:
		return "🚀 暴騰デー! コインが跳ねた。売り抜けるなら今日だ。"
	case casino.EventCrash:
		return "💥 暴落デー! コインが崩れた。拾いに行くか、様子を見るか。"
	}
	streak := trendStreak(history)
	switch today.Trend {
	case casino.TrendBull:
		return fmt.Sprintf("コイン強気%d日目。天井はどこだ?", streak)
	case casino.TrendBear:
		return fmt.Sprintf("コイン弱気%d日目。底値を狙うなら今か。", streak)
	default:
		return fmt.Sprintf("凪%d日目。動かない相場も相場だ。", streak)
	}
}
```

**なぜ `marketCommentary` は `internal/commands` にあるのか**: §3.4 の規約
「日本語ユーザー向け文言は `internal/commands` 層だけが持つ」に従うため(既存の `rateRankCommentary`
と同じ理由)。`internal/casino` は `EventKind`/`TrendState` という**構造化された事実**だけを提供する。

**注**: `casino.DailyRate`/`casino.SpinResult`/`casino.RankEntry`/`casino.AccountView`/`casino.ExchangeResult`/`casino.DailyClaimResult`/`casino.AnnouncementJob` は `internal/casino` パッケージからの型なので、参照時は必ず `casino.` パッケージ修飾を付けること(この計画書では簡潔さのため一部箇所で略記しているが、実装時はフルパスで書く)。

**テスト(`casino_shared_test.go`)**:
- `TestRequireGuildContext_{EmptyGuildID,NonEmptyGuildID}`
- `TestRequireAdministrator_{NilMember,MissingPermission,HasPermission}`
- `TestSparkline_{Empty,FlatSeries,IncreasingSeries}`
- `TestRateRankCommentary_{TodayIsHighest_Rank1,TodayIsSecondHighest_Rank2,WindowCapsAt7Entries}`
- **`TestRateRankCommentary_Empty`(SF-9)** — 空スライスで panic せず `""` を返すこと。
- **`TestTrendStreak_{Empty,SingleEntry,ThreeConsecutive,BrokenByOtherTrend}`**
- **`TestMarketCommentary_{Empty,Bull3Days,Bear1Day,Flat,Surge,Crash,LegacyEmptyEventTreatedAsNone}`** —
  trend × EventKind の各分岐(BL-5)。`Surge`/`Crash` は trend に関わらずイベント文言が優先されること。

**完了条件**: `go test ./internal/commands/... -run 'TestRequireGuildContext|TestRequireAdministrator|TestSparkline|TestRateRankCommentary|TestTrendStreak|TestMarketCommentary'` すべて `ok`。

**コミット境界**: **Commit 6**(Task 8〜16 をまとめて1コミット)。単独ではコミットしない — 7コマンドが揃って初めて「動く機能」として意味を持つため。

---

以降 Task 9〜16 はすべて `internal/commands` パッケージ。各コマンドは共通で:
- `store *casino.Store` フィールドを持ち、`init()` で `Register(&XCommand{store: casino.Default()})`。
- `Definition()` に `Contexts: &guildOnlyContexts`(`/casino-admin` はさらに `DefaultMemberPermissions`)。
- `Handle()` の冒頭で `requireGuildContext(i)`(`/casino-admin` はさらに `requireAdministrator(i)`)。
- **ガードを通過したら、本処理より前に必ず `c.store.EnsureCasinoAccess(i.GuildID, resolveUserID(i), now)`
  を呼ぶ(7コマンド全部。`/rate` `/rank` も含む)**。これが「初回アクセスで口座自動開設+
  当日レート保証」を全経路で成立させる唯一の入口(§3.8)。失敗したら本処理へ進まず
  「❌ カジノの初期化に失敗しました。」を返し、詳細は `slog.Error` に落とす。
  `EnsureCasinoAccess` が当日レートを確定させるので、以降の処理はレート欠損を心配しなくてよい。
- Discordのエラー変換は各コマンドファイル内に非公開関数として置く(`schedule_result.go`/`reminder.go` の慣習どおり、パッケージ横断の共有翻訳器は作らない)。
- テストは実 `*discordgo.Session` を使わず、`casino.New(filepath.Join(t.TempDir(), "casino.json"))` で構築したテスト専用 `*casino.Store` を注入する(**`casino.Default()` を直接使ってテストしない** — `store.New`/`Default` の使い分け規約と同一。`Default()` は `data/casino.json` を指すので、テストが禁止パスへ書き込んでしまう)。
- **各コマンドの `_test.go` に「初回アクセスで口座が作られ 1,000 チップが入っていること」のテストを
  1本ずつ置く**(BL-4: どのコマンドを最初に叩いても挙動が同じであることの回帰テスト)。

## Task 9 — `internal/commands/balance.go`: `/balance`

**目的/変更ファイル/テスト**: 自分の残高を本人にのみ表示(ephemeral)。`balance.go` + `balance_test.go`。

```go
func init() { Register(&BalanceCommand{store: casino.Default()}) }

type BalanceCommand struct{ store *casino.Store }

func (c *BalanceCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:        "balance",
		Description: "自分のコイン・チップ・ストリーク・総資産を表示します（本人のみに表示）",
		Contexts:    &guildOnlyContexts,
	}
}

// formatBalanceMessage renders /balance's response. Wording is this plan's
// own judgment (設計書 doesn't pin exact copy for /balance) — consistent
// with the rest of the bot's tone.
func formatBalanceMessage(view casino.AccountView) string {
	return fmt.Sprintf("💰 **残高**\nチップ: %d枚\nコイン: %d枚\nストリーク: %d日\n総資産: %d相当(本日レート %d)",
		view.Account.Chips, view.Account.Coins, view.Account.StreakDays, view.TotalAssets, view.RateUsed)
}

func (c *BalanceCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	content := ""
	if msg := requireGuildContext(i); msg != "" {
		content = msg
	} else {
		now, userID := time.Now(), resolveUserID(i)
		// §3.8: open the account (+ welcome bonus) and today's rate as their
		// OWN committed transaction, before anything that can fail.
		if err := c.store.EnsureCasinoAccess(i.GuildID, userID, now); err != nil {
			slog.Error("casino: EnsureCasinoAccess failed", "command", "balance", "error", err)
			content = "❌ カジノの初期化に失敗しました。"
		} else if view, err := c.store.ViewAccount(i.GuildID, userID, now); err != nil {
			slog.Error("casino: ViewAccount failed", "error", err)
			content = "❌ 残高の取得に失敗しました。"
		} else {
			content = formatBalanceMessage(view)
		}
	}
	return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{
			Content: content,
			Flags:   discordgo.MessageFlagsEphemeral, // 本人のみ表示(設計書) — ガード失敗時の❌もこの方針を踏襲し全応答をephemeralに統一する(判断の開示)
		},
	})
}
```

**テスト**: `TestBalanceCommand_Definition_GuildOnly`、`TestFormatBalanceMessage_RendersAllFields`(固定 `casino.AccountView` を渡して文字列を厳密比較)、`TestBalanceCommand_FirstAccess_OpensAccountWithWelcomeBonus`(テスト専用ストアを注入し、`Handle` の内部処理と同じ順序で `EnsureCasinoAccess` → `ViewAccount` を呼んで 1,000 チップが入っていることを確認)。

**完了条件**: `go test ./internal/commands/... -run 'TestBalanceCommand|TestFormatBalanceMessage'` すべて `ok`。
(**`-run A -run B` と書かない** — Go の flag は後勝ちなので最後の `-run` だけが有効になり、
前のパターンのテストは**実行されないまま「ok」と表示される**。必ず `-run 'A|B'` の単一正規表現にする。)

---

## Task 10 — `internal/commands/daily.go`: `/daily`

```go
func init() { Register(&DailyCommand{store: casino.Default()}) }

type DailyCommand struct{ store *casino.Store }

func (c *DailyCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:        "daily",
		Description: "デイリーボーナスを受け取ります（1日1回、連続受け取りでボーナス増加）",
		Contexts:    &guildOnlyContexts,
	}
}

// formatDailyMessage renders /daily's success response with a 🔥×streak
// flourish (設計書). Wording is this plan's own judgment.
func formatDailyMessage(displayName string, result casino.DailyClaimResult) string {
	jackpot := ""
	if result.JackpotBonus {
		jackpot = "\n🎉 大入り袋(+500)発生!"
	}
	return fmt.Sprintf("🔥×%d %sさんがデイリーボーナス**+%dチップ**を受け取りました!%s", result.NewStreak, displayName, result.Amount, jackpot)
}

func (c *DailyCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	content := ""
	if msg := requireGuildContext(i); msg != "" {
		content = msg
	} else {
		now, userID := time.Now(), resolveUserID(i)
		if err := c.store.EnsureCasinoAccess(i.GuildID, userID, now); err != nil { // §3.8
			slog.Error("casino: EnsureCasinoAccess failed", "command", "daily", "error", err)
			content = "❌ カジノの初期化に失敗しました。"
		} else {
			result, err := c.store.ClaimDaily(i.GuildID, userID, now)
			switch {
			case errors.Is(err, casino.ErrAlreadyClaimedToday):
				content = "❌ 本日は既に受け取り済みです。また明日お越しください。"
			case errors.Is(err, casino.ErrChipCapExceeded):
				content = "❌ チップ残高が上限に達しているため受け取れません。"
			case err != nil:
				slog.Error("casino: ClaimDaily failed", "error", err)
				content = "❌ デイリーボーナスの受け取りに失敗しました。"
			default:
				content = formatDailyMessage(resolveDisplayName(i), result)
			}
		}
	}
	return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{Content: content}, // 公開(設計書)
	})
}
```

**テスト**: `TestDailyCommand_Definition`、`TestFormatDailyMessage_{NoJackpot,WithJackpotBonus}`、`TestDailyCommand_FirstAccess_OpensAccountWithWelcomeBonus`。`errors`/`log/slog`/`time`/`fmt`/`github.com/SioKo-Shox3/todayistodayBot/internal/casino`/`github.com/bwmarrin/discordgo` をimport。

**完了条件**: `go test ./internal/commands/... -run 'TestDailyCommand|TestFormatDailyMessage'` すべて `ok`。

---

## Task 11 — `internal/commands/rate.go`: `/rate`

```go
func init() { Register(&RateCommand{store: casino.Default()}) }

type RateCommand struct{ store *casino.Store }

func (c *RateCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:        "rate",
		Description: "今日のコイン⇔チップ両替レートと直近7日間のスパークラインを表示します",
		Contexts:    &guildOnlyContexts,
	}
}

// rateChangeLine renders the day-over-day line shared by /rate and the 9am
// announcement embed, so the two can never drift apart (they used to
// disagree on the diff == 0 case). event comes from the PERSISTED
// casino.EventKind of today's DailyRate, never from a percentage threshold
// — the 70/140 clamp can hide a real surge/crash (§3.2). history must be
// oldest..newest.
func rateChangeLine(history []casino.DailyRate) string {
	if len(history) == 0 {
		return ""
	}
	today := history[len(history)-1]
	if len(history) < 2 {
		return "本日が最初の記録です。"
	}
	prev := history[len(history)-2].Rate
	diff := today.Rate - prev
	pct := float64(diff) / float64(prev) * 100
	arrow := "📈▲"
	switch {
	case diff < 0:
		arrow = "📉▼"
	case diff == 0:
		arrow = "➖"
	}
	eventTag := ""
	switch today.Event {
	case casino.EventSurge:
		eventTag = "🚀"
	case casino.EventCrash:
		eventTag = "💥"
	}
	return fmt.Sprintf("%s%s%.1f%%(前日比 %+d)", eventTag, arrow, pct, diff)
}

// buildRateMessage renders /rate's response: today's rate, day-over-day
// change%, a 7-day sparkline (casino_shared.go's sparkline), the 相場コメント
// (marketCommentary) and the "参謀コメント" (rateRankCommentary). history
// must be oldest..newest; an empty history is guarded rather than trusted,
// because a panic here runs in a discordgo handler goroutine and kills the
// whole process (§3.3 / SF-9).
func buildRateMessage(history []casino.DailyRate) string {
	if len(history) == 0 {
		return "❌ レート情報がまだありません。"
	}
	today := history[len(history)-1]
	values := make([]int, len(history))
	for i, r := range history {
		values[i] = r.Rate
	}
	return fmt.Sprintf("💱 **本日のレート**: 1コイン = %dチップ\n%s\n直近%d日: %s\n%s\n%s",
		today.Rate, rateChangeLine(history), len(history), sparkline(values),
		marketCommentary(history), rateRankCommentary(history))
}

func (c *RateCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	content := ""
	if msg := requireGuildContext(i); msg != "" {
		content = msg
	} else {
		now, userID := time.Now(), resolveUserID(i)
		// §3.8: /rate も共通入口を通る。これがないと「/rate を最初に叩いた
		// ユーザーだけ口座が作られない」という経路依存が残る。
		if err := c.store.EnsureCasinoAccess(i.GuildID, userID, now); err != nil {
			slog.Error("casino: EnsureCasinoAccess failed", "command", "rate", "error", err)
			content = "❌ カジノの初期化に失敗しました。"
		} else if history, err := c.store.RecentRates(i.GuildID, now, 7); err != nil {
			slog.Error("casino: RecentRates failed", "error", err)
			content = "❌ レート情報の取得に失敗しました。"
		} else {
			content = buildRateMessage(history)
		}
	}
	return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{Content: content},
	})
}
```

**テスト**: `TestRateCommand_Definition`、`TestBuildRateMessage_{FirstDayNoChangeLine,RisingRateShowsUpArrow,FallingRateShowsDownArrow,FlatRateShowsDash}`、
**`TestBuildRateMessage_EmptyHistory`(SF-9 — panic せず ❌ 文言を返すこと)**、
**`TestRateChangeLine_{Empty,SingleEntry,Rising,Falling,Flat,SurgeShowsRocket,CrashShowsExplosion,SurgeClampedSmallDiff_StillShowsRocket}`**
(最後の1本が BL-5 の本丸: レート130→140 で前日比 7.7% でも、`Event == EventSurge` なら 🚀 が出ること)。

**完了条件**: `go test ./internal/commands/... -run 'TestRateCommand|TestBuildRateMessage|TestRateChangeLine'` すべて `ok`。

---

## Task 12 — `internal/commands/exchange.go`: `/exchange coin-to-chip` / `/exchange chip-to-coin`

```go
func init() { Register(&ExchangeCommand{store: casino.Default()}) }

type ExchangeCommand struct{ store *casino.Store }

func (c *ExchangeCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:        "exchange",
		Description: "コインとチップを両替します（手数料3%）",
		Contexts:    &guildOnlyContexts,
		Options: []*discordgo.ApplicationCommandOption{
			{
				Type: discordgo.ApplicationCommandOptionSubCommand, Name: "coin-to-chip", Description: "コインをチップに両替します",
				Options: []*discordgo.ApplicationCommandOption{
					{Type: discordgo.ApplicationCommandOptionInteger, Name: "amount", Description: "両替するコインの枚数（1以上）", Required: true, MinValue: floatPtr(1)},
				},
			},
			{
				Type: discordgo.ApplicationCommandOptionSubCommand, Name: "chip-to-coin", Description: "チップをコインに両替します",
				Options: []*discordgo.ApplicationCommandOption{
					{Type: discordgo.ApplicationCommandOptionInteger, Name: "amount", Description: "両替するチップの枚数（1以上）", Required: true, MinValue: floatPtr(1)},
				},
			},
		},
	}
}

// translateExchangeError maps internal/casino's sentinel/typed errors to
// the exact ❌ Japanese text (chip-insufficient wording matches 設計書
// verbatim; coin-insufficient is this plan's symmetric extension —
// 設計書 only gives the chip case explicitly).
func translateExchangeError(err error) string {
	var insufficientCoins *casino.ErrInsufficientCoins
	var insufficientChips *casino.ErrInsufficientChips
	switch {
	case errors.As(err, &insufficientCoins):
		return fmt.Sprintf("❌ コインが足りません(現在: %d枚)", insufficientCoins.Balance)
	case errors.As(err, &insufficientChips):
		return fmt.Sprintf("❌ チップが足りません(現在: %d枚)", insufficientChips.Balance)
	case errors.Is(err, casino.ErrInvalidAmount):
		return "❌ 両替量は1以上を指定してください。"
	case errors.Is(err, casino.ErrBelowMinimumExchange):
		return "❌ その枚数では両替後の受取量が1コイン未満になります。もう少し多い枚数を指定してください。"
	case errors.Is(err, casino.ErrAmountOverflow):
		return "❌ 指定された枚数が大きすぎます。"
	case errors.Is(err, casino.ErrChipCapExceeded):
		return "❌ 両替するとチップ残高が上限を超えます。もっと少ない枚数を指定してください。"
	case errors.Is(err, casino.ErrCoinCapExceeded):
		return "❌ 両替するとコイン残高が上限を超えます。もっと少ない枚数を指定してください。"
	default:
		slog.Error("casino: exchange failed", "error", err)
		return "❌ 両替に失敗しました。"
	}
}

func (c *ExchangeCommand) handleCoinToChip(guildID, userID string, amount int64, now time.Time) string {
	result, err := c.store.ExchangeCoinToChip(guildID, userID, amount, now)
	if err != nil {
		return translateExchangeError(err)
	}
	commentary := ""
	if history, err := c.store.RecentRates(guildID, now, 7); err == nil {
		commentary = "\n" + rateRankCommentary(history)
	}
	return fmt.Sprintf("✅ %dコインを%dチップに両替しました(レート%d)%s", result.Spent, result.Received, result.RateUsed, commentary)
}

func (c *ExchangeCommand) handleChipToCoin(guildID, userID string, amount int64, now time.Time) string {
	result, err := c.store.ExchangeChipToCoin(guildID, userID, amount, now)
	if err != nil {
		return translateExchangeError(err)
	}
	commentary := ""
	if history, err := c.store.RecentRates(guildID, now, 7); err == nil {
		commentary = "\n" + rateRankCommentary(history)
	}
	return fmt.Sprintf("✅ %dチップを%dコインに両替しました(レート%d)%s", result.Spent, result.Received, result.RateUsed, commentary)
}

func (c *ExchangeCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	content := ""
	if msg := requireGuildContext(i); msg != "" {
		content = msg
	} else {
		now, userID := time.Now(), resolveUserID(i)
		data := i.ApplicationCommandData()
		if err := c.store.EnsureCasinoAccess(i.GuildID, userID, now); err != nil { // §3.8
			slog.Error("casino: EnsureCasinoAccess failed", "command", "exchange", "error", err)
			content = "❌ カジノの初期化に失敗しました。"
		} else if len(data.Options) == 0 {
			content = "❌ サブコマンドを指定してください（coin-to-chip / chip-to-coin）。"
		} else {
			sub := data.Options[0]
			var amount int64
			for _, opt := range sub.Options {
				if opt.Name == "amount" {
					amount = opt.IntValue()
				}
			}
			switch sub.Name {
			case "coin-to-chip":
				content = c.handleCoinToChip(i.GuildID, userID, amount, now)
			case "chip-to-coin":
				content = c.handleChipToCoin(i.GuildID, userID, amount, now)
			default:
				content = "❌ 不明なサブコマンドです。"
			}
		}
	}
	return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{Content: content},
	})
}
```

**テスト**: `TestExchangeCommand_Definition_HasTwoSubcommands`、`TestTranslateExchangeError_{InsufficientCoins,InsufficientChips,InvalidAmount,BelowMinimum,Overflow,ChipCapExceeded,CoinCapExceeded}`、`TestHandleCoinToChip_{Success,InsufficientCoins}`(実 `casino.New(filepath.Join(t.TempDir(), "casino.json"))` を注入、事前に `store.Mint` でコインを与えてから実行)、`TestHandleChipToCoin_{Success,BelowMinimumResult}`、`TestExchangeCommand_FirstAccess_OpensAccountWithWelcomeBonus`(初回両替が残高不足で失敗しても、**口座とウェルカムボーナスが残る**ことを確認 — BL-4)。

**完了条件**: `go test ./internal/commands/... -run 'TestExchangeCommand|TestTranslateExchangeError|TestHandleCoinToChip|TestHandleChipToCoin'` すべて `ok`。

---

## Task 13 — `internal/commands/slot.go`: `/slot`(コマンド本体。`internal/casino/slot.go` とは別ファイル・別パッケージ)

**API確認済み**: `discordgo.WebhookEdit.Content` は `*string`、`Session.InteractionResponseEdit(interaction *Interaction, newresp *WebhookEdit, ...) (*Message, error)`、`Session.ChannelMessageSend(channelID, content string, ...) (*Message, error)` — いずれも直接ソースを読んで確認済み。

```go
func init() { Register(&SlotCommand{store: casino.Default()}) }

type SlotCommand struct{ store *casino.Store }

func (c *SlotCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:        "slot",
		Description: "スロットを回します（ベット: 10〜1,000チップ）",
		Contexts:    &guildOnlyContexts,
		Options: []*discordgo.ApplicationCommandOption{
			{Type: discordgo.ApplicationCommandOptionInteger, Name: "bet", Description: "ベット額（10〜1,000チップ）", Required: true, MinValue: floatPtr(10), MaxValue: 1000},
		},
	}
}

// slotResponder abstracts the *discordgo.Session methods the reveal
// animation needs, so runReveal can be unit tested without a real Discord
// connection. *discordgo.Session satisfies this structurally (same pattern
// as reminder.go's discordWebhookCreator).
type slotResponder interface {
	InteractionRespond(interaction *discordgo.Interaction, resp *discordgo.InteractionResponse, options ...discordgo.RequestOption) error
	InteractionResponseEdit(interaction *discordgo.Interaction, newresp *discordgo.WebhookEdit, options ...discordgo.RequestOption) (*discordgo.Message, error)
	ChannelMessageSend(channelID, content string, options ...discordgo.RequestOption) (*discordgo.Message, error)
}

const slotRevealPlaceholder = "❔"

// buildSlotRevealStages returns the 4 progressive message bodies for the
// "リールを1個ずつ止める" animation (設計書): stage 0 = all ❔, stages
// 1-3 reveal one more reel each; stage 3 also appends the payout line.
func buildSlotRevealStages(result casino.SpinResult) []string {
	reelText := func(revealed int) string {
		cells := make([]string, 3)
		for i := 0; i < 3; i++ {
			if i < revealed {
				cells[i] = string(result.Reels[i])
			} else {
				cells[i] = slotRevealPlaceholder
			}
		}
		return strings.Join(cells, " ")
	}
	stages := make([]string, 4)
	for revealed := 0; revealed <= 3; revealed++ {
		line := fmt.Sprintf("🎰 %s", reelText(revealed))
		if revealed == 3 {
			line += "\n" + slotResultLine(result)
		}
		stages[revealed] = line
	}
	return stages
}

func slotResultLine(result casino.SpinResult) string {
	if result.Payout > 0 {
		return fmt.Sprintf("🎉 %d枚 獲得!(ベット%d枚)", result.Payout, result.Bet)
	}
	return fmt.Sprintf("😢 ハズレ(ベット%d枚)", result.Bet)
}

func translateSlotError(err error) string {
	var insufficientChips *casino.ErrInsufficientChips
	switch {
	case errors.As(err, &insufficientChips):
		return fmt.Sprintf("❌ チップが足りません(現在: %d枚)", insufficientChips.Balance)
	case errors.Is(err, casino.ErrBetOutOfRange):
		return "❌ ベットは10〜1,000チップです" // 設計書と完全一致させる
	case errors.Is(err, casino.ErrChipCapExceeded):
		return "❌ チップ残高が上限に達しているため回せません。"
	default:
		slog.Error("casino: spin failed", "error", err)
		return "❌ スロットの実行に失敗しました。"
	}
}

// runReveal performs the reel-by-reel animation via InteractionRespond +
// InteractionResponseEdit at ~800ms intervals (設計書), and — if
// result.IsJackpot — posts a SEPARATE public celebration message
// @mentioning userID (設計書). result's balances are ALREADY
// persisted by the time this runs (c.store.Spin, called from Handle,
// already completed) — a Discord API failure or crash here cannot affect
// balances or duplicate/roll back the payout (設計書:
// 「演出中のクラッシュは払い戻しに一切影響しない」). sleep is injected
// (production: time.Sleep) so tests don't take ~2.4s of real wall-clock
// time. Blocking here for ~2.4s is safe: discordgo dispatches each
// InteractionCreate handler in its own goroutine, so this does not
// stall the gateway read loop or other concurrent interactions.
//
// runReveal ALWAYS returns nil (the error result is kept for signature
// stability). Every Discord failure here is logged and swallowed, initial
// InteractionRespond included. Propagating that first failure would be
// actively harmful: cmd/bot/main.go:53-61 responds to the SAME interaction
// with a second InteractionRespond whenever a handler returns non-nil,
// which Discord rejects as "already acknowledged" — so the user would get
// nothing either way, plus a misleading error in the log. The spin is
// already persisted before we get here, so "log and stop" is the correct
// terminal behavior. (Decision recorded rather than left asymmetric: the
// earlier draft returned the error for the first call and nil for the
// edits.)
func (c *SlotCommand) runReveal(responder slotResponder, interaction *discordgo.Interaction, channelID, userID string, result casino.SpinResult, sleep func(time.Duration)) error {
	stages := buildSlotRevealStages(result)

	if err := responder.InteractionRespond(interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{Content: stages[0]},
	}); err != nil {
		slog.Error("discord: InteractionRespond failed at slot reveal start", "error", err)
		return nil
	}

	for _, stage := range stages[1:] {
		sleep(800 * time.Millisecond)
		content := stage
		if _, err := responder.InteractionResponseEdit(interaction, &discordgo.WebhookEdit{Content: &content}); err != nil {
			slog.Error("discord: InteractionResponseEdit failed during slot reveal", "error", err)
			return nil // 演出の失敗は払い戻しに一切影響しない(設計書)。ログのみ、呼び出し元にはエラーを伝播しない。
		}
	}

	if result.IsJackpot {
		celebration := fmt.Sprintf("🎉🎉🎉 <@%s> が %s%s%s で大当たり!! 🎉🎉🎉", userID, result.Reels[0], result.Reels[1], result.Reels[2])
		if _, err := responder.ChannelMessageSend(channelID, celebration); err != nil {
			slog.Error("discord: ChannelMessageSend failed for jackpot celebration", "error", err)
		}
	}
	return nil
}

func (c *SlotCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	if msg := requireGuildContext(i); msg != "" {
		return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
			Type: discordgo.InteractionResponseChannelMessageWithSource,
			Data: &discordgo.InteractionResponseData{Content: msg},
		})
	}

	var bet int64
	for _, opt := range i.ApplicationCommandData().Options {
		if opt.Name == "bet" {
			bet = opt.IntValue()
		}
	}
	if bet < 10 || bet > 1000 { // defense in depth — Discord's MinValue/MaxValue already enforce this client-side
		return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
			Type: discordgo.InteractionResponseChannelMessageWithSource,
			Data: &discordgo.InteractionResponseData{Content: "❌ ベットは10〜1,000チップです"},
		})
	}

	userID := resolveUserID(i)
	if err := c.store.EnsureCasinoAccess(i.GuildID, userID, time.Now()); err != nil { // §3.8
		slog.Error("casino: EnsureCasinoAccess failed", "command", "slot", "error", err)
		return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
			Type: discordgo.InteractionResponseChannelMessageWithSource,
			Data: &discordgo.InteractionResponseData{Content: "❌ カジノの初期化に失敗しました。"},
		})
	}

	result, err := c.store.Spin(i.GuildID, userID, bet)
	if err != nil {
		return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
			Type: discordgo.InteractionResponseChannelMessageWithSource,
			Data: &discordgo.InteractionResponseData{Content: translateSlotError(err)},
		})
	}

	return c.runReveal(s, i.Interaction, i.ChannelID, userID, result, time.Sleep)
}
```

**テスト**: `TestSlotCommand_Definition_BetRange10To1000`、`TestBuildSlotRevealStages_{WinShowsPayoutLine,LossShowsHazureLine,PlaceholdersRevealProgressively}`、`TestTranslateSlotError_{InsufficientChips,BetOutOfRange}`、`TestRunReveal_SendsInitialResponseThenThreeEdits`(フェイクの `slotResponder` — 呼び出しを記録するstruct — と `sleep=func(time.Duration){}`(no-op)を注入し、`InteractionRespond`1回+`InteractionResponseEdit`3回が呼ばれることを確認。**実時間待機は一切発生しない**)、`TestRunReveal_Jackpot_SendsPublicCelebrationMention`、`TestRunReveal_NonJackpot_NoPublicMessage`、`TestRunReveal_EditFailure_DoesNotPropagateError`(フェイクが2回目の `InteractionResponseEdit` でエラーを返しても `runReveal` が`nil`を返すことを確認 — 演出失敗が呼び出し元に伝播しないことの回帰テスト)、
**`TestRunReveal_InitialRespondFailure_DoesNotPropagateError`(SF-8 — 初回 `InteractionRespond` が失敗しても `nil` を返し、main.go 側の二重応答を誘発しないことの回帰テスト)**。

**完了条件**: `go test ./internal/commands/... -run 'TestSlotCommand|TestBuildSlotRevealStages|TestTranslateSlotError|TestRunReveal'` すべて `ok`、かつ実行時間がミリ秒オーダー(`sleep`注入により実待機なし)であること。

---

## Task 14 — `internal/commands/rank.go`: `/rank`

```go
func init() { Register(&RankCommand{store: casino.Default()}) }

type RankCommand struct{ store *casino.Store }

func (c *RankCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:        "rank",
		Description: "総資産（チップ＋コイン×当日レート）ランキングTOP10を表示します",
		Contexts:    &guildOnlyContexts,
	}
}

var rankMedals = []string{"🥇", "🥈", "🥉"}

// formatRankMessage renders /rank's response using <@userID> mentions —
// Discord resolves these to display names client-side, so this needs no
// *discordgo.Session lookup (a deliberate simplicity choice, also used by
// the jackpot celebration in slot.go and the announce embed).
func formatRankMessage(entries []casino.RankEntry) string {
	if len(entries) == 0 {
		return "📊 まだ誰も口座を持っていません。"
	}
	var b strings.Builder
	b.WriteString("📊 **総資産ランキング TOP10**\n")
	for i, e := range entries {
		marker := fmt.Sprintf("%d.", i+1)
		if i < len(rankMedals) {
			marker = rankMedals[i]
		}
		fmt.Fprintf(&b, "%s <@%s> — %d\n", marker, e.UserID, e.TotalAssets)
	}
	return b.String()
}

func (c *RankCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	content := ""
	if msg := requireGuildContext(i); msg != "" {
		content = msg
	} else {
		now, userID := time.Now(), resolveUserID(i)
		// §3.8: /rank も共通入口を通す(実行者に口座が無いまま順位表だけ見える、
		// という経路依存をなくす)。
		if err := c.store.EnsureCasinoAccess(i.GuildID, userID, now); err != nil {
			slog.Error("casino: EnsureCasinoAccess failed", "command", "rank", "error", err)
			content = "❌ カジノの初期化に失敗しました。"
		} else if entries, err := c.store.TopAssets(i.GuildID, now, 10); err != nil {
			slog.Error("casino: TopAssets failed", "error", err)
			content = "❌ ランキングの取得に失敗しました。"
		} else {
			content = formatRankMessage(entries)
		}
	}
	return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{Content: content},
	})
}
```

**テスト**: `TestRankCommand_Definition`、`TestFormatRankMessage_{Empty,TopThreeGetMedals,FourthOnwardsGetsNumber}`、`TestRankCommand_FirstAccess_OpensAccountWithWelcomeBonus`。

**完了条件**: `go test ./internal/commands/... -run 'TestRankCommand|TestFormatRankMessage'` すべて `ok`。

---

## Task 15 — `internal/commands/casino_admin.go`: `/casino-admin mint` / `/casino-admin channel`

**import**: `errors` / `fmt` / `log/slog` / **`time`**(`buildResponse` が `now time.Time` を取るため)/
`github.com/SioKo-Shox3/todayistodayBot/internal/casino` / `github.com/bwmarrin/discordgo`。

```go
func init() { Register(&CasinoAdminCommand{store: casino.Default()}) }

type CasinoAdminCommand struct{ store *casino.Store }

func (c *CasinoAdminCommand) Definition() *discordgo.ApplicationCommand {
	return &discordgo.ApplicationCommand{
		Name:                     "casino-admin",
		Description:              "カジノ経済の管理コマンド（コイン発行・掲示チャンネル設定）",
		DefaultMemberPermissions: permPtr(discordgo.PermissionAdministrator), // Discord側で非管理者に非表示(設計書)
		Contexts:                 &guildOnlyContexts,
		Options: []*discordgo.ApplicationCommandOption{
			{
				Type: discordgo.ApplicationCommandOptionSubCommand, Name: "mint", Description: "指定ユーザーにコインを発行します",
				Options: []*discordgo.ApplicationCommandOption{
					{Type: discordgo.ApplicationCommandOptionUser, Name: "user", Description: "コインを発行する相手", Required: true},
					// MaxValue is a plain float64 in discordgo v0.29.0 (NOT a pointer — unlike MinValue),
					// so it is set directly. float64 represents 1e12 exactly (< 2^53), no precision loss.
					// This is UX only; the real enforcement is casino.Mint's MaxCoins check (§3.5).
					{Type: discordgo.ApplicationCommandOptionInteger, Name: "amount", Description: "発行するコインの枚数（1以上）", Required: true, MinValue: floatPtr(1), MaxValue: float64(casino.MaxCoins)},
				},
			},
			{
				Type: discordgo.ApplicationCommandOptionSubCommand, Name: "channel", Description: "毎朝9時のレート掲示先チャンネルを設定します",
				Options: []*discordgo.ApplicationCommandOption{
					{Type: discordgo.ApplicationCommandOptionChannel, Name: "channel", Description: "掲示先チャンネル", Required: true, ChannelTypes: []discordgo.ChannelType{discordgo.ChannelTypeGuildText}},
				},
			},
		},
	}
}

func translateMintError(err error) string {
	switch {
	case errors.Is(err, casino.ErrInvalidAmount):
		return "❌ 発行枚数は1以上・上限以内で指定してください。"
	case errors.Is(err, casino.ErrCoinCapExceeded), errors.Is(err, casino.ErrAmountOverflow):
		return "❌ 発行枚数が大きすぎます。対象ユーザーの残高上限を超えます。"
	default:
		slog.Error("casino: Mint failed", "error", err)
		return "❌ コインの発行に失敗しました。"
	}
}

func (c *CasinoAdminCommand) handleMint(guildID string, opts []*discordgo.ApplicationCommandInteractionDataOption, now time.Time) string {
	var targetUserID string
	var amount int64
	for _, opt := range opts {
		switch opt.Name {
		case "user":
			targetUserID = opt.UserValue(nil).ID
		case "amount":
			amount = opt.IntValue()
		}
	}
	// §3.8: open the RECIPIENT's account in its own transaction first, so a
	// cap-exceeded Mint cannot roll their brand-new account and welcome bonus
	// back. (The invoking admin's own account is opened by buildResponse.)
	if err := c.store.EnsureCasinoAccess(guildID, targetUserID, now); err != nil {
		slog.Error("casino: EnsureCasinoAccess failed", "command", "casino-admin/mint", "error", err)
		return "❌ カジノの初期化に失敗しました。"
	}
	if err := c.store.Mint(guildID, targetUserID, amount); err != nil {
		return translateMintError(err)
	}
	return fmt.Sprintf("✅ <@%s> に %d 枚のコインを発行しました。", targetUserID, amount)
}

func (c *CasinoAdminCommand) handleChannel(guildID string, opts []*discordgo.ApplicationCommandInteractionDataOption) string {
	var channelID string
	for _, opt := range opts {
		if opt.Name == "channel" {
			channelID = opt.ChannelValue(nil).ID
		}
	}
	if err := c.store.SetAnnounceChannel(guildID, channelID); err != nil {
		slog.Error("casino: SetAnnounceChannel failed", "error", err)
		return "❌ 掲示チャンネルの設定に失敗しました。"
	}
	return fmt.Sprintf("✅ 掲示チャンネルを <#%s> に設定しました。", channelID)
}

// buildResponse is Handle's discordgo-Session-free core, extracted for
// testing (permission/guild checks + subcommand dispatch). now is injected
// so tests are not wall-clock dependent.
func (c *CasinoAdminCommand) buildResponse(i *discordgo.InteractionCreate, now time.Time) string {
	if msg := requireGuildContext(i); msg != "" {
		return msg
	}
	if msg := requireAdministrator(i); msg != "" { // 設計書の文言と完全一致(実行時の二重チェック)
		return msg
	}
	if err := c.store.EnsureCasinoAccess(i.GuildID, resolveUserID(i), now); err != nil { // §3.8
		slog.Error("casino: EnsureCasinoAccess failed", "command", "casino-admin", "error", err)
		return "❌ カジノの初期化に失敗しました。"
	}
	data := i.ApplicationCommandData()
	if len(data.Options) == 0 {
		return "❌ サブコマンドを指定してください（mint / channel）。"
	}
	sub := data.Options[0]
	switch sub.Name {
	case "mint":
		return c.handleMint(i.GuildID, sub.Options, now)
	case "channel":
		return c.handleChannel(i.GuildID, sub.Options)
	default:
		return "❌ 不明なサブコマンドです。"
	}
}

func (c *CasinoAdminCommand) Handle(s *discordgo.Session, i *discordgo.InteractionCreate) error {
	return s.InteractionRespond(i.Interaction, &discordgo.InteractionResponse{
		Type: discordgo.InteractionResponseChannelMessageWithSource,
		Data: &discordgo.InteractionResponseData{Content: c.buildResponse(i, time.Now())},
	})
}
```

**テスト**: `TestCasinoAdminCommand_Definition_HasAdministratorPermissionAndGuildContext`(`DefaultMemberPermissions` と `Contexts` の両方、および **`mint.amount` に `MinValue` と `MaxValue` が両方入っていること**を検査)、`TestBuildResponse_NonGuildContext_ReturnsError`、`TestBuildResponse_NonAdministrator_ReturnsExactDesignMessage`(`i.Member.Permissions` にAdministratorビットを含まない値をセットして `"❌ このコマンドはサーバー管理者のみ使用できます"` と完全一致することを確認)、`TestBuildResponse_Mint_Success`(実 `casino.New(filepath.Join(t.TempDir(), "casino.json"))` 注入、`i.Member.Permissions` にAdministratorビットをセット)、**`TestBuildResponse_Mint_ExceedsMaxCoins_RecipientKeepsWelcomeBonus`**(上限超過の mint が失敗しても、発行先の口座と1,000チップが残ること — BL-4)、`TestBuildResponse_Channel_Success`、`TestTranslateMintError_{InvalidAmount,CapExceeded,Overflow}`。

**完了条件**: `go test ./internal/commands/... -run 'TestCasinoAdminCommand|TestBuildResponse|TestTranslateMintError'` すべて `ok`。

---

## Task 16 — `internal/commands/casino_announce.go`: 9時掲示のdiscordgo配線(スラッシュコマンドではない)

**目的**: §3.1 で決めたとおり、`internal/casino`(discordgo非依存)と実際のDiscord送信を繋ぐ薄い層。`Command`インターフェースを実装しない・`Register`しない — `cmd/bot/main.go` から直接1回だけ呼ばれる関数。

```go
package commands

import (
	"context"
	"fmt"
	"strings"
	"time"

	"github.com/SioKo-Shox3/todayistodayBot/internal/casino"
	"github.com/bwmarrin/discordgo"
)

var announceMedals = []string{"🥇", "🥈", "🥉"}

// buildAnnouncementEmbed renders one AnnouncementJob into the 9am posting.
// 設計書 L62-68 requires FOUR elements and all four are present here:
//  1. today's rate + day-over-day change (rateChangeLine, shared with /rate
//     so the two renderings cannot disagree — they used to differ on
//     diff == 0)
//  2. the 7-day sparkline
//  3. the 相場コメント (marketCommentary)
//  4. the top-3 total-asset ranking
//
// The 🚀/💥 tag comes from the PERSISTED casino.EventKind via
// rateChangeLine, not from a day-over-day threshold. A threshold cannot
// work: the 70/140 clamp turns a genuine +15% surge from 130 into a
// +7.7% move, so a 10% threshold would silently drop 🚀 on exactly the
// days that most deserve it (§3.2).
func buildAnnouncementEmbed(job casino.AnnouncementJob) *discordgo.MessageEmbed {
	values := make([]int, len(job.RecentRates))
	for i, r := range job.RecentRates {
		values[i] = r.Rate
	}

	changeLine := rateChangeLine(job.RecentRates)   // rate.go (Task 11)
	commentary := marketCommentary(job.RecentRates) // casino_shared.go (Task 8)

	rankLines := make([]string, 0, len(job.TopAssets))
	for i, e := range job.TopAssets {
		marker := fmt.Sprintf("%d.", i+1)
		if i < len(announceMedals) {
			marker = announceMedals[i]
		}
		rankLines = append(rankLines, fmt.Sprintf("%s <@%s> — %d", marker, e.UserID, e.TotalAssets))
	}
	rankValue := "まだ誰も口座を持っていません。"
	if len(rankLines) > 0 {
		rankValue = strings.Join(rankLines, "\n")
	}

	return &discordgo.MessageEmbed{
		Title:       "💱 本日のコインレート",
		Description: fmt.Sprintf("1コイン = %dチップ\n%s\n直近%d日: %s\n%s", job.Today.Rate, changeLine, len(job.RecentRates), sparkline(values), commentary),
		Color:       0xf1c40f,
		Fields: []*discordgo.MessageEmbedField{
			{Name: "総資産ランキング TOP3", Value: rankValue, Inline: false},
		},
	}
}

// startAnnounceScheduler is StartCasinoAnnounceScheduler's testable core:
// it takes the store, a send function, a clock and a sleeper instead of a
// *discordgo.Session, so tests can drive it with casino.New(t.TempDir()),
// a fake sender and a FIXED clock.
//
// now and sleep MUST be parameters, not time.Now/casino.RealSleepUntil
// hardcoded here. casino.runAnnouncePass only posts when
// casino.announceWindowOpen(now) is true, i.e. from 09:00 JST onward. With a
// hardcoded real clock, any test that asserts "send was called" would fail
// (or hang until go test's 10-minute timeout) whenever the suite happens to
// run between 00:00 and 08:59 JST, and any test written to avoid that hang
// would assert nothing at all — a test that cannot fail is not evidence
// (Docs/agent-guide/build-and-verify.md). casino.SleepFunc is already
// exported for exactly this reason (see its doc comment, Task 6).
func startAnnounceScheduler(ctx context.Context, st *casino.Store, send func(channelID string, embed *discordgo.MessageEmbed) error, now func() time.Time, sleep casino.SleepFunc) (wait func()) {
	done := make(chan struct{})
	go func() {
		defer close(done)
		casino.RunAnnounceScheduler(ctx, st, func(job casino.AnnouncementJob) error {
			return send(job.ChannelID, buildAnnouncementEmbed(job))
		}, now, sleep)
	}()
	return func() { <-done }
}

// StartCasinoAnnounceScheduler starts the 9am-JST casino rate announcement
// scheduler as a background goroutine and returns immediately, together
// with a wait function that blocks until that goroutine has fully exited.
// This is NOT a slash command — it has no Definition()/Handle() and is not
// Register()'d — it is Discord-facing infrastructure wiring for
// internal/casino.RunAnnounceScheduler (itself discordgo-free).
// cmd/bot/main.go calls this exactly once (Task 17).
//
// The caller MUST invoke wait() after cancelling ctx and BEFORE closing the
// session: the scheduler can be inside s.ChannelMessageSendEmbed when the
// shutdown signal arrives, and tearing the session down underneath it is a
// use-after-close race. Returning nothing (as an earlier draft did) makes
// that impossible to get right from the outside.
//
// This is the ONLY place that supplies the real clock and the real sleeper.
func StartCasinoAnnounceScheduler(ctx context.Context, s *discordgo.Session) (wait func()) {
	return startAnnounceScheduler(ctx, casino.Default(), func(channelID string, embed *discordgo.MessageEmbed) error {
		_, err := s.ChannelMessageSendEmbed(channelID, embed)
		return err
	}, time.Now, casino.RealSleepUntil)
}
```

**テスト(`casino_announce_test.go`)**:
- `TestBuildAnnouncementEmbed_{FirstDayNoChangeLine,NormalDayNoEventTag,BoomDayShowsRocket,CrashDayShowsExplosion,EmptyTopAssetsShowsPlaceholder,TopAssetsRenderedWithMedals}`
- **`TestBuildAnnouncementEmbed_IncludesMarketCommentary`** — 設計書の4要素目(相場コメント)が
  Description に含まれること(BL-5)。
- **`TestBuildAnnouncementEmbed_SurgeClampedToCeiling_StillShowsRocket`** — 130→140 の暴騰
  (前日比 7.7%)でも `Event == EventSurge` なら 🚀 が出ること。
- **`TestBuildAnnouncementEmbed_FlatDayShowsDash`** — `diff == 0` のとき `/rate` と同じ `➖` になること
  (両者が `rateChangeLine` を共有していることの回帰テスト)。
- **`TestStartAnnounceScheduler_WaitBlocksUntilCallbackFinishes`(SF-3)** — テスト専用ストアに
  掲示チャンネル設定済みギルドを仕込み、`send` をチャネルでブロックさせた状態で ctx を cancel し、
  `send` を解放するまで `wait()` が戻らないことを検証する。
  **`now` は当日 10:00 JST を返すスタブに固定し、`sleep` もスタブ(即 `false`)を渡す**
  — 実クロックのままだと 00:00〜08:59 JST に走らせたとき `announceWindowOpen` が偽になり
  `send` が一度も呼ばれないため、**テストが時刻依存でハング(10分タイムアウトで FAIL)するか、
  あるいは何も検証しない**。`startAnnounceScheduler` が `now`/`sleep` を引数に取るのはこのため。
  実時間待機は一切発生しない。

**完了条件**: `go test ./internal/commands/... -run 'TestBuildAnnouncementEmbed|TestStartAnnounceScheduler'` すべて `ok`。`go build ./...` で `StartCasinoAnnounceScheduler` / `startAnnounceScheduler` の型シグネチャがコンパイルエラーなく通ること。
**実行時間がミリ秒オーダーであること、および1日のどの時刻に実行しても結果が変わらないこと**を確認する
(時刻依存のテストは Done ゲートを不安定にする)。

---

**Task 9〜16 の完了条件(まとめ)**: `go build ./...` エラー0、`go vet ./...` 警告0、`go test ./internal/commands/...` すべて `ok`。この時点でまだ `cmd/bot/main.go` は変更していないため、`/casino-admin` を含む15個目のコマンドは `commands.All()` には**含まれるが**掲示スケジューラは**まだ起動しない**(main.goの変更はTask 17)。

**コミット境界**: Task 8〜Task 16 をまとめて **Commit 6**(`internal/commands` のカジノ層完成 — 7コマンド登録+`casino_announce.go`)。この時点で `go build ./... && go vet ./... && go test ./...` が通ることを確認してからコミットする。

---

## Task 17 — `cmd/bot/main.go`: 掲示スケジューラのgoroutine起動(唯一の変更)

**目的**: 設計書「`cmd/bot/main.go` にフェーズA以来初めての変更が入る…掲示スケジューラのgoroutineを起動する1箇所のみ」を実現する。

**変更ファイル**: `cmd/bot/main.go` のみ(`cmd/bot/main_test.go` は変更不要 — `findCommandByName` という純粋関数のみを対象にしており、今回の変更の影響を受けないことを確認済み)。

**現状**(`cmd/bot/main.go`、読了済み):
```go
	ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
	defer stop()
	<-ctx.Done()

	slog.Info("shutdown signal received, closing session")
```

**変更後**(既存の `ctx`/`stop` の生成位置・順序は一切動かさない — `ctx, stop := signal.NotifyContext(...)` は既に `session.Open()` 後・コマンド登録後・`<-ctx.Done()` の直前という、スケジューラ起動に必要十分な位置にあるため、構造変更は不要):

```go
	ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
	defer stop()

	// フェーズC-1: 毎朝9:00 JSTのレート掲示スケジューラ(起動直後に1回パスを走らせる)
	waitScheduler := commands.StartCasinoAnnounceScheduler(ctx, session)

	<-ctx.Done()

	slog.Info("shutdown signal received, waiting for casino scheduler")
	waitScheduler() // 掲示送信中にセッションを閉じないよう、goroutineの終了を待つ

	slog.Info("shutdown signal received, closing session")
```

`commands.StartCasinoAnnounceScheduler`(Task 16で定義済み)は内部で goroutine を起動して即座に返るので、この追加は `main()` の他の挙動(コマンド登録・ディスパッチ)に一切影響しない。`ctx` はシャットダウンシグナル(SIGINT/SIGTERM)で `Done()` になる既存の同一 `ctx` を渡すので、Bot終了時にスケジューラのgoroutineも `casino.RealSleepUntil` 内の `select` から抜けて終了する。
**`waitScheduler()` を `defer session.Close()`(既存、`main` の末尾で走る)より前に呼ぶ**ことで、掲示の `ChannelMessageSendEmbed` が飛んでいる最中にセッションが閉じられる競合を防ぐ(SF-3)。

**import**: 追加不要(`internal/commands` は既にimport済み)。

**テスト**: この変更自体に新規の自動テストは追加しない — `main()` はエントリポイントであり、既存コードも `findCommandByName` 以外は自動テストの対象外(実Discord接続が必要なため)。**代わりに以下を確認する**:
1. `go build ./cmd/bot/...` がエラー0で通ること(`waitScheduler := commands.StartCasinoAnnounceScheduler(ctx, session)` の型シグネチャ — `(context.Context, *discordgo.Session) func()` — がコンパイルレベルで正しいことの確認)。
2. **手動起動確認 — 【Done の必須条件】(BL-7)**: `config.json` または `DISCORD_TOKEN` を設定したローカル環境で `go run ./cmd/bot` を実行し、ログに `"bot is running" registered_commands=15`(既存8+新規7)が出て、**直後にクラッシュ/終了しない**ことを確認する。**実出力を証拠として貼ること**(`Docs/agent-guide/build-and-verify.md` の「証拠の基準」)。

   **なぜ推奨ではなく必須か**: 本フェーズは**このリポジトリで初めて `Contexts` と
   `DefaultMemberPermissions` を使う**。`cmd/bot/main.go:72-77` は `ApplicationCommandCreate` が
   1つでも失敗したら `os.Exit(1)` する。つまり Discord 側が新しい payload
   (`contexts` / `default_member_permissions` / サブコマンドオプションの `channel_types` /
   `min_value`・`max_value`)を1つでも拒否すると、Bot は degraded ではなく**起動不能**になり、
   **既存8コマンドも巻き添えで全滅する**。これは `go build` / `go vet` / `go test` の
   **どれでも検出できない**種類の破壊なので、自動ゲートの代替にならない。
   この確認を**最終コミット(Commit 7)の前提条件**とする。

   **UNCERTAIN(実APIを叩くまで解消しない)**: Discord API が `contexts` を `integration_types` 無しで
   受理するか。discordgo 側の構造体・タグが正しいことは実ソースで確認済み
   (`discordgo@v0.29.0/interactions.go:44,49`)だが、サーバ側の受理可否は実測でしか分からない。
   もし拒否された場合は**計画を止めて親へ報告する**(`Contexts` を落とすか `integration_types` を
   足すかは設計判断であり、実装者が独断で変えない)。

3. 9:00 JSTの実際の掲示は待てないので、**起動直後のキャッチアップパス**(D1)で代替確認する:
   `/casino-admin channel` で掲示先を設定 → Bot を再起動 → **起動直後にその日のレートが掲示される**
   ことを確認する(起動時パスが実際に動いている証拠)。
   **この確認は 09:00 JST 以降に行うこと** — 掲示には 09:00 の下限があるため、
   9時前に再起動しても(仕様どおり)レート生成だけが行われ掲示は飛ばない。
   9時前に確認せざるを得ない場合は、`data/casino.json` に生成された当日の `rates` エントリの存在をもって
   「レート生成は動いた」ことの証拠とし、掲示の確認は9時以降に回す。

**完了条件**: `go build ./...` エラー0、`go vet ./...` 警告0、`go test ./...`(既存 `cmd/bot` のテストが引き続き `ok`)、**および上記2の手動起動確認の実出力**。

**コミット境界**: **Commit 7** の一部(Task 18〜19のドキュメント更新とまとめて1コミット — 「フェーズC-1の統合」というひとつの論理的単位)。

---

## Task 18 — `Docs/agent-guide/architecture.md`: `internal/casino` の追記

**目的**: 危険地帯は「共有可変状態には`sync.Mutex`または単一書き込み者の規律が要る」と書く。今回は本フェーズ自体が2つ目の永続化層を導入するので、既存の危険地帯パターン(`internal/store`)をそのまま `internal/casino` に拡張する形で書く。

**変更ファイル**: `Docs/agent-guide/architecture.md` のみ。

**追記1 — レイヤー表に1行追加**(「ローカル永続化」行の直後):
```
| カジノ経済 | 2層通貨(コイン/チップ)・日次レート・スロット抽選・9時掲示スケジューラ(discordgo非依存) | `internal/casino/*.go` |
```
併せて、表の直後の説明文に1文追加:
> フェーズC-1で `internal/commands` に7コマンド(balance/daily/rate/exchange/slot/rank/casino-admin)と、
> スラッシュコマンドではない配線ファイル `casino_announce.go`(`cmd/bot/main.go` から直接呼ばれる
> 9時掲示スケジューラの起動)を追加した。

**追記2 — 所有権と寿命(既存の `internal/store.Default()` 説明の直後)**:
```
- `internal/casino.Default()` も同様のプロセス内シングルトンパターン(`sync.Once`)。フェーズC-1で
  追加した7コマンド(balance/daily/rate/exchange/slot/rank/casino-admin)と
  `internal/commands/casino_announce.go` の9時掲示スケジューラ(`cmd/bot/main.go` が起動する唯一の
  goroutine)は、すべて `casino.Default()` を経由して同一の `*casino.Store`(=同一の `sync.Mutex`)
  を共有する。`internal/store.Store` と `internal/casino.Store` は別ファイル
  (`data/chosei-events.json` と `data/casino.json`)を管理する独立したミューテックスであり、
  互いに排他しない(別ファイルなので競合せず、設計上問題ない)。
```

**追記3 — 依存方向に2行追加**:
```
- `internal/commands/*` → `internal/casino`(フェーズC-1で追加。7コマンド + `casino_announce.go` が
  `casino.Default()` を使用)。
- `internal/casino` → 標準ライブラリのみ。discordgoを含む他の内部パッケージに一切依存しない
  (`internal/weather`/`internal/today`/`internal/choseisama`/`internal/store` と同じ規約 — 経済ロジックと
  掲示スケジューリングのタイミング計算をDiscordから独立させる。9時掲示の実送信は
  `internal/commands/casino_announce.go` が担う)。
```

**追記4 — 危険地帯に4点目を追加**:
```
4. **`internal/casino` の単一ライター規律(フェーズC-1で新規)** — `internal/store`と全く同じ理由・
   同じ規律。カジノ系7コマンド + 9時掲示スケジューラ(`internal/commands/casino_announce.go` 経由で
   `cmd/bot/main.go` が起動する唯一のgoroutine)は、必ず `casino.Default()`(プロセス内シングルトン)
   経由で `data/casino.json` にアクセスすること。`casino.New(path)` で独立に `*casino.Store` を
   構築して本番コードで使うと、同じファイルに対して排他されない複数の `sync.Mutex` が存在することに
   なり、同時ベット(`/slot`)や同時両替(`/exchange`)で残高のロスト更新・JSON破損が起こり得る
   (`internal/casino/store_test.go` の並行 `Spin`/`Update` テストはこの規律が壊れていないことの
   回帰テスト)。加えて、`Store.Update` は再入不可の `sync.Mutex` を使うため、**`Update` の
   クロージャ内から `s.Update`/`s.EnsureTodayRate` などロックを取る公開メソッドを呼ぶとデッドロックする**
   — 同一トランザクション内で「今日のレートを保証しつつ残高を操作する」必要がある箇所は、ロックを
   取らない非公開の `*Locked` ヘルパー(`ensureTodayRateLocked`/`topAssetsLocked` など)を使うこと。
```

**テスト/検証**: ドキュメント変更のみなので自動テスト対象外。`git diff Docs/agent-guide/architecture.md` を目視確認し、既存の記述(フェーズA/Bの内容)を誤って書き換えていないこと(追記のみであること)を確認する。あわせて `git diff --numstat` と `git diff --ignore-cr-at-eol --numstat` を比較し、行末の全体書き換えが起きていないことを確認する(`CLAUDE.md` 運転規律4)。

**コミット境界**: Commit 7 の一部。

---

## Task 19 — `CLAUDE.md` / `AGENTS.md`: 絶対規則3への追記(両ファイル同一内容)

> ### ⚠️ このタスクだけはユーザーの明示承認が必要
>
> `CLAUDE.md` / `AGENTS.md` は**「働き方の合意」そのもの**であり、その改変はコード変更とは
> 性質が違う。承認依頼の際は**この1点を他タスクに埋もれさせず単独で提示し、明示的な
> 承認を取ってから**着手すること。承認が得られない場合は Task 18(`architecture.md`)までで
> 止め、Task 19 は残課題として報告する(他タスクの Done を妨げない)。

**目的**: 「`CLAUDE.md`と`AGENTS.md`は完全に同一内容で運用する」運用規約に従い、絶対規則3(並行性)に `casino.Default()` の言及を追加する。**スコープを絶対規則3の追記のみに限定する**(「プロジェクト概要」節への機能追記は本タスクの必須範囲外とする — ドキュメント差分を必要最小限に保つための意図的な判断)。

**変更ファイル**: `CLAUDE.md`、`AGENTS.md`(**必ず同一の差分を両方に適用する** — `diff CLAUDE.md AGENTS.md` が空になることをコミット前に確認する)。

**現状**(両ファイル共通、確認済み):
```
3. **並行性はGoの規律に従う** — goroutine/channelを使い、共有可変状態には`sync.Mutex`/`sync.WaitGroup`で
   排他する。フェーズBで導入した `internal/store.Store` の `Save`/`FindLatestByChannel`/
   `FindByChannelAndRef` は `sync.Mutex` で単一ライター化されており、`schedule`/`schedule-result`/
   `reminder` の3コマンドは必ず `store.Default()` シングルトンを経由してこのミューテックスを共有する
   (→ `Docs/agent-guide/architecture.md` の危険地帯)。
```

**変更後**(末尾に1文追加):
```
3. **並行性はGoの規律に従う** — goroutine/channelを使い、共有可変状態には`sync.Mutex`/`sync.WaitGroup`で
   排他する。フェーズBで導入した `internal/store.Store` の `Save`/`FindLatestByChannel`/
   `FindByChannelAndRef` は `sync.Mutex` で単一ライター化されており、`schedule`/`schedule-result`/
   `reminder` の3コマンドは必ず `store.Default()` シングルトンを経由してこのミューテックスを共有する
   (→ `Docs/agent-guide/architecture.md` の危険地帯)。フェーズC-1で追加した `internal/casino.Store`
   も同じ規律に従う — カジノ系7コマンド+9時掲示スケジューラは必ず `casino.Default()` を経由し、
   `Update` のクロージャ内から再度ロックを取るメソッドを呼ばない(→ 同architecture.mdの危険地帯4点目)。
```

**検証**: `diff CLAUDE.md AGENTS.md` が出力なし(完全一致)であること。

**コミット境界**: Commit 7 の一部。

---

## 全体の検証手順(証拠の基準は `Docs/agent-guide/build-and-verify.md` に準拠)

### フェーズ全体のDoneの前に実行するコマンド(コピペ可能)

```bash
go build ./...
go vet ./...
go test ./...
```
**成功の判定**: `go build ./...` は無出力・終了コード0。`go vet ./...` は無出力・終了コード0。`go test ./...` は以下のパッケージすべてに `ok` 行が出て `FAIL` が1つも無いこと:
`internal/config`, `internal/commands`, `internal/weather`, `internal/today`, `internal/choseisama`, `internal/store`, **`internal/casino`(新規)**, `cmd/bot`。

### このフェーズ特有の追加検証(既存ゲートへの追加であり、置き換えではない)

```bash
go test -race ./internal/casino/...
```
**成功の判定**: `ok` で終わり、`WARNING: DATA RACE` が出力に含まれないこと。Task 7の並行性回帰テストが対象。設計書のミューテックス規律を保証する最も直接的な証拠なので、**このフェーズに限り必須**とする。
**`-race` は Linux 環境(CI・WSL・コンテナ等)で実行すること**(§3.7-6)。
**Windows 開発機(Cコンパイラなし)では `-race` が `cgo` 不足で実行できないことが既知**(フェーズA/Bで確認済み) — その場合は**非raceでのテスト成功を代替証拠とし、`-race` を省略した旨を必ず報告する**(黙って省略しない)。コードは Linux で `-race` が通る前提で書くこと。

```bash
go test ./internal/casino/... -run 'TestSpin_RTP|TestPayoutTable' -v
```
**成功の判定**: `PASS` で、実行時間が数秒以内(seed=1 の100万回スピン1本+216通り全列挙の literal 検査。通常ミリ秒〜数百ミリ秒で終わるはず — 数十秒かかっていたら実装に想定外の重い処理が混入している兆候なので調査する)。
**注**: リポジトリにコミットする統計テストは **seed=1 の1本のみ**である(計画作成時の5シード×100万スピンは
**計画段階の検証**であって、テストコードとして残すものではない — 5本走らせても得られる保証は
`TestPayoutTable_ExactLiterals` の厳密検査に及ばない)。

```bash
diff CLAUDE.md AGENTS.md
```
**成功の判定**: 無出力(完全一致)。

```bash
make build
```
**成功の判定**: `bin/todayistodaybot`(または相当のバイナリ)が生成されエラーが出ないこと。新規パッケージ追加によりMakefileのビルド対象リストが暗黙に壊れていないことの最終確認。
**注**: `bin/` は「編集・コミット禁止/検証による生成は許可」(§書き込み禁止)。生成物は `.gitignore`
済みなのでコミットには載らない。

### プラットフォーム非依存の機械的確認(§3.7 — 実装後に実行して結果を報告する)

```bash
grep -rn "time\.Local\|LoadLocation\|GOOS\|//go:build\|+build" internal/casino/ internal/commands/casino*.go internal/commands/balance.go internal/commands/daily.go internal/commands/rate.go internal/commands/exchange.go internal/commands/slot.go internal/commands/rank.go
```
**成功の判定**: `time.Local` / `LoadLocation`(テストの意図的な使用を除く)/ `GOOS` / build tag が
**1件も出ないこと**。出た場合は §3.7 違反なので直す。

```bash
grep -rn '"[^"]*\\\\[^"]*"' internal/casino/
```
**成功の判定**: パス区切りのハードコード(`\\`)が出ないこと。パス結合は `filepath.Join` のみ(§3.7-5)。

### 手動確認(自動化されていない、`build-and-verify.md` の既存方針どおり)

- **【Done の必須条件、BL-7】** ローカルで `go run ./cmd/bot` を実行し、`registered_commands=15` のログと共に起動し、
  **直後にクラッシュ/終了しない**こと。**実出力を証拠として貼る**。
  (理由と失敗時の扱いは Task 17 の「手動起動確認」を参照 — Discord が新しい payload を拒否すると
  既存8コマンドごと起動不能になり、これは自動ゲートでは検出できない)
- テスト用Discordサーバーで7コマンドを一通り実行:
  - `/balance`(初回実行でウェルカムボーナス1,000チップ+ephemeral表示になること)
  - `/daily`(2回目実行で「❌ 本日は既に受け取り済みです」になること)
  - `/rate`(スパークラインが表示されること)
  - `/exchange coin-to-chip`/`chip-to-coin`(コインが無い状態でコイン→チップを試して「❌ コインが足りません」になること)
  - `/slot`(リールが1個ずつ止まる演出になること。ベット範囲外(例: 5や2000)で「❌ ベットは10〜1,000チップです」になること)
  - `/rank`
  - `/casino-admin mint`/`channel`(管理者権限を持つアカウントで成功すること、**管理者権限を持たないアカウントでは`/casino-admin`自体がDiscordのコマンド一覧に表示されない**こと、直接叩けたとしても「❌ このコマンドはサーバー管理者のみ使用できます」になること)
- **起動時キャッチアップの実地確認(D1)**: `/casino-admin channel` で掲示先を設定してから Bot を再起動し、
  **起動直後にその日のレート掲示が投稿される**こと。**09:00 JST 以降に実施すること**
  (掲示には 09:00 の下限があり、9時前の再起動ではレート生成のみが行われる — 仕様どおり)。
  9:00 JST の定時掲示そのものはこの検証の必須条件にしない(Task 6の自動テストで代替)。
- 手動確認は `data/casino.json` を生成する。**コミット前に `git status` がクリーンであることを確認する**
  (`/data/` は `.gitignore` 済みなので通常は載らないが、確認は省かない)。

---

## リスクレベル

`Docs/agent-guide/orchestration.md` の「昇格条件(高リスク領域)」および `architecture.md` の既存「危険地帯」パターンと突き合わせると、本フェーズは以下が該当する:

1. **`internal/commands/registry.go`(Task 1)と `cmd/bot/main.go`(Task 17)** — orchestration.md「`commands.Command`契約・自己登録レジストリ・`cmd/bot/main.go`のディスパッチ配線 — 全コマンドに波及する」に**文字通り該当**。特にTask 1の変更は既存8コマンド全ての起動可否に影響し得る(重複名判定のロジックにバグがあれば、無関係な既存コマンドの起動が壊れる)。
   - **封じ込め策**: Task 1は独立した最小のコミット(**Commit 1**)とし、他の変更と混ぜない。テストは新規追加分だけでなく`TestRegisterAndAll`(既存)も必ず再実行して既存動作に影響がないことを確認する。
   - **さらに Task 17 固有のリスク(BL-7)**: `cmd/bot/main.go` の `ApplicationCommandCreate` ループは
     1つでも失敗すると `os.Exit(1)` する。本フェーズは**このリポジトリで初めて `Contexts` /
     `DefaultMemberPermissions` / `channel_types` / `max_value` を送る**ので、Discord 側が
     どれか1つでも拒否すれば **Bot は起動不能になり既存8コマンドも全滅する**。
     `go build`/`go vet`/`go test` の**どれでも検出できない**。
     → **封じ込め策**: Task 17 の手動起動確認(`registered_commands=15` が出て即死しないこと)を
     **Done の必須条件**とし、実出力を証拠として残す。
2. **`internal/casino.Store` の単一ライター規律(Task 2, 5〜7)** — `internal/store`と同格の危険地帯(architecture.md 危険地帯4点目、Task 18で追記)。**加えて本フェーズ固有の新しいリスク**として、`Update`クロージャ内から再度ロックを取るとデッドロックする構造(`ensureTodayRateLocked`等の`*Locked`ヘルパー分離)がある — この分離を実装者が誤って崩す(=`s.Update`をクロージャ内から呼んでしまう)と、本番で特定の操作(両替・スロット・ランキング表示)がハングする重大障害になり得るが、**ローカルの単体テストでは検出しにくい**(デッドロックはテストがタイムアウトするまで気づかれない可能性がある)。
   - **封じ込め策**: 実装レビューで`*Locked`ヘルパーの呼び出し元をすべて確認し、`s.Update`/`s.EnsureTodayRate`等ロックを取る公開メソッドが他の公開メソッドの`Update`クロージャ内から呼ばれていないことを目視+`grep`で機械的に確認する(例: `grep -n "s\.Update\|s\.EnsureTodayRate\|s\.TopAssets\|s\.RecentRates" internal/casino/*.go` でクロージャ内呼び出しがないか洗う)。`go test ./...`のデフォルトタイムアウト(パッケージあたり10分)により、デッドロックがあれば少なくとも検証時にハングという形で顕在化する(サイレントに見逃されることはない)。
3. **`/casino-admin`の権限ガード(Task 15)** — 明示的な危険地帯リストには無いが、**任意のコインを無制限に発行できる管理者専用コマンド**であり、権限チェックの不備は経済の完全性を破壊する。`DefaultMemberPermissions`(Discord UI側)と`requireAdministrator`(実行時)の二重ガードは設計書の要求どおりだが、**実装レビューでこの2つが両方とも実際にDefinition()/Handle()に存在し、どちらか一方だけになっていないことを重点確認する**。
   - **封じ込め策**: `TestCasinoAdminCommand_Definition_HasAdministratorPermissionAndGuildContext`(Definition側)と`TestBuildResponse_NonAdministrator_ReturnsExactDesignMessage`(実行時側)の両方が独立してレビューで確認されること。
4. **スロットの配当表(Task 4)の数値的正しさ** — 経済的に大きな影響(RTPが計算間違いで93%を大きく外れると、長期的にBotの通貨経済が壊れるか、あるいは即座に破綻するほど気前が良すぎる)。本計画では厳密分数演算+実測(Go `math/rand`5シード)+改訂時の216通り独立再計算で**三重に検証済み**。**実装レビューでこの計画に書かれた重み・倍率の表がそのまま`internal/casino/slot.go`に転記されているか(実装者が「もっともらしい」別の数値に変えていないか)を数値レベルで突き合わせて確認すること**。`TestPayoutTable_ExactLiterals`(Task 4 テスト6)がこの退行を機械的に防ぐ受け皿である。
5. **`readLocked` の nil マップ経路(Task 2)** — 0バイト/`null` の `data/casino.json` を読むと map への代入が
   panic し、discordgo が recover 無しでハンドラ goroutine を起動するため**プロセスごと落ちる**。
   ファイルは壊れたまま残るので**恒久クラッシュループ**になる(人手で削除するまで復旧しない)。
   - **封じ込め策**: §3.3 の実装コードどおり3経路すべてで `Data{}` を返し、
     `TestStore_Read_EmptyFileTreatedAsEmptyData` / `_NullJSONTreatedAsEmptyData` /
     `TestStore_Mint_AfterZeroByteFile_DoesNotPanic` で機械的に守る。
     同じ理由で `rateRankCommentary`/`buildRateMessage`/`marketCommentary` にも空判定を入れる(SF-9)。
6. **int64 オーバーフローによる静かな残高破損(§3.5)** — 上限が無いと、管理者が最大値ミントを
   数回繰り返すだけで `Coins × 140` が int64 を超え、`/rank` と `/balance` の総資産が**負値へラップ**する。
   例外も panic も出ないので**気づかれにくい**。
   - **封じ込め策**: `MaxChips`/`MaxCoins` による構造的封じ込め + 全 credit 経路の
     `creditChipsLocked`/`creditCoinsLocked` 一本化 + `TestStore_TopAssets_MaxBalances_DoesNotOverflow`。
     実装レビューでは **`account.Chips +=` / `account.Coins +=` の直書きが1箇所も無いこと**を
     `grep -n "\.Chips +=\|\.Coins +=" internal/casino/*.go` で機械的に確認する
     (ヒットしてよいのは `creditChipsLocked`/`creditCoinsLocked` の中だけ)。

**ロールバック/封じ込め戦略**: Commit 1〜7は各々独立してrevert可能な単位にする(各コミット時点で `go build`/`go vet`/`go test` が緑になる状態を保つ)。

本番投入後に経済的な不整合(残高のロスト更新・レート異常等)が発覚した場合の手順は、
**軽い方から順に**試す:

1. **コードのrevert**(第一選択) — 問題のコミットを `git revert` する。`data/casino.json` は触らない。
   コミットが7分割されているので、原因箇所だけを限定的に戻せる(bisect も効く)。
2. **データのバックアップと部分修復** — まず `data/casino.json` を**タイムスタンプ付きでコピーして退避**
   (`data/casino.json.bak-YYYYMMDD-HHMMSS`)。そのうえで、壊れた特定ギルド/特定ユーザーのエントリだけを
   Bot 停止中に手で修正する。ファイルは人間可読な JSON なので部分修復が可能。
3. **⚠️ `data/casino.json` の全削除は最終手段** — これは**全ギルド・全ユーザーの残高・コイン・
   ストリーク・レート履歴・掲示チャンネル設定をすべて失う**破壊的操作である。
   確かに Bot は自己修復する(次回アクセス時に口座・レートが自動再生成される)が、
   **プレイヤーの積み上げが消える**ので「gitignore 対象だから気軽に消してよい」ものではない。
   **実行にはユーザーの明示承認を要する**。実行する場合も必ず先に手順2のバックアップを取る。

(改訂前の本計画は「`data/casino.json` を削除すればリセットできる」を第一の手段として書いていたが、
損失の大きさを過小評価していたため上記のとおり順序と条件を明記した。)

---

## コミット境界(まとめ) — 7分割(ユーザー決定)

改訂前の4分割(A/B/C/D)は Commit C が新規9ファイルという粗さで、bisect と限定 revert が
困難だった。**ユーザーの決定により以下の7分割に再定義する。**

| コミット | 内容 | Task(節) | 各コミット時点でゲートが緑になる根拠 |
|---|---|---|---|
| **Commit 1** | `Register` への重複名panic追加 | Task 1 | `registry.go`/`registry_test.go` のみ。既存8コマンド名は相互に重複していないので既存テスト・起動は不変。他に依存しない。 |
| **Commit 2** | 基盤: 型定義+センチネルエラー+`store.go`(永続化・ロック規律) | Task 2 | `types.go`/`errors.go`/`store.go`/`store_test.go`。`randSource`・全センチネルエラー・上限定数がここに揃うので**前方参照ゼロ**。`Mint`/`SetAnnounceChannel` は当日レート不要なのでここで完結。`internal/commands` からは未参照。 |
| **Commit 3** | `economy.go`(日次レート・両替・デイリー)+ それに依存する Store メソッド | Task 3 + Task 5(`Spin` を除く全部) | `nextRate`/`dailyBonusAmount`/`exchange*` と、それらを使う `EnsureTodayRate`/`ensureTodayRateLocked`/`EnsureCasinoAccess`/`ClaimDaily`/`Exchange*`/`ViewAccount`/`TopAssets`/`topAssetsLocked`/`RecentRates`。`spin` にはまだ依存しない。 |
| **Commit 4** | `slot.go`(配当表・抽選・RTP統計/literal テスト)+ `Store.Spin` + 並行性回帰テスト | Task 4 + Task 5 の `Spin` + Task 7 | `Spin` は `spin` に依存するのでここまで来て初めてコンパイルできる。並行性テストは `Spin`/`ClaimDaily` の両方を使うのでここが最初の可能地点。 |
| **Commit 5** | 掲示ロジック(`announce.go`: `collectDailyAnnouncements`/スケジューラ、discordgo非依存部) | Task 6 | `ensureTodayRateLocked`(C3)と `topAssetsLocked`(C3)に依存。`internal/commands` からは未参照。 |
| **Commit 6** | 7スラッシュコマンド + 共有ヘルパー + `casino_announce.go` | Task 8〜16 | `internal/casino` の公開APIが全部揃っている。15コマンドすべてが `commands.All()` から取れる状態。`main.go` は未変更なのでスケジューラはまだ起動しない。 |
| **Commit 7** | `cmd/bot/main.go` 配線 + ドキュメント更新 | Task 17〜19 | `StartCasinoAnnounceScheduler` は C6 で定義済み。**全ゲート + BL-7 の手動起動確認まで完了してからコミットする — フェーズ全体のDoneはこのコミットをもって確定する。** |

**Task → Commit 対応表(逆引き)**:

| Task | Commit |
|---|---|
| 1 | 1 |
| 2 | 2 |
| 3 | 3 |
| 4 | 4 |
| 5 | **3**(`Spin` とそのテストだけ **4**) |
| 6 | 5 |
| 7 | 4 |
| 8〜16 | 6 |
| 17〜19 | 7 |

**Task 5 が2コミットにまたがる理由(勝手な変更ではなく、依存関係からの帰結)**: ユーザー指定の
分割は Commit 3 = `economy.go`、Commit 4 = `slot.go` だが、`Store` の経済メソッド群(Task 5)は
**そのほとんどが `economy.go` に依存し、`Spin` だけが `slot.go` に依存する**。
「各コミット時点で全ゲート緑」を満たすには、メソッドをそれぞれの依存先と同じコミットに載せるしかない。
これは分割方針の変更ではなく、指定された分割を実現するための機械的な配置である。

各コミットは「1コミット=1論理変更」(CLAUDE.md)に従い、日本語・命令形の件名を付ける(例: `カジノ経済基盤: internal/casinoの永続化層を追加する`)。全コミットで `go build`/`go vet`/`go test` が独立して緑になることを実装完了条件とする(手戻り時に安全にrevertできるようにするため)。
ステージは**宣言スコープのファイルを明示列挙**して行う(`git add -A`/`git add .` は使わない。`CLAUDE.md` 運転規律4)。

---

## 未解決/UNCERTAIN として明示しておく点(実装・レビュー時に確認してほしいもの)

1. **改行コード**: `coding-style.md`が「明示的な指定は無い(UNCERTAIN)」としている。新規ファイルはLF既定で作成する方針に従う。**`.gitattributes` がリポジトリ直下に存在しないことは本改訂で確認済み**(`ls -la .gitattributes` → `No such file or directory`)。したがって Git 側の改行正規化は効いておらず、**エディタ/ツールが CRLF を書き込まないよう実装者が注意する必要がある**。既存ファイルを編集した後は `git diff --numstat` と `git diff --ignore-cr-at-eol --numstat` を比較し、行末の全体書き換えが起きていないことをコミット前に確認する(`CLAUDE.md` 運転規律4)。
2. **参謀コメント・成功メッセージ等の日本語文言**: 設計書が具体例を示している箇所(❌系エラー4件、大入り袋演出、参謀コメントの例文形式、相場コメントの例文形式)は**厳密にその通りの文言・書式**を実装で使うこと。それ以外(✅系成功メッセージ、balance/rank/dailyの具体的な表示レイアウト等)はこの計画が示した文言を**妥当な提案**として実装してよいが、最終的な言い回しはレビューで調整可能(経済ロジック・並行性・権限チェックほど厳密性が要求される箇所ではない)。
3. **スロットの配当表**: Task 4に記載した重み(28/22/21/18/7/4)・倍率(×7/×16/×22/×32/×98/×196)・シード(1)は、計画作成時にPython厳密分数演算+実Go `math/rand`で検証済みであり、**本改訂でも 216 通り全列挙による独立再計算で一致を確認した**(RTP分子 929900、ニアミス分子 18279)。**実装時に`go test`で最終確認すること**(「このプランの数値を鵜呑みにする」のではなく「このプランの数値で書いたテストが実際に通ることを実装時に確認する」のがDoneの条件)。`TestPayoutTable_ExactLiterals` がその受け皿。
4. **Go の doc コメントの言語(裁定済み — 英語)**: `Docs/agent-guide/coding-style.md` は「doc コメントは日本語で書く」としているが、**既存コードベースの実態は全パッケージ英語**である(実測: `grep -rhn "^// [A-Z][A-Za-z]* " --include=*.go internal/ cmd/` で `// Client is a thin HTTP client for chosei-sama's public API...` 等が全パッケージから出る)。**コードベース内の一貫性が、実態と乖離した文書に優先する**という裁定により、本フェーズの Go doc コメントは**既存実態に揃えて英語で書く**(この計画のコード例もすべて英語)。`coding-style.md` 側の是正は**残課題として code-gardening へ送る**(下記)。
5. **起動時キャッチアップの掲示条件(裁定済み — 下限 09:00 あり / 上限なし)**:
   計画改訂の過程で、決定文の「時間窓は切らない」という表現が「時刻を一切問わない」とも読めるため、
   **9時前の再起動でも掲示するのか**が争点になった。**オーケストレーターの裁定により以下で確定**:

   - **掲示の下限は 09:00 JST(あり)**。`now`(JST)が当日 09:00 より前なら**掲示しない**。
     理由: 03:00 に掲示すると `LastAnnounced` が立ち、6時間後の定時パスがスキップされ、
     **その日の掲示が恒久的に深夜へ移動する**。設計書「毎朝9時(JST)にBotが本日のレートを
     指定チャンネルへ自動掲示する」への正面からの違反になる。
     下限 09:00 は「時間窓」ではなく**定時掲示の定義そのもの**である。
   - **掲示の上限は設けない(なし)**。「時間窓は切らない」の意味は**上限を設けない**こと
     (「正午までに限る」のような制限をしない)。23:00 の再起動では**遅れてでも掲示する** —
     その日の 09:00 は既に過ぎて取り逃しているため。
     **深夜に掲示が飛ぶ副作用はユーザーが承知のうえで選択した**もので、想定外の挙動ではない。
   - **レート生成は時刻に関わらず常に行う**(設計書「レートは絶対に欠損しない」)。

   実装上の条件表と根拠は §Task 6 の `announceWindowOpen` / `runAnnouncePass` を参照。
   回帰テストは `..._BeforeNineAM_GeneratesRateOnly`(下限)と `..._LateNight_StillAnnounces`(上限なし)の
   2本で両側を固定してある。**この2本は削除・緩和しないこと。**

---

## 残課題(本フェーズでは直さない。code-gardening サイクルへ送る)

1. **`internal/store/events.go` の `writeLocked` に `tmp.Sync()` が無い** — 新規の `internal/casino` には
   §3.7-2 で追加するため、2つのストアの間に意図的な差異が残る。既存 store は本フェーズの禁止パスなので
   触らない。次のサイクルで既存側にも `Sync()` を足して揃える。
2. **`internal/store/events.go` の `readLocked` は nil を返す3経路を持つ** — 対象が
   `[]EventRecord`(nil スライスに append 可)なので現状は無害だが、将来 map 化されたら
   `internal/casino` と同じ恒久クラッシュループのリスクになる。防御的に `[]EventRecord{}` へ揃えるのが望ましい。
3. **`Docs/agent-guide/coding-style.md` の「doc コメントは日本語で書く」が実態(全パッケージ英語)と乖離** —
   上記 UNCERTAIN 4 の裁定に従い、guide 側を実態に合わせて是正する。
4. **表示専用メソッドが `Update`(JSON全書き戻し)を使っている** — `TopAssets`/`ViewAccount`/`RecentRates`。
   `EnsureCasinoAccess` が先に当日レートを確定させる設計になったので、これらを `Snapshot` ベースへ
   切り替えれば `/balance` `/rank` `/rate` のたびのファイル全書き換えを省ける。現規模では実害なし。
5. **乱数源が `math/rand`(v1)** — 既存 `internal/commands/dice.go:88` と一貫しており、
   `crypto/rand` ではないためシード推測による将来予測が理論上は可能。
   **玩具Botとして許容する**(実害は「同一プロセスの `*rand.Rand` の内部状態を推測できる攻撃者が
   次のスピンを予測できる」というもので、Discord 越しにその情報は得られない)。
   将来、賭けの規模が意味を持つようになったら `math/rand/v2` または `crypto/rand` への移行を検討する。
6. **レートの切替(00:00 JST)と掲示(09:00 JST)の時刻差 — これは意図どおりの仕様**:
   レートは JST 暦日が変わる **00:00 に切り替わり**、掲示は 09:00。したがって 00:00〜09:00 の間は
   「まだ誰にも掲示されていない当日レート」で両替やスロットができる(`/rate` を叩けば本人は見える)。
   早起きの情報優位はゲーム性の一部として**許容する**。C-2 以降でこの前提が揺れないよう明記しておく。
